# 包清单

本包包含 20 个任务合同、终端快速启动说明和首批统一代理提示词、一个总执行书、一个工程代理协议、8 个 Gate、T0-T5 测试规范、证据 schema、迁移/回滚模板和执行脚本。

基线：`6b0d95dfa67465be421d9a4f5eaa5a406e7c3341`。

生成后请运行：

```bash
python delivery/scripts/validate_package.py
sha256sum -c CHECKSUMS.sha256
```

最终文件哈希见 `CHECKSUMS.sha256`。
