# 基线冻结报告 — 6b0d95dfa67465be421d9a4f5eaa5a406e7c3341

- 基线 commit：`6b0d95dfa67465be421d9a4f5eaa5a406e7c3341`
- 执行分支：`refactor/full-system-convergence-v3`
- 冻结时间：2026-08-06
- 执行代理：Claude Code
- Python：3.14.6
- 系统：Darwin (macOS arm64)

## 基线验证结果

### 编译门

| 门 | 命令 | 结果 |
|---|---|---|
| compileall | `python -m compileall -q beidou_* apps` | PASS |
| guard.py | `python -m py_compile beidou_core/guard.py` | PASS |
| AST parse | `ast.parse(guard.py)` | PASS |

### 代码质量门

| 门 | 命令 | 结果 |
|---|---|---|
| 格式化 | `ruff format --check --force-exclude beidou_* apps tests scripts` | PASS (199 files) |
| Lint | `ruff check beidou_* apps tests scripts` | 58 pre-existing errors (not introduced by baseline) |

### 测试门

| 门 | 命令 | 结果 |
|---|---|---|
| 收集 | `pytest --collect-only -q` | PASS (818 tests) |
| 执行 | `pytest tests -q --maxfail=1` | PASS (817 passed, 1 skipped) |

## 基线修复

1. **Python 版本统一**：README.md badge `3.11+` → `3.12+`（与 pyproject.toml `>=3.12` 一致）
2. **ruff exclude 配置**：添加运行时 DB 文件排除规则，确保 `--force-exclude` 生效
3. **.gitignore**：添加 `beidou_state.db*`、`beidou_monitor_state.json`、`beidou_repair_log.jsonl`
4. **格式化**：13 个文件通过 `ruff format` 格式化

## 失败分类

| 类别 | 数量 | 说明 |
|---|---|---|
| SOURCE | 0 | 无源码级阻断错误 |
| TEST | 0 | 无测试失败 |
| ENVIRONMENT | 0 | 无环境问题 |
| EXTERNAL | 0 | 无外部依赖失败 |
| NOT_VERIFIABLE | 0 | 无可验证性问题 |
| PRE-EXISTING LINT | 58 | ruff lint 预存问题（非本任务引入） |

## G0 Gate 状态

- **当前**：PASS（compile、collect、format、test 均可复现）
- **条件**：58 个预存 lint error 需在后续任务中处理
