# Backtest validity report

- Code/data/config: branch `codex/v4-incident-remediation`, base HEAD `818518e8d6a839d014689f30ad66f44d794e4b60`; frozen dataset index SHA-256 `811affae3e2cc2ee5d24a617f4278f8bcf97ed7b208bb92b2b22a83cab572d28`; fixed `trend-alpha-v3-policy-v1`; cost policy SHA-256 `0fe937fbe1e13ad8112f1dd3c2d86c39822533fb97d4c51b8cd9cb4daf50d1a0`.
- Selection boundary: no parameter search, candidate generation, ranking, or choice occurred. This is a frozen-current-policy diagnostic, not an optimization result.
- Availability time: 2,676 signals used history through a closed bar and were applied at the following bar open; `lookahead_violations=0`, `same_bar_executions=0`.
- Evaluation window: 669 hourly observations per symbol after the 51-bar warmup. Strategy and benchmark both use next-bar open-to-close returns, so close-to-next-open movement is excluded symmetrically.
- Portfolio result: four equal-capital sleeves produced about `+4.40%` gross, `+2.40%` after the diagnostic cost schedule, annualized hourly Sharpe about `2.44`, maximum drawdown about `-3.30%`, average absolute exposure about `0.148`, and turnover about `30.18` portfolio units.
- Comparator: the gross, zero-cost, hourly equal-weight price benchmark returned about `+26.91%` with maximum drawdown about `-5.35%`. It is an opportunity-cost comparator, not a costed investable benchmark. The current Alpha materially lagged this rising-market comparator.
- Costs: 6 bps per unit turnover plus 0.125 bps adverse funding per exposure-hour, sourced from the versioned mining policy. A separate governance artifact now contains 120/120 official bookDepth checksums, 86,400 depth samples per symbol, 90 funding observations and 720 hourly-volume rows, but fees/fills/latency remain uncalibrated, terminal liquidation is sensitivity-only, and strategy capacity is `NOT_VERIFIABLE_NO_CANDIDATE_NET_RETURN`.
- Bias/OOS: the authorized range was inspected before an immutable seal. It cannot be relabelled one-shot OOS, and only about 135 evaluable hours post-date the shipped app's commit. The August PIT/feature-lineage reconstruction covers 10 roles and all 2,676 feature/label pairs, but its source vintage is explicitly `NOT_VERIFIABLE_RETROSPECTIVE_RECONSTRUCTION`. No purged walk-forward, CPCV, multiple-testing control, parameter sensitivity, or multi-regime evidence exists.
- Reproducibility: `baseline-report.json` SHA-256 `1968c1ecfbf061540b4ccd378e35f924616d4f6c1434e2810a1e6368853778f1`; repeated run was byte-identical; GREEN2 `3 passed`, impacted regression `33 passed`, Ruff and compileall PASS. A no-context second implementation reproduced every primary/per-symbol metric within `1e-12`; its first turnover disagreement and correction are preserved in `independent-recompute.json` and `12-acceptance-report.md`. After the later ARO-06 remediation, author-run authority/arithmetic verification passed `81` tests and the full repository passed `4823`; the historical independent ARO-06 P1/P2 `FAIL` remains preserved and no independent post-remediation verdict is claimed.
- Status: `DIAGNOSTIC_ONLY / DO_NOT_PROMOTE`.
- Governance inputs: a frozen Metric Owner policy is verified for its original scope; the October 2026 unknown window is sealed as a machine-custodied contract with first read not before `2026-11-02T00:00:00Z`, while the candidate freeze is intentionally absent and no OOS data was accessed. Evidence: `governance/inputs/governance-inputs-report.json`, `governance/cost-capacity/calibration-report.json`, and `governance/pit-lineage/lineage-report.json`.
- Domain gate decision: `BLOCKED`. The run describes the current Alpha and the additive stateful execution contract now passes local remediation tests, but no real bound snapshots/candidate uses it; contemporaneous PIT source custody, one-shot OOS, candidate freeze, WFO/CPCV, sensitivity, multi-regime stability, fill-calibrated costs, impact/capacity, and independent post-remediation ARO-06 acceptance are missing. Improved or deployable return is not demonstrated.
