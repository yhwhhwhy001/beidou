# Task Plan

1. `ARO-00` — `COMPLETE`: froze scope, authority, baseline, protected-work boundary, and `HOLD / NEED_EVIDENCE` economic decision.
2. `ARO-01` — `COMPLETE`: captured RED contracts for provenance, integrity, endpoint identity, manifest parsing, promotion, E0 binding, and scanner behavior.
3. `ARO-02` — `COMPLETE`: implemented schema-v2 provenance, canonical OHLCV identity, source eligibility, cross-source reconciliation, and fail-closed integrity checks.
4. `ARO-03` — `COMPLETE`: connected safe backfill, persisted-manifest local factor mining, zero-promotion API diagnostics, and E0 market-data binding.
5. `ARO-04` — `COMPLETE_WITH_CONDITIONS`: focused regression, registry gates, Ruff, scoped strict mypy, compileall, diff-check, and full regression passed; self-review passed with conditions. Independent acceptance remains absent, so G8 is blocked.
6. `ARO-05A` — `COMPLETE`: acquired the exact authorized Binance USD-M August range into an isolated dataset, verified 120 official checksums, reconciled 720/720 raw rows per symbol against the public API, and passed the market-data admission gate.
7. `ARO-05B` — `COMPLETE_WITH_CONDITIONS`: established a fixed-policy causal diagnostic baseline for the shipped `OfflineAlphaApp`; repeated local execution is deterministic, focused/static/impact checks pass, and a no-context second implementation matches every primary/per-symbol metric within `1e-12`. Conditions are the disclosed economic/OOS limitations, not an arithmetic disagreement.
8. `ARO-06A` — `IMPLEMENTED / LOCAL_REMEDIATION_PASS / POST_REMEDIATION_INDEPENDENT_UNAVAILABLE`: the additive stateful `NO_ACTION`/target and bound cost-source contracts, provider-adversarial validation, canonical result/snapshot digests, typed fail-closed errors, and machine-visible legacy compatibility are implemented. The historical independent verifier's P1/P2 counterexamples were repaired; fresh local authority/arithmetic verification passed `81` tests, the current full repository passed `4823` tests, and no independent post-remediation verdict was available after service usage limits.
9. `ARO-06B` — `IMPLEMENTED / LOCAL_VERIFICATION_PASS / POST_REMEDIATION_INDEPENDENT_UNAVAILABLE`: the governed v2 preseal-to-promotion path, v1 research-only labelling, shared `fcntl.flock`, durable audit append, dual-record seal protocol, v1/v2 OS-process race tests, and crash/partial-state cases pass locally. The historical independent P1 authority bypass is preserved and remediated locally; two post-remediation independent attempts hit the service usage limit. The active instance remains `DRAFT_BLOCKED`; no real seal or OOS read occurred.
10. `ARO-06C` — `BLOCKED`: select or evaluate any Alpha candidate only after a real pre-sealed unseen window, complete PIT/feature lineage, named Metric Owner policy, fill-calibrated cost/impact/capacity evidence, and a new explicit authorization.

`ARO-06A/B` close semantic and governance contract gaps only. They do not unblock the economic `ARO-06C` gate, and no Testnet or Mainnet run is part of this plan.

The immediate next action is a no-author-context behavior verification for ARO-06A/B when independent verifier capacity is available; the historical verifier's pre-remediation `FAIL` remains preserved and cannot be relabelled as post-remediation acceptance. It is not parameter search on August. ARO-06C still requires a precommitted unseen window plus current candidate-scoped ownership, contemporaneous lineage, calibrated-cost evidence and a new explicit authorization.
