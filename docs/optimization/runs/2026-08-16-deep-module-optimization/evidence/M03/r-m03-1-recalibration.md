# R-M03-1 阈值重标定证据(M08 修正内核后回测)

生成: 2026-08-16 · 脚本 `scripts/recalibrate_r_m03_1.py`
数据: .beidou/data/klines/{BTCUSDT,ETHUSDT,SOLUSDT}/1h.parquet(约 17.8k bars ≈ 2 年)
内核: M08 修正后 `simulate_paper_window`(复利 PnL/年化/复利回撤/bars_per_year 参数化)
策略: trend_entry_v1 引擎同语义状态机(Wilder RSI 入场门 + trend_20_pct + trailing ATR 退出)

## 一、阈值扫描(5 RSI 对 × 4 trailing × 3 成本 = 60 配置/品种)

**结论:所有配置 paper_ir 为负,阈值变化对 PnL 无统计区分度。**

| 品种 | 旧阈值(70/30,trail2.0) ir @cost | 网格最优 ir | 网格内 ir 极差 |
|---|---|---|---|
| BTCUSDT | -0.028 / -0.030 / -0.039 | -0.027 (60,40) | < 0.02 |
| ETHUSDT | -0.095 / -0.096 / -0.106 | -0.092 (60,40) | < 0.02 |
| SOLUSDT | -0.162 / -0.163 / -0.167 | -0.153 (60,40) | < 0.02 |

- 满仓单标的口径下 trend_entry 在该数据段无 alpha(3000-4200 笔换仓,
  成本侵蚀主导);相邻阈值配置的 ir 差 < 0.01 —— 任何"最优阈值"
  均为噪声拟合,不构成重标定依据。
- 成本敏感性:cost 1→8bps 时 ir 单调下降(每 +3.5bps ≈ -0.005 ir),
  无阈值交互效应 —— 重标定不能修复成本主导的亏损结构。

## 二、旧 SMA-RSI → 新 Wilder RSI 阈值等效映射

| 品种 | 旧 70 → 新中位数 [p25,p75] | 旧 30 → 新中位数 [p25,p75] |
|---|---|---|
| BTCUSDT | 61.96 [57.6, 66.1] | 36.38 [32.5, 40.1] |
| ETHUSDT | 60.84 [57.9, 65.3] | 36.74 [32.2, 41.4] |
| SOLUSDT | 62.30 [58.3, 66.4] | 36.61 [33.4, 40.4] |

**语义漂移量化**:当前新数学下的 `rsi<70` 入场门等效于旧数学的
≈78(超买门放宽 ~8 点);`rsi>30` 等效于旧数学的 ≈24(超卖门收紧
~6 点)。三品种一致(±1 点)。

## 三、重标定决策(登记)

按 M06 scope 两选项("回测证据重标定 **或** 显式登记风险接受"):

1. **保持现有阈值(70/30, trail 2.0)不变** —— 回测证据表明阈值
   平移不产生可测的 PnL 改善(噪声级),改动只有行为风险没有收益。
2. **显式登记风险接受**:当前阈值在新数学下相对旧数学入场门放宽
   6-8 点。若未来需要恢复旧数学的入场频次语义,等效阈值为
   **(62, 37)**(三品种映射中位数均值),但该平移须有新的 alpha
   证据支撑方可执行(当前证据不支持)。
3. R-M03-3(vol 档位 0.2/0.4/0.6)同族,待其证据轮次一并处理。

## 四、证据文件

- 全量扫描:`r-m03-1-recalibration.json`(60×3 配置 + RSI 映射分布)
- 可复现:`python scripts/recalibrate_r_m03_1.py [symbol...]`
