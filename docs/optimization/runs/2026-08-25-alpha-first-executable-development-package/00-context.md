# Context

- Run ID: `2026-08-25-alpha-first-executable-development-package`
- Repository/version: `/Users/maguannan/beidou` at `d21a9676df24737252c3019d332b41746723977f` (`main`, matching `origin/main` when inspected before package work).
- Applicable instructions: no repository `AGENTS.md` was found; follow `README.md`, `pyproject.toml`, `.github/workflows/ci.yml`, existing `delivery/task-packages/BD-T00..BD-T19`, lifecycle skills, and the approved pivot in `../2026-08-25-alpha-first-architecture-review/03-architecture.md`.
- User authority: create a local, executable and verifiable multi-Agent development package based on the optimized architecture. This does not authorize implementation, Git branch/tag/commit/push, Testnet/Paper activation, exchange/account access, deployment, deletion, or any Mainnet/real-funds capability.
- Technical stack: Python `>=3.12,<4.0`; current `.venv` Python 3.14.7; hatchling; pytest/ruff/mypy; package tooling must use the Python standard library so the extracted development package can validate without repository imports.
- Commands and baseline: `git status --short --branch`, `git rev-parse HEAD`, targeted Alpha/architecture test evidence from the prior review (`127 passed`), package schema/manifest validation, deterministic rebuild comparison, and `git diff --check`.
- Dirty work / protected areas: before this run the repository already had 67 modified/deleted paths (`333 insertions / 3764 deletions`). Those changes belong to the user, must not be cleaned or folded into package claims, and make implementation preflight fail closed until a human chooses a recoverable isolation path.
- Risks and unknowns: five P0 architecture kills remain open for implementation; economic thresholds and human Owners are not fixed; current Mainnet capability is still constructible; prior T24 package evidence belongs to another checkout and cannot be reused as current runtime evidence.
- Selected specialists: `planning-and-task-breakdown`, `requirements-traceability`, `test-strategy-and-coverage`, plus multi-Agent package-convention, task-decomposition, and adversarial-acceptance reviews.
- Deferred specialists: `security-and-hardening`, `deprecation-and-migration`, `backtest-validity-audit`, `market-data-integrity-validation`, `paper-live-parity-validation`, `exchange-order-execution-validation`, and `trading-production-readiness-gate` are mandatory at the task/Gate named in the package, but are not run now because G4 implementation and runtime actions are not authorized.
- Stop conditions: any need to alter or hide pre-existing changes; any credential or account read; any network/exchange write; any Testnet/Paper process action; any branch/tag/commit/push; any proposal to weaken UNKNOWN, reconciliation, protection, Mainnet-deny, evidence, tests, or rollback gates.
- G0 decision: `READY` for package generation only. The versioned first archive is `BD-AF-P0P3-V1`; implementation preflight remains `BLOCKED_DIRTY_BASELINE`.
