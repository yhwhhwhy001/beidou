# 三轮独立对抗式审查

## Round 1：代码与架构
搜索网络旁路、第二事实源、默认值、静默失败、类型豁免、非法状态迁移、事务断裂、旧实现仍被导入。

## Round 2：量化与算法
搜索未闭合 bar、前视/泄漏、幸存者偏差、多重检验、成本/容量遗漏、参数脆弱、regime 依赖、组合集中、错误收益归因。

## Round 3：生产与故障
注入行情中断/乱序/缺口、REST 超时/ACK 丢失/重复、部分成交、撤单竞争、保护拒绝、用户流断开、DB 重启、双实例、磁盘满、时钟漂移、限频、凭据失效、对账差异和极端行情。

每轮必须由独立上下文执行；发现 P0/P1 后修复并重跑全量回归。
