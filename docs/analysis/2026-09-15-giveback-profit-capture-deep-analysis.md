# 深度分析：浮盈回吐与「怎么多拿收益」——0.60 目标下的第一个全天（V5 · 2026-09-15）

> 深度分析 V3.7 · 等级 L · 只分析不执行（操作者原话："先拿到分析结果，并给出方案，不要执行"）。
>
> **Reading Check**：本次理解为「操作者看到 2026-09-14 账户浮盈冲高（按其读数约 +8%）、2026-09-15 回落到约 −1%，期间止盈止损零触发，个别仓位盈亏超过 50% 也未触发；要判断这是否异常，并在不放大风险的前提下给出多拿收益的方案；交付对象是单一操作者；交付物是一份可决策的方案，不改任何配置、不跑任何计费实验」。最高风险预设为 Pre-GB1（P0）「昨天那 +8% 是可以被某条退出规则留住的收益」；若该预设不成立（它是 `vol_target 0.60` 下 1–1.6 个设计日 σ 的正常摆动，且任何按仓位计的退出规则昨天都锁不住它），结论从「改止盈止损」变为「拿收益的杠杆不在退出层：要么接受 0.60 的摆动，要么降 `vol_target`，要么测一个从未测过的书级规则——先写后跑，冻结期内不采纳」。
>
> **Interaction**：Yellow（完整度 🟢🟢🟡🟡🟢🟢：损失是盯市回吐而非已实现损失、口径待澄清；成功标准只有方向） ｜ 等级：**L**，命中：模块 ≥3（退出叠加层、再平衡带、风险预算阶梯、日报仪表）；资金链路（demo，规则将带进真实资金）；AI/策略维度为"自主决策 + 生产影响"；两个维度落在 M/L 之间 → 向上取 L；本档不覆盖：无默认省略，按操作者要求 Phase 9–10 只出契约不执行 ｜ 当前决策上限：**PIVOT**（H2：P0 Claim C-GB01、C-GB02 REFUTED；书级规则分支另受 H5：相对价值 Unproven → 只能以受控实验进入；任何采纳类动作在 2026-10-13 之前受冻结测试与 K-EX14 阻断） ｜ 输出状态：**DONE_WITH_CONCERNS**（三个决定交操作者，各带默认答案与价钱，见 §12；C-GB05 UNKNOWN 为 P1） ｜ 外部动作授权：无（本次只做了只读取证：读仓库、读 `.beidou/live`、跑 `beidou live status` 与 `beidou report daily`——两者只读 demo 账户；没有下单、没有改配置、没有写试验账本、没有提交）。
>
> **修订记录二（2026-09-15，收尾执行时）**：把 DL-GB4 写进 RESEARCH_LOG 时逐条复核，查出本文五处数字/措辞错误，已就地更正并标 **[修订二]**：flatten 跑过两次不是一次、`restarts` 不清锚、`_reconcile` 连 `unit` 一起重算、周期计数混了 SKIPPED、`vol_target` 0.60 的生效时刻是 UTC 09-13T19:00Z 而非 09-14。更正处保留原值以便复核。
>
> **修订记录**：§8 的独立对抗审查（2026-09-15T08:44Z，子代理 `a3411914`）给出 P1 ×5 / P2 ×6，全部 OPEN；本版按其证据逐条改写了 §0、§2.1、§3.1（E-GB12、新增 E-GB18–20）、§4.2、§5、§7、§9–§12，关闭方式见 §8.2 末列。审查者未复审本版；标 **[R 修订]** 的段落是改动处。
>
> ID 命名空间 `GB`（giveback）：E-GB / C-GB / A-GB / D-GB / M-GB / RISK-GB / KILL-GB / DL-GB。Constitution：仓库无 `deep-analysis-constitution.md`；战略与硬约束从 `config/live.demo.yaml` 注释、`docs/RUNBOOK.md`、`tests/live/test_the_construction_is_frozen_until_the_holdout_matures.py` 与 2026-09-07 的退出层分析中读取。本文是 2026-09-07 那份分析的**续篇**：那里已经把"退出层是灾难后备而不是 P&L 机制"裁定为 Q1=B，四个自适应方向全部 REFUTED；本文不重跑那些证据，只回答今天这个新的观察和它引出的一个新分支。
>
> Context Intake（M/L 首轮本应暂停提问，操作者要求一次出结果，故以假设登记）：A-GB01「+8% 的读数来自币安持仓 ROE 或 USDT 权益，而不是权益」——三把尺子都给出换算，结论不依赖哪一把；A-GB02「"控制风险"指不突破 09-14 裁定的 −70% 预算、不把换手推出 D-017 的换手条款」——若指"日内回吐更小"，那是 `vol_target` 议题（§12 Q-CRITICAL）；A-GB03「操作者接受自己 09-14 裁定的构造冻结到 2026-10-13」。

---

## 0. Decision Memo

| 项目 | 结论 |
| --- | --- |
| Final Decision | **PIVOT**（命中 H2；O-GB2 分支另受 H5；采纳类动作在 2026-10-13 前 HOLD）。「止盈止损失职」作为诊断不成立；「多拿收益」作为诉求成立，但杠杆不在退出层：**唯一确定有效的杠杆是 `vol_target`（代价是 CAGR），唯一未测的新分支是书级浮盈锁定（先写后跑，冻结期内可测不可采纳）**。现在不改任何配置。 |
| 一句话问题 | 0.60 目标（生效于 2026-09-13T19:00:25Z，北京时间 09-14 凌晨）的第一个全天：权益从 09-13T23:00Z 低点 10,608 涨到 09-14 约 21:00Z 的 HWM 11,147.6，再回到 09-15T08:00Z 的 10,717.5；退出层零触发；操作者把这读成"10 个点没拿到"。 |
| 对观察的解释（D-GB00 选定框定 F2 + F4） | 低点→HWM +5.09% = **1.62 个设计日 σ**；HWM→现在 −3.86%（−430 U）= **1.28 个设计日 σ**。09-14 起 `vol_target` 是 0.60，设计日 σ 从 168 U 翻倍到 **337 U**；一个 +1.6σ 接 −1.3σ 的 36 小时，在这个目标下正态近似大约每三四周来一次。"8% 对 −1%"不在同一把尺子上：币安持仓 ROE 是价格变动 × 5 倍杠杆（HWM 时 +15%、20:00Z 时 +9%、现在 −2%）；权益尺子上是 +3.3% / −0.65%（相对重建时）；今天 −1.13% 是 UTC 日内对 `day_start_equity`。 |
| 为什么止盈止损没触发 | 按构造不会，而且这次比 09-07 那次更远：18 个仓位的最大有利偏移是 0.15–2.63σ（最高 XRP 2.63σ，BTC 1.36σ），阈值 6σ。把 09-07 已 REFUTED 的最紧档 TP 3σ 装上，昨天**一次也不会触发**。期望退出数 0.41、实际 0，P(0)=66%。 |
| 「个别仓位盈亏超过 50%」 | AKEUSDT：入场 0.01519 → 0.018566，价格 +22.2%，5 倍杠杆下 ROE ≈ **+111%**——这就是操作者看到的数。它的日波动单位是 16.5%，+22% 只算 **1.35σ**；6σ 止盈在 +99%。权重 1.6%，对权益的贡献 ≈ +37 U = 0.35%。**不是异常**：逆波动率定价已经把这种币缩到无关紧要，UI 的百分比是杠杆的放大，不是书的读数。 |
| 按仓位的规则能不能把昨天留住（E-GB12，34 根周期 / 33 个小时区间的重放，E5，n=1） | 不能。持有到现在 −58 U；TP 3σ 零触发；1σ 移动止损（逐小时）−55 U（9 个名字触发，全是当止损用）；「先到 1σ 再挂 1σ 移动」−24 U；唯一为正的是「到 1σ 后挂 0.5σ 移动」+7 U——那是 1–24h 反转区里一条每次来回付 14 bps 的规则，且 5.6 年上 4σ 移动止损的配对读数已经是 −0.016～−0.146 Sharpe。**+8% 是 16 个名字各 0.4–1.4σ 加出来的，没有哪一个名字自己走到任何规则的距离**。审查者用 09-10 平仓前的旧锚反向检验，结论不变（E-GB18）。 |
| 书级规则能不能碰到这次事件 **[R 修订，KILL-GB01]** | 只有书级规则在形状上能碰到（16 个名字合起来才有 1.5σ）。但在规则**自己的锚**（`day_start_equity`，引擎按 bar 日在 01:00Z 周期翻转）上重放：+0.5σ 于 09-14T06:00Z 触发、比持有多 **+10 U**（审查者算 −17，差别在跨日的账法，都是零）；+0.75σ +55 U；+1σ 于 20:00Z 在记录上触发、+103 U（审查者 +80）；+1.5σ **从未触发**（当日峰 1.07σ，HWM 也只 1.49σ）。初稿写的「+0.5σ 多留 +135 U」是用「自重建的浮盈」当锚算的——它与当日 P&L 重合只因为 09-13 的平仓恰好在前一晚，那是巧合不是定义。**这条从未测过，也从未在任何网格里出现过**；它的镜像 `drawdown_throttle` 在 0.60 下被 D-017 判为「太贵的刹车」（两 universe 最优格都 −0.18 Sharpe，限额 1.8 倍）。先验：低。n=1 的数不构成支撑。 |
| 推荐方案 D-GB01 **[R 修订]** | ① O-GB0：不改配置；日报 DL-EX0 加**自 HWM 的跨日回吐 / 设计 σ**一行（今天 1.28σ；现有口径只读当日 0.43σ）与一行**三把回撤尺子的对账**（权益 HWM −3.86% / 阶梯尺 −1.9% / 当日 0.43σ）；UI ROE 那行等操作者答一句「你读的 8% 是哪个数」再写（A-GB01）；② 一页操作者签字的**预期书**「0.60 长什么样」，用操作者自己的单位（ROE、USDT 权益）写：日 σ ±337 U = ROE ±14%、3σ 日 ±1,000 U = ROE ±43%、这次形状约每 6 周一次、按 09-07 §12 你自己接受的异常线（> 2 设计日 σ = 673 U）这次 430 U 不到线；③ Q-CRITICAL 给**默认答案 A（维持 0.60）**并把 B 的价钱写全：0.45 回吐 −25% / pit CAGR 126→108、0.30 回吐 −50% / 126→77，且 B 是 12 天里第 11 次构造变更，M-010、M-G06、`realised_vol` 三个时钟再清零；④ O-GB2 书级浮盈锁定降为 Could：不在本轮写预登记，等 GAP-GB02（0.60 下 30 天跨日回吐分布，与冻结同日 2026-10-13 满期）再议；若议，k 事先定为 `daily_loss_pause` 的镜像（+5% 当日 ≈ 1.6σ）而不是从这次事件挑，账本 tsmom +2 且 flow +2。 |
| Won't | 按仓位更紧的止盈 / 移动止损 / 保本止损 / 部分止盈（本次事件上锁不住任何东西，5.6 年先验为负）；缩仓专用窄带（**[R 修订，KILL-GB06]** 大日之后波动目标想做的不是修剪而是加仓：目标毛敞口之和 18:00Z 1.178 → 20:00Z 1.263，+7%；实际毛敞口反而比目标低 7–14%，是带持住的差——所以这条带即使改了也没有修剪单可发）；交易所原生条件单（D-012）；手动干预（会成为 D-032 的外来成交，污染唯一的样本外记录）；用这 33 根 bar 的任何数字调参（K-EX07）。 |
| 最大价值 / 最大风险 | 价值：把"多拿收益"从退出层移到它真正所在的位置（风险预算），并把"0.60 长什么样"写成操作者签过字的预期，而不是再交付一把尺子——09-07 已经交付过 DL-EX0 和一张异常表，操作者在 1.28σ 上照样问了（KILL-GB03）。风险：RISK-GB01——+103 U 那个 n=1 的数会诱使人跳过第 0 步直接上规则；RISK-GB02——若操作者其实不接受 ±337 U 的日摆动，那么 09-14「绝对收益、预算可重谈」的裁定与今天的诉求互相矛盾；本文的判断是今天的话更像对**路径**的偏好而不是对预算的反悔（今日回撤 −3.9%，自设的第一档在 −49%），所以默认 A。 |
| Strategic Fit / Relative Value / Economic | Medium（O-GB2 是组合层不是信号层，不计入 90% alpha 投入；但它便宜）/ Adequate（No-Build + 仪表拿到全部"读数"价值；O-GB2 的相对价值 Unproven → 只以实验进入）/ N/A（单人项目） |
| 被接受的损失与补偿 | 操作者"马上有一套能锁住浮盈的规则"的即时诉求被延后为一个先写后跑的实验；补偿 = 两行仪表 + 三个带价钱的选项 + 一份写死判读的预登记文本（§11.2）。 |
| 开放项（见 §12，各带默认） | Q-CRITICAL：维持 0.60（默认 A）还是降 k；Q2：「你读的 8% 是哪个数」（一句话，决定 ROE 行写不写）；Q3：DL-GB0 约 15 行 live 顶 KILL-R12 ceiling，抬还是不做。O-GB2 的预登记**不在本轮问**，2026-10-13 后再议。 |
| G0–G7 一行 | G0 PASS · G1 PASS · G2 PASS（C-GB05 UNKNOWN，P1）· G3 PASS · G4 PASS · G5 PASS · **G6 PARTIAL → PASS**（审查 P0 0 / P1 5 / P2 6；本版以新证据关闭 GB01/02/06/07/09，以方案与范围改动关闭 GB03/04/05/08/11；GB10 是 N/A-until-build，见 §8.2）· G7 PASS（契约齐，Delivery Gate 等操作者批准） |

---

## 1. Gate Summary

| Gate | 状态 | 依据 | 未通过项 / 备注 | 决策上限 |
| --- | --- | --- | --- | --- |
| G0 | PASS | 用户、场景、损失、约束、证据齐；无 FATAL | 成功标准只有方向 → K4 WARNING，Phase 3 写出 Success Definition 后解除 | — |
| G1 | PASS | 移除操作者的隐含方案（改止盈止损）后，"少回吐、多拿收益"仍成立；"退出层失职"的缺陷框定不成立 | 区分了症状（回吐）、根因（0.60 的设计噪声 + 只在周级翻转退出 + UI 杠杆放大）、可干预杠杆（风险预算 / 书级规则 / 仪表） | — |
| G2 | PASS | 17 条证据，P0 Claim 全部 E1；唯一 UNKNOWN 是 P1 的 C-GB05 | C-GB05 从未测过 → 不触发 H3；实验分支受 H5 | — |
| G3 | PASS | 比较了 No-Build / 降 k / 书级锁定 / 按仓位收紧 / 缩仓窄带 / 原生单 / 手动 | O-GB2 相对价值 Unproven（H5） | Weak GO（实验） |
| G4 | PASS | 组合层工作，不占 alpha 投入；账本名额是真实机会成本 | D-028 余量：口径 ④ 下 N=259、门 1.5572、OOS 1.5919，余量 +0.035（旧口径约 97 次试验） | — |
| G5 | PASS | 工程预检：O-GB0 落点 `beidou_live/reports.py:824`；O-GB2 可复用 `drawdown_throttle` 的书级标量管线，但进指纹会换 digest（要走 `CONSTRUCTION_ALIASES` 读取侧，或等 10-13） | — | — |
| G6 | PARTIAL → PASS | 独立子代理（§8）：最强反方论点部分成立；C-GB01/02 REFUTED 经复核与反向检验维持；C-GB05 的 n=1 支撑被撤销 | 5 条 P1 全部要求改数字 / 方案 / 范围，本版已改；审查者未复审本版 | PIVOT（内容按 §8 修改） |
| G7 | PASS（分析交付物） | 契约与 Source Trace 齐；实验的 Delivery Gate 以操作者批准为人类确认点 | 不得在批准前运行计费实验；冻结期内不得改构造 | — |

命中硬门禁：**H2**（P0 Claim C-GB01 / C-GB02 REFUTED → PIVOT）、**H5**（O-GB2 相对价值 Unproven → 上限 Weak GO，只以受控实验进入）。未命中 H1（审查 P0 = 0）、H3（UNKNOWN 的 C-GB05 是 P1）、H4（P0/P1 主要靠 E1）、H8（demo 范围、边界已知）。

---

## 2. Phase 1 · Reality Check

### 2.1 需求四问

| 问 | 答 | 证据 |
| --- | --- | --- |
| 谁的需求 | 单一操作者，同时是风险承担者与审批者 | — |
| 频率 / 规模 / 损失 | 规模：HWM→现在 −430 U（−3.86%，1.28 设计日 σ）；低点→HWM +539 U（+5.09%，1.62σ）。频率：在 0.60 下，单日 ≥ +1.6σ 的上涨正态近似约每 18 个交易日一次，\|日收益\| ≥ 1.2σ 约每 4 天一次——这不是尾部事件。损失：**没有已实现损失**；今天 UTC 日 −1.13%；相对 09-13T21:00Z 重建 −0.65%。"没拿到"的是盯市峰值与现在的差 | E-GB01、E-GB02 |
| 现在怎么绕过、付出什么代价 **[R 修订，KILL-GB02/03]** | 绕行有两种。一是提问：8 天内第 4 次「要更多收益」的提问（09-07 退出层回吐 65 U；09-12 成交频率；09-14 自适应杠杆「第五次被问」；今天），其中专门关于回吐的是第 2 次；而 09-07 那次已经交付了 DL-EX0 噪声尺度行和一张「什么才算异常」表（> 2 设计日 σ），操作者在 1.28σ 上照样问了——**「给一把尺子提问就会停」这个前提已经失败过一次**。二是手动干预：09-13T20:37Z 操作者下令「全部平仓并重启整个量化交易系统」，`live flatten --yes` 平掉 17 张（RESEARCH_LOG 9378）。**[修订二]** 初稿把 `trades.jsonl` 里 32 笔 `bdflat-` 全算给这一次；按成交时间拆开是 09-04T17:09Z 的 15 笔 + 09-13T20:37Z 的 17 笔——`live flatten` 这 12 天跑过**两次**。确凿的空仓重开 4 次（09-04T06:53Z、09-04T17:21Z、09-10T06:00Z、09-13T21:00Z；09-03 那两根的 `gross_before` 整段读 0，不可信）。代价：每次**平仓→重开**都重置全部退出锚（E-GB18），M-010 与 `realised_vol` 的时钟清零。**重启本身不清锚**——`exit_states` 持久化在 `state.json` 里，所以那 47 次重启（初稿写 46，冻结时的值）不属于这条代价 | E-GB18、`.beidou/live/trades.jsonl`、`docs/RESEARCH_LOG.md:9378`、09-07 分析 §12 |
| 现有方案能否满足大部分需求 | 这本书"拿收益"的机制有三个：周级信号翻转（主机制，中位 17 次/币·年）、波动目标再平衡（修剪赢家，但被 40% 相对带压制）、6σ 退出层（几乎不触发的灾难后备）。三者按设计都**不在日内尺度**拿收益 | E-GB08、E-GB09 |

方案型输入还原：操作者没有提出具体方案，但问题的提法（"没有产生止盈止损单"）预设了退出层是拿收益的工具。移除该预设后，Problem 是「这本书在 0.60 目标下的日内摆动比操作者的直觉大，且操作者没有一把能读跨日回吐的尺子」。

### 2.2 需求来源与偏差

| 项 | 内容 | 折减 |
| --- | --- | --- |
| 渠道 | 操作者直接观察 | 陈述证据 E2（是对 UI 的一次读数） |
| 样本 | n=1 事件（0.60 目标的第一个全天） | 推广到"这本书经常这样"需要 0.60 下的 30 天分布，现在只有 1.5 天 |
| 已知偏差 | **近因**（第一天翻倍风险就撞上一个 1.6σ 的日子）；**尺子锚定**（UI 的 ROE 把价格变动放大 5 倍；USDT 权益把抵押品剔除后放大约 2 倍）；**框架**（把浮盈峰值当作已属于自己的收益） | 三把尺子在 §3.1 E-GB16 给出换算；结论用权益尺子 |

### 2.3 需求预设清单

| Pre ID | 提出者默认成立的前提 | P级 | 依据 | 若不成立会改变什么 | 处理 |
| --- | --- | --- | --- | --- | --- |
| Pre-GB1 | 昨天的 +8% 是"可以拿到"的收益，某条退出规则能把它留住 | **P0** | E-GB04、E-GB12 | 方向从"改退出层"变为"风险预算 / 书级规则 / 仪表" | → A-GB04，已由 E-GB12 检验：**不成立** |
| Pre-GB2 | 单个仓位盈亏 > 50% 未止盈是异常 | P1 | E-GB05 | 若成立需查退出层缺陷 | 不成立：ROE = 价格 × 5，AKE 1.35σ |
| Pre-GB3 | 止盈止损是这本书拿收益的机制 | P1 | 09-07 分析 Q1=B | 若成立，退出层应改为主动 P&L 机制 | 09-07 已裁定为灾难后备，四个方向 REFUTED |
| Pre-GB4 | "控制风险"与当前 −70% 预算相容 | P1 | E-GB06、E-GB07 | 若操作者其实不接受 ±337 U 的日摆动，议题是重开 P32，退出层无关 | → §12 Q-CRITICAL |
| Pre-GB5 | "8%" 与 "−1%" 在同一把尺子上 | P2 | E-GB16 | 只影响表述 | 不在：ROE / 权益 / USDT 权益 |

### 2.4 Early Kill / G0

K1 否（观察真实、回吐真实）；K2 否（P0 全 E1）；K3 否；**K4 WARNING**（成功只有方向）→ §4.4 解除；K5 否；K6 部分（任何新规则的价值对复杂度，见 O-GB2 定价）；K7 否；K8 否。**G0 PASS**。

---

## 3. Phase 2 · Evidence Ledger

### 3.1 证据（全部可复核）

**E-GB01（E1）** `.beidou/live/cycles.jsonl` 权益路径，0.60 重建之后：09-13T21:00Z 重建 10,787.9 → 09-13T23:00Z 低点 10,608.0（浮亏 −168.6）→ 09-14T20:00Z 11,008.8（浮盈 +220.8，重启前最后一根完整周期）→ HWM **11,147.6**（写在 09-14T22:00Z 那行的 `throttle.equity_hwm`；20:24:52Z 重启 #46，19:00Z/21:00Z 两根周期只有心跳行）→ 09-15T03:00Z 10,862.7（今日日内峰）→ 09-15T08:00Z **10,717.5**（浮亏 −49.0）。`day_start_equity` 09-15 = 10,840.5。

**E-GB02（E1）** 设计日 σ（0.60 自 2026-09-13T19:00:25Z 生效）：`vol_target 0.60 / √365` = 3.14% = **336.6 U**（日报 DL-EX0 同值）；0.30 时 168 U，0.45 时 252 U。实测：重建后 33 根小时收益的日化 σ 2.34%（年化 0.45，低于设计，样本太短）；日报的 30 天实测 148.6 U 主要是 0.30 时代的周期。**换算：低点→HWM +5.09% = 1.62σ；重建→HWM +3.33% = 1.06σ；HWM→现在 −3.86% = −430 U = 1.28σ（按 U）。**

**E-GB03（E1）** `exit_events`：**[修订二]** 338 根 armed 行里累计 0——其中完整周期 307 根（写本节时 309），另有 27 根 SKIPPED、4 根 ERROR；初稿写的「336 根 armed 周期」把 SKIPPED 与 ERROR 的行也数成了周期，两个口径下 `exit_events` 都是 0。日报 `expected_exits_so_far 0.41`、`exits_so_far 0`，Poisson(0.41) 下 P(0) = 66.4%。M-005 反事实 `n_needed_for_decision 16`，events 0。

**E-GB04（E1）** 18 个 `exit_states` 对最新收盘的 σ 偏移（`unit` = 入场时日波动）：

| 币 | 权重 | 现在 σ | 最大有利 σ | 6σ 止盈在价格 | UI ROE（5x） |
| --- | ---: | ---: | ---: | ---: | ---: |
| BTCUSDT | 0.150 | −0.35 | 1.36 | +9.3% | −2.7% |
| BNBUSDT | 0.150 | −0.52 | 0.30 | +11.5% | −5.0% |
| SOLUSDT | 0.105 | −0.37 | 0.65 | +16.2% | −5.1% |
| ETHUSDT | 0.101 | −0.55 | 0.47 | +17.1% | −7.8% |
| DOGEUSDT | 0.096 | −0.77 | 0.15 | +17.7% | −11.3% |
| TUTUSDT（空） | −0.087 | +0.22 | 0.68 | +48.6% | +8.7% |
| HYPEUSDT | 0.083 | −0.09 | 0.89 | +20.4% | −1.5% |
| XRPUSDT | 0.079 | +0.84 | **2.63** | +17.4% | +12.1% |
| SUIUSDT | 0.075 | −0.63 | 0.52 | +22.2% | −11.6% |
| ADAUSDT | 0.074 | −0.72 | 0.42 | +22.8% | −13.6% |
| 1000PEPEUSDT | 0.067 | −0.16 | 0.51 | +26.5% | −3.4% |
| CYSUSDT（空） | −0.056 | −0.57 | 0.23 | +74.0% | −35.0% |
| UNIUSDT | 0.049 | +0.84 | 1.30 | +32.4% | +22.7% |
| ZECUSDT | 0.044 | +0.50 | 1.31 | +36.0% | +15.0% |
| LINKUSDT | 0.035 | −0.32 | 0.73 | +19.8% | −5.3% |
| AKEUSDT | 0.016 | **+1.35** | 1.51 | **+98.8%** | **+111%** |

（ENAUSDT、TRUMPUSDT 已移出 universe，状态残留。）**没有一个名字曾到过 3σ。**

**E-GB05（E1）** AKEUSDT：入场 0.01519（09-13T21:00Z 成交 0.015202）、收盘 0.018566、`unit` 0.1647、年化 σ 3.25；价格 +22.2% = 1.35σ；名义 ≈ 170 U，贡献 ≈ +37 U = 0.35% 权益。`leverage_set` 全部 5。

**E-GB06（E1）** 风险姿态（`config/live.demo.yaml`、RESEARCH_LOG 9285–9340）：`vol_target 0.60`、预算 −70%、自助 q95 −68.8% / −69.7%、P(击穿 −50%) 53% / 59%；阶梯 −49% → 0.45、−70% → 0.30；`drawdown_throttle` 在 0.60 下重跑 D-017 两个 universe 0/6，最优格 −0.18 Sharpe（限额 0.10）换 5.1pp / 1.5pp 回撤——「太贵的刹车」；`daily_loss_pause −5%` 只禁加仓不减仓；研究日志原话「k=0.60 下这个账户仍然基本没有回撤刹车」。

**E-GB07（E1）** k 阶梯（profile 96–100 行，P32，registry 书，双 universe）：

| k | CAGR pit / static | q95 MDD pit / static | P(击穿 −50%) | 设计日 σ |
| --- | --- | --- | --- | --- |
| 0.30 | 77.3% / 56.7% | −44.1% / −50.3% | 1.2% / 5.3% | 168 U |
| 0.45 | 108.3% / 87.2% | −59.0% / −63.1% | 20.1% / 30.3% | 252 U |
| **0.60** | **126.5% / 119.1%** | **−68.8% / −69.7%** | **53.4% / 58.6%** | **337 U** |

**E-GB08（E1）** 再平衡带：`no_trade_band 0.005`（≈54 U）、`no_trade_rel_band 0.40`（同向调整）；最近 60 根周期 995 次 NO_TRADE_BAND；今天 128 个币·周期被带持住；P10 cell B：带 0.25→0.40 换来 +0.142 Sharpe（"holding through"）；09-12 分析：对称带 0.20 每年 −8.45%（t 4.46）。

**E-GB09（E1）** 退出层既有证据，按网格点名（K-EX01）：止损只测过 {2.5, 4, 6, 8}σ，2.5/4 不过 D-017；移动止损只测过 {0, 4}σ，4σ 配对 ΔSharpe pit −0.0156～−0.1438、static −0.0293～−0.1463（0.15 时）；止盈测过 {0, 3, 4, 6}σ，3σ static Sharpe −0.1736（MDD +6.34pp）、4σ static 回撤不过、当前 σ 单位双不过（P22，0.30）；行情切档 2×2 跨 universe 不稳（P22b）；信号衰减 t=0.92（P23）。**k<3 的止盈、k<4 的移动止损、保本止损、部分止盈从未进过任何网格。**

**E-GB10（E1）** M-EX01 基线（`scratchpad/m_ex01_retention.py`）：持仓段 MFE 中位 0.39%（pit）/ 0.70%（static），p75 是中位的 2.6–3 倍；留存比中位为负。这本书的收益在右尾。

**E-GB11（E1）** 采纳约束（**[R 修订，KILL-GB09]** 证据窗 36 bar 的成立依赖 `CONSTRUCTION_ALIASES`：09-14T20:00Z 起周期写的是 `ccd7bb…`，读取侧归一到 `46b8d7…`）：`tests/live/test_the_construction_is_frozen_until_the_holdout_matures.py`：`FREEZE_ENDS = 2026-10-13T19:00Z`、`FROZEN_CONSTRUCTION 46b8d731…`（操作者 09-14 裁定）；RUNBOOK K-EX14：M-010 不满 30 天连续构造不采纳退出/信号改动；日报证据窗：构造 `46b8d731530a` 自 2026-09-13T18:00Z、36 根 bar；D-028：口径 ④ 下 N=259、门 1.5572、OOS 1.5919（`governance/verdicts.jsonl` 2026-09-14T17:45Z），旧口径余量 +0.0369 ≈ 97 次试验。

**E-GB12（E5，n=1，K-EX07 豁免范围内的描述性重放：34 根 armed 周期 / 33 个小时区间，09-13T21:00Z → 09-15T08:00Z，09-14T19:00Z 与 21:00Z 缺，不得据此改配置）** 用 `exit_states` 的入场锚与 `cycles.jsonl` 每根周期的 `closes` 逐小时重放；名义按 `targets × equity`——**[R 修订，KILL-GB06/09]** 实际毛敞口比目标之和低 7–14%（`gross_before / (Σ|targets| × equity)` = 0.926 于 18:00Z、0.861 于 20:00Z），所以下表每个 U 数高估约一成，触发名单与次数不受影响（审查者独立重放，逐条一致）：

| 规则 | 窗口末 P&L | 触发 |
| --- | ---: | --- |
| 持有到现在 | −58 U | — |
| 止盈 3σ（P22 已 REFUTED 的最紧档） | −58 U | **0 次** |
| 移动止损 1σ，不设武装门槛 | −55 U | 9 次，全部当止损用（−0.3～−1.1σ） |
| 移动止损 2σ | −154 U | 1 次（CYS 空头） |
| 先到 +1σ 再挂 1σ 移动 | −24 U | 3 次（BTC +0.29σ、XRP +1.59σ、ENA） |
| 先到 +1σ 再挂 0.5σ 移动 | **+7 U** | 4 次 |
| 先到 +1.5σ 再挂 1σ 移动 | −40 U | 1 次（XRP） |
| 书级 +0.5σ，锚 = **自重建的浮盈**（初稿的算法，**不是规则的锚**） | +86 U | 09-14T20:00Z；比持有多 +135 U——**作废**，见下四行 |
| **书级 +0.5σ，锚 = `day_start_equity`（规则自己的锚，01:00Z 周期翻日）** | −60 U | 09-14T06:00Z（+0.52σ）；比持有 **+10 U**（审查者 −17 U，差在跨日账法；都是零） |
| 书级 +0.75σ，同锚 | −16 U | 09-14T10:00Z（+0.79σ）；+55 U |
| 书级 +1.0σ，同锚 | +33 U | 09-14T20:00Z（+1.07σ，**在记录上**，不在重启空档）；+103 U（审查者 +80） |
| 书级 +1.5σ，同锚 | −70 U | **从未触发**：当日峰 1.07σ（周期行）、1.49σ（HWM） |

**E-GB18（E1，审查者补入，本会话两遍复核）** 09-13T20:37Z `live flatten --yes` 平 **17** 张（**[修订二]** 不是 32；32 是 09-04 的 15 笔加这次的 17 笔），21:00Z 重开 17 张：`beidou_live/exits.py:93` 的 `_reconcile` 在 `held` 由 0 变 ±1 时采用交易所 VWAP 作新锚，`entry_price`、`extreme` **以及 `unit`**（**[修订二]** 初稿漏了第三个）三个量同时从新价重算，所以**每次平仓→重开都把全部退出锚清到当前价**；**重启不走这一支**（同向重启只 `replace(state, extreme=…)`）。12 天里确凿的空仓重开 4 次。用 09-10T06:00Z 重开时的 18 笔成交均价当锚反向检验：审查者报 BTC +0.39σ / XRP +1.81σ / ETH +0.90σ，本会话第二遍报 BTC +0.38σ / XRP +1.91σ / ETH +1.25σ（差别在 σ 取哪根周期、最大值取整窗还是取 20:00Z 那根）——两遍都是**最大值在 1.9σ 一带，无一到 3σ**，C-GB01 对锚点重置稳健；但「为什么从来没有止盈止损」的另一半答案是：6σ 是几周的路，而锚每几天被清一次。这条副作用此前无处记录。详见 `docs/RESEARCH_LOG.md` 2026-09-15 「补记 DL-GB4」一节。

**E-GB19（E1，审查者补入，本会话按比例复核）** 大日之后目标毛敞口之和 18:00Z 1.178 → 20:00Z 1.263（+7%）；波动目标想做的是加仓不是修剪。

**E-GB20（E5，正态 + 漂移蒙特卡洛，60 年小时路径，漂移取 pit CAGR 126.5%）** 任意 11 小时跌 ≥ 3.86%：约每 16 天一次；**紧接 30 日新高之后**的这种跌：约每 42 天一次（审查者 47 天）；单个日历日 ≥ +5.09%：约每 15 天一次（审查者 17.7）。肥尾下更频繁。同一个 430 U 按 11 小时的 σ 读是 **1.89σ_11h**，按日 σ 读是 1.28σ——日 σ 把快速回吐读小，仪表要明示尺度（KILL-GB07）。

**E-GB13（E1）** 抵押品：权益的 53% 是非 USDT 抵押品；日报 RISK-G11 在 182 根周期上重定价占权益变动 41%。**但本次 36 小时里 Δ权益 ≈ Δ浮盈**（+400.8 对 +389.4；−291 对 −270）——这次事件是仓位驱动的，不是抵押品。

**E-GB14（E1）** DL-EX0 的 `peak_giveback` 只在**当日**内算（`beidou_live/reports.py:824`，"the largest drop from the running equity high inside the day"）：今天读 145.2 U = 0.43σ；操作者的事件是跨日的 430 U = 1.28σ。**仪表读不到操作者看到的那个数。**

**E-GB15（E1）** 信号状态：tsmom `contributions` 全部 ±1 未变，无翻转；13 多 / 2 空（CYS、TUT）+ flow 空头；峰值→现在是宽基 beta（BTC −2.6%、ETH −2.9%、XRP −4.8%、ZEC −4.6%）。

**E-GB16（E1）** 三把尺子：初始保证金 = gross / 5（20:00Z gross 12,021）= 2,404 U → HWM 浮盈 ≈ +360 U 是 **ROE +15.0%**、20:00Z +221 U 是 +9.4%、18:00Z +165 U 是 +7.1%、现在 −49 U 是 −2.1%；USDT 权益 4,932（09-13T23:00Z）→ 5,312（20:00Z，+7.7%）→ 5,040；权益 +3.3% / −0.65%（相对重建）。**操作者的"8%"最可能是 ROE 或 USDT 权益；书按权益设计。**

**E-GB17（E3/E4，09-07 分析附录 A.1 转引）** Kaminski & Lo：止损规则在收益正自相关尺度上有正溢价、负自相关尺度上为负；止盈是镜像。E-EX09：本 universe 1–24h 前瞻 IC 为负（反转）、周级为动量。

### 3.2 Claim Register

| Claim | 命题 | P级 | Axiom | Falsifier | 证据 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| C-GB01 | 昨天止盈止损"失职" | P0 | A1/A6 | 任一仓位到过 ≥ 3σ 而未触发 | E-GB03/04/12 | **REFUTED**（最大 2.63σ；TP3 零触发） |
| C-GB02 | +8% → −1% 是异常 | P0 | A2 | 回吐 > 2 设计日 σ | E-GB01/02/07 | **REFUTED**（1.62σ / 1.28σ；0.60 的第一天） |
| C-GB03 | 单仓位 ROE > 50% 未止盈是异常 | P1 | A1 | 该仓位 σ 偏移 ≥ 6 | E-GB05 | **REFUTED**（1.35σ；ROE = 价格 × 5） |
| C-GB04 | 按仓位的退出规则能把这类书级浮盈变成已实现收益 | P1 | A3 | 某个按仓位变体在 n=1 上显著为正且 5.6 年先验不为负 | E-GB09/12 | **REFUTED**（最好 +7 U，且在反转区） |
| C-GB05 | 书级浮盈锁定在 5.6 年双 universe 上净增收益且不增回撤 | P1 | A6/A7 | 第 0 步条件均值不显著为负，或 overlay 不过 D-017 | E-GB06（镜像 −0.18，限额 1.8 倍）；E-GB12 在规则自己的锚上 n=1 为 0～+103 U，不构成支撑；E-GB17 是单币横截面 1–24h IC，**不能推到书级日收益自相关** | **UNKNOWN**（从未测过；先验 **低**，**[R 修订，KILL-GB05]**） |
| C-GB06 | 降 `vol_target` 是唯一确定缩小回吐的杠杆 | P1 | A3 | 存在另一个已验证机制按比例缩小日摆动 | E-GB07、P13/P32 | SUPPORTED（代价 CAGR） |

### 3.3 Assumption Register

| A | 假设 | Impact × Risk | 最小验证 | 决策上限 |
| --- | --- | --- | --- | --- |
| A-GB01 | "8%" 来自 ROE 或 USDT 权益 | 低 × 低 | 操作者一句话 | 不影响结论 |
| A-GB02 | "控制风险" = 不突破 −70% 预算、不出换手条款 | 高 × 中 | §12 Q-CRITICAL | 若不成立 → 议题变为重开 P32 |
| A-GB03 | 操作者维持冻结到 10-13 | 高 × 低 | 冻结测试仍在 | 不成立 → 任何采纳提前但重置 M-010 |
| A-GB04（=Pre-GB1） | 某条按仓位规则能留住昨天 | 高 × 已验 | E-GB12 | 已检验：不成立 |
| A-GB05 | 书级日收益在 ≥ +1.6σ（`daily_loss_pause` 镜像）之后有负的条件均值 | 高 × 高 | O-GB2 第 0 步（2026-10-13 后再议是否预登记） | 不成立 → O-GB2 关闭 |

### 3.4 Gap Plan

| Gap | 缺什么 | 不确定性 × 决策影响 | 获取路径 | Owner |
| --- | --- | --- | --- | --- |
| GAP-GB02（先） | 0.60 下跨日回吐的实际分布（30 天） | 中 × 高 | 自然积累 + DL-GB0 仪表；2026-10-13 满期，与冻结同日 | 循环 |
| GAP-GB01（后） | 书级日收益的滞后一阶自相关与条件均值（5.6 年，双 universe） | 高 × 高 | O-GB2 第 0 步：scratchpad 脚本复现 shipped 构造的书级日收益（`research validate --out` 不导出序列，需自写）；账本 **tsmom +2 且 flow +2**（registry 书含 flow_short）；只在 GAP-GB02 显示 ≥ +1.6σ 的日子足够多时才值得 | 操作者 10-13 后批准 |

---

## 4. Phase 3 · Problem Research

### 4.1 Problem Statement

操作者在 `vol_target 0.60` 的第一个全天看到一个 +1.6σ / −1.3σ 的往返，用币安 UI 的杠杆化百分比读它，读成"10 个点没拿到"；系统没有任何仪表告诉他这次回吐在设计尺子上是多少（DL-EX0 只读当日），也没有任何机制在日内尺度上把浮盈变成已实现收益——按设计如此。

### 4.2 Causal Chain

`vol_target` 0.30 → 0.60（P32，操作者裁定，预算 −70%；裁定与提交记在 09-14，**[修订二]** 实际生效是 **2026-09-13T19:00:25Z** 那根周期——`risk_ladder.base_vol_target` 在该根由 0.3 变 0.6、构造 digest `18b8b20f` → `46b8d731`、毛敞口 5,885 → 12,183；「09-14」是北京时间）→ 设计日 σ 168 → 337 U → **09-13T20:37Z 操作者下令全书平仓再重开，全部退出锚清到 21:00Z 的价（E-GB18）** → 一个普通的 1.6σ 上涨日 = +540 U → 16 个名字各涨 0.4–1.4σ，没有一个到任何退出距离（即使用 09-10 的旧锚也没有）→ 周级信号未翻 → 持有 → 宽基回落 −1.3σ（按 11 小时尺度 1.9σ）→ 权益回到重建附近 → UI 的 ROE 把 ±3% 显示成 ±15% → 操作者读成异常。

### 4.3 Problem Reframing（D-GB00）

| 框定 | 一句话 | 区分证据 | 支持度 |
| --- | --- | --- | --- |
| F1 参数问题 | 止盈止损太宽 | 某条更紧的按仓位规则昨天能触发并留住 | **排除**（E-GB04/12：TP3 零触发；最好的变体 +7 U） |
| F2 尺度问题 | 是 0.60 的设计噪声 | 回吐 / 设计 σ | **支持**（1.28σ；第一天） |
| F3 工具缺失 | 书级日内尺度上没有任何规则 | 书级规则在 n=1 上能碰到事件，且 5.6 年未测 | **弱**（**[R 修订]** 在规则自己的锚上 n=1 为 0～+103 U；C-GB05 UNKNOWN、先验低） |
| F4 尺子问题 | 三把尺子 + 仪表只读当日 | E-GB14/16 | **支持** |

**D-GB00**：F2 解释观察；F4 解释"8 对 −1"的表述，但**不**解释反复提问（09-07 的尺子没拦住，KILL-GB03）——反复提问的根因是操作者的目标（路径、锁定、资金效率）与治理目标（D-017 的 Sharpe/回撤）不是同一个，本文对此的交付物是预期书而不是又一把尺子；F3 是唯一可证伪的新分支但先验低；F1 被证据排除（不是被推理排除——E-GB12 是本次事件上的直接重放，E-GB09 是 5.6 年上的网格）。

### 4.4 Success Definition（写在方案之前）

| Metric | 类型 | 指标 | 基线 | 为什么量的是问题 |
| --- | --- | --- | --- | --- |
| **M-GB01** | 领先（问题级） | **自 HWM 的跨日回吐 / 设计日 σ**（= `throttle.drawdown × equity / design_daily_sigma_u`），日报每日一行；30 天后给分布 | 今天 1.28σ；0.60 下预期：大多数日子 < 1σ，每月 1–2 次 > 1.5σ | 直接量"回吐相对设计有多大"，与用什么规则无关 |
| M-GB02 | 滞后 | M-010 30 天归因 Sharpe vs 预期（已有） | 日报 | 盈利本身 |
| M-GB03 | 护栏 | D-017 双 universe（OOS Sharpe 损失 ≤ 0.10 且 MDD 改善）+ 换手条款（上升 ≤ 25%、成本占毛利 ≤ 15%） | validate | 防止"留住浮盈"靠砍掉右尾或靠换手 |

（O-GB2 分支级指标 M-GB04 已移到 §7.1 O-GB2 名下，不再放在方案之前的表里。）
| M-GB01 的可证伪阈值 **[R 修订，KILL-GB07/08]** | 问题级 | 30 天内 > 1.5σ_d 的跨日回吐 ≥ 3 次，或任一 > 2σ_d（673 U）→ 本文「设计噪声」的框定需重审；否则维持。回吐同时按 11 小时 σ 报一遍 | 今天 1.28σ_d / 1.89σ_11h | 让 F2 能被推翻 |

### 4.5 Stakeholder（含受损方）

| 角色 | 诉求 | 反对点 / 受损 |
| --- | --- | --- |
| 操作者 | 少回吐、多拿、少操心 | 不想等；09-14 刚裁定 0.60 |
| 实盘记录与冻结（KILL-006、冻结测试） | 不被构造变更重置 | 任何采纳都重置 M-010，且 10-13 前会让冻结测试变红 |
| tsmom / flow 账本 | 不被组合层实验膨胀 | O-GB2 第 0 步 +1/universe、网格 +6/策略 |
| 未来维护者 | 参数少 | O-GB2 +3 参数（k、f、锚） |

### 4.6 Edge Cases（进入 Test Contract）

| 情形 | 要求 |
| --- | --- |
| HWM 落在重启空档（本次） | M-GB01 用 `state.equity_hwm`，它由每根周期与心跳更新，不依赖完整行 |
| 外部转账重定基线 | M-GB01 沿用 `external_flows.rebaselined` 的跳过规则 |
| O-GB2 与 `daily_loss_pause` 同日 | 两者锚点都是 `day_start_equity`；先触发者生效，另一者只读 |
| O-GB2 触发后信号翻转 | 翻转仍即时（scalar 只乘权重，不改方向） |

---

## 5. Phase 4 · Relative Value / Strategic / Economic

| 候选 | 对本次事件（n=1） | 5.6 年证据 | 代价 | 结论 |
| --- | --- | --- | --- | --- |
| 不行动 + 仪表（O-GB0） | 读数 1.28σ | — | ≈15 行 live | 基线；拿到全部"读数"价值 |
| 降 k（O-GB1） | 0.45：回吐 −25%；0.30：−50% | E-GB07：CAGR 126→108→77（pit） | 一行 + 重开 P32 + 重置冻结/M-010 | 唯一确定有效；是预算议题 |
| 书级锁定（O-GB2） | 规则自己的锚上 0～+103 U（k=1.5 不触发） | 无（镜像 −0.18） | 第 0 步 tsmom +2 / flow +2；网格再 +2/+2（单一 k）；≈80 行代码 | Unproven → 10-13 后再议 |
| 按仓位收紧（O-GB4/5） | ≤ +7 U | 负（E-GB09） | 0 代码 | Won't |
| 缩仓窄带（O-GB3） | 0（大日后目标毛敞口 +7%，没有修剪单可发，E-GB19） | 对称带 0.20 −8.45%/yr | — | Won't |
| 原生条件单 / 手动 | — | D-012 / D-032 | — | Won't |

Strategic Fit：Medium——O-GB2 是组合层，不占 90% alpha 投入，但它便宜且回答的是操作者反复提出的那个问题。Cost of Delay：低——冻结到 10-13 之前什么都不能采纳，所以 4 周内"先测"与"后测"对上线时间没有差别；差别只在账本名额何时花。**[R 修订，KILL-GB04]** 反过来，Q-CRITICAL 若答 B（降 k）是 12 天里第 11 次构造变更：M-010、M-G06、`realised_vol` 单构造条件与 L3 的时钟全部再清零，冻结测试要改常量——这个价钱在 09-14 的裁定里没有，写在这里。Economic：N/A（单人）。

---

## 6. Phase 5 · System Analysis

### 6.1 As-Is / To-Be

| 层 | As-Is | To-Be（若 O-GB0 / O-GB2） |
| --- | --- | --- |
| 日报 DL-EX0 | `peak_giveback` 当日内；无 ROE 换算 | 加 `giveback_since_hwm_u` 与 `_in_design_sigma`；加 `ui_roe_pct = unrealized / (gross / leverage)` |
| 组合层书级标量 | `drawdown_throttle`（disabled，回撤触发、线性缩） | 复用同一标量管线，新增上涨触发：当日 P&L ≥ +kσ → scalar f 到下一 UTC 日 |
| 研究侧 | `validate` 重放 guards + exits | 需同形重放（T-X05：触发只读 bar 收盘的书级 P&L，不读未来） |
| 构造指纹 | 不含此规则 | 新增字段 → digest 变 → 走 `CONSTRUCTION_ALIASES`（读取侧）或等 10-13 后作为构造变更采纳 |

### 6.2 Impact Radius

R0 日报（O-GB0）；R1 `beidou_live/engine.py` 标量路径 + `beidou_alpha` 组合层（O-GB2）；R2 `research validate` 重放；R3 指纹与 M-010 窗口；R4 账本；R5 `tests/architecture/test_source_budget.py` CEILING（行数棘轮零余量，O-GB0 的 15 行也要抬并写理由）。

### 6.3 Engineering Pre-check

- O-GB0：`noise_scale()` 已有 `design`；`state.equity_hwm` 与最新 `equity` 在 `StateStore` 可读；ROE 需 `gross_before` 与 `leverage_set`（都在行里）。≈15 行 + 测试。
- O-GB2：`drawdown_throttle` 的 scalar 在研究与实盘各有一份实现且已有等价测试；上涨触发是同形反号。锚点用 `day_start_equity`（与 `daily_loss_pause` 同锚，避免第二套"当日"定义）。研究侧的"当日 P&L"要用 open_to_close 的书级净收益累加，与 `validate` 的执行口径一致。
- 风险：`no_trade_rel_band 0.40` 会吞掉 f > 0.60 的缩仓单（R8 第一档哑掉的同一条算术，RESEARCH_LOG 9052–9057）；**f 必须 ≤ 0.5**，否则规则在实盘下不出去而在回测里出去——KILL-027 的形状。

### 6.4 Risk Register

| RISK | 风险 | 概率 | 影响 | 预警 | 失败动作 |
| --- | --- | --- | --- | --- | --- |
| RISK-GB01 | +103 U（k=1，规则自己的锚上）的 n=1 数字诱使跳过第 0 步 | 中 | 高 | 有人提议"先上再说" | 引用 K-EX07 与 D-017；不上 |
| RISK-GB02 | 操作者不接受 ±337 U 日摆动，与 09-14 预算裁定矛盾 | 中 | 高 | §12 Q-CRITICAL 答 B | 重开 P32；本文 O-GB2 暂停 |
| RISK-GB03 | O-GB2 在实盘被带吞掉（f 与带的算术） | 低（若 f ≤ 0.5） | 高 | dry-run 缩仓单被 NO_TRADE_BAND 跳过 | 不上线 |
| RISK-GB04 | O-GB2 甩鞭：触发次日反弹，双向付成本 | 中 | 中 | 换手条款 | D-017 拦下 |
| RISK-GB05 | 账本余量：口径 ④ 下余量 +0.035，网格 +12 行推门 | 低 | 中 | 先算门再跑 | 只跑第 0 步 |

---

## 7. Phase 6 · Solution Reasoning

### 7.1 Option Set

| Option | 方案 | 先验 | 成本 | 可逆 | 结论 |
| --- | --- | --- | --- | --- | --- |
| O-GB0 No-Build + 仪表 + 预期书 **[R 修订]** | 保持一切不动；DL-EX0 加「自 HWM 跨日回吐 / σ_d 与 / σ_11h」一行、「三把回撤尺子对账」一行；UI ROE 行等 A-GB01 答复；一页操作者签字的预期书「0.60 长什么样」（用 ROE / USDT 单位写日 σ、3σ 日、这次形状的频率、09-07 异常线） | — | ≈15 行 live + CEILING；预期书 0 行 | 高 | **Must** |
| O-GB1 降 `vol_target` | 0.45 或 0.30 | 确定有效（线性） | CAGR −18 / −49pp（pit）；重开 P32；第 11 次构造变更、三个时钟清零、改冻结常量 | 高（一行） | **默认不做（A）**；操作者要 B 须书面理由，记为新裁定 |
| O-GB2 书级浮盈锁定 **[R 修订，KILL-GB01/05]** | 当日 P&L ≥ +kσ → 全书 × 0.5 到下一 UTC 日；**k 事先定为 `daily_loss_pause` 的镜像（+5% 当日 ≈ 1.6σ）**，不从这次事件挑（它 09-14 不会触发） | **低**：镜像 −0.18 反对（限额 1.8 倍）；Kaufman 反对；E-GB17 是单币横截面证据，不能跨到书级；n=1 不算 | 第 0 步 tsmom +2 / flow +2；网格 +2/+2；≈80 行；采纳 ≥ 10-13 | 高 | **Could，2026-10-13 GAP-GB02 满期后再议是否预登记** |
| O-GB3 缩仓专用窄带 | 缩仓相对带 0.20、加仓 0.40 | 低：E-GB19——大日后没有修剪单可发 | 参数 + validate | 高 | Won't |
| O-GB4 按仓位更紧止盈 / 移动止损（k<3） | — | 负（E-GB09/12） | 0 代码 | 高 | Won't |
| O-GB5 保本止损 / 部分止盈 | — | 同 O-GB4 | — | 高 | Won't |
| O-GB6 交易所原生条件单 | — | D-012 | — | — | Won't |
| O-GB7 手动干预 | — | 污染记录（D-032） | — | — | Won't |

### 7.2 Failure Modes

| FM | Option | 模式 | 预警 | 失败动作 |
| --- | --- | --- | --- | --- |
| FM-GB1 | O-GB2 | 第 0 步在一个 universe 为负、另一个不为负 | 双 universe 不同号 | 关闭分支，记 REFUTED |
| FM-GB2 | O-GB2 | 通过第 0 步但 overlay 不过 D-017 换手条款 | 换手 > +25% | 关闭 |
| FM-GB3 | O-GB2 | 实盘缩仓单被带吞 | dry-run | 不上线（RISK-GB03） |
| FM-GB4 | O-GB1 | 降 k 之后操作者又要"多拿收益" | 同形提问第四次 | 指向 E-GB07 的表 |

### 7.3 Recommendation Decision（D-GB01）

| 字段 | 内容 |
| --- | --- |
| 推荐 **[R 修订]** | **PIVOT**：O-GB0（仪表 + 预期书）现在做；O-GB1 默认不做，操作者要做须书面理由；O-GB2 降为 Could、2026-10-13 后再议（§11.2 文本保留为草案，不写入 RESEARCH_LOG）；O-GB3～O-GB7 Won't |
| 胜出理由 | 唯一同时满足"不推翻 09-07 的四个 REFUTED、不动冻结、回应诉求、可证伪"的组合 |
| 放弃 | 直接改退出层（本次事件上无效）；按 E-GB12 调参（n=1，且初稿的头条数字是错锚算的）；缩仓窄带（没有修剪单可发） |
| 关键成立条件 | A-GB02（操作者接受 −70% 预算的日常形态）；A-GB03（冻结维持） |
| Falsifier | O-GB2 第 0 步双 universe 条件均值显著为负且 overlay 双通过 D-017 → 本文"F3 只是分支"要升格为"应采纳"；反之关闭 |
| 灰度 / 降级 / 回滚 | O-GB2 任何采纳先 `--dry-run` 于 scratch state_dir；上线 = profile 一段 + 重启 + 记构造变更；回滚 = 还原 + 重启 |
| 接受的损失 | 操作者即时诉求延后；账本名额 |
| 最可能的偏差与对策 | **锚定既有否定**（把"从未测过"写成"已排除"）——对策：E-GB09 逐网格点名，k<3 止盈与 k<4 移动止损标为未测，并用 E-GB12 在本次事件上直接重放而不是引用；**新奇偏差**（O-GB2 是本文自己提出的）——**审查证实它确实在起作用**：初稿把一个错锚算出的 +135 U 印在 Decision Memo 第一屏，把先验写成"中低"，把第 0 步的 1.5σ 门定在这次事件的峰值上。对策（本版）：数字更正、先验改低、k 由既有 guard 的镜像事先定、分支延后到 GAP-GB02 满期 |

---

## 8. Phase 7 · Adversarial Review（独立子代理 `a3411914`，2026-09-15T08:44Z，只读冻结件 §0–§7、§9–§11 与原始证据；未读作者推理）

### 8.1 独立性与最强反方论点

审查者复核了 E-GB01–05/13/16（一致）、E-GB12（触发名单与次数逐条一致，U 数因起点差一根 bar 与名义口径而不同）、D-028 余量（+0.0347，≈92 次）；自写 `scratchpad/gb_review_replay.py` 逐小时重放并做了三种锚的并排；跑了含漂移的蒙特卡洛。**在读 §7 之前写定的最强反方论点**：「操作者的诉求不是退出层；作者把它改判为尺子/工具缺失，并从一个 n=1、33 根 bar 上搜过约 9 条规则后剩下的唯一赢家里长出一个新分支，而那条分支正是 D-017 已经定价为 −0.18 的 `drawdown_throttle` 的镜像；唯一有证据的杠杆和"昨天的裁定 vs 今天的诉求"这个矛盾被推给了操作者。」审查后判定：后半成立且比预想更强（+135 U 建在规则没有的锚上）；前半不能证明（09-14 裁定原话是"绝对收益、预算可重谈"，今天更像对路径的偏好），作者本该自己判这一句。

### 8.2 Kill Register（P0 0 / P1 5 / P2 6；末列为本版的关闭方式，审查者未复审）

| Kill | 攻击命题（压缩） | 严重度 | 本版处理 |
| --- | --- | --- | --- |
| KILL-GB01 | E-GB12 书级行用"自重建的浮盈"当锚，O-GB2 自己指定的锚是 `day_start_equity`（01:00Z 周期翻日）；规则的锚上 +0.5σ ≈ 0、+1σ 在记录上 20:00Z 触发、+1.5σ 不触发。作者把平仓造成的巧合当定义 | P1 | **CLOSED（新证据）**：本会话重算，+10 / +55 / +103 / 不触发；E-GB12 表与 Memo 改写；O-GB2 从 Should 移出 |
| KILL-GB02 | "没有手动干预"不成立：09-13T20:37Z 操作者令 `live flatten`，≥4 次空仓重开，每次重置退出锚；因果链缺这半句 | P1 | **CLOSED（新证据）**：E-GB18；§2.1/§4.2 改写；DL-GB4 建议补记 RESEARCH_LOG |
| KILL-GB03 | 提问模式数少数错；09-07 已交付尺子与异常表，操作者在 1.28σ 上照问——"给尺子提问就停"已失败一次；F4 不是根因 | P1 | **CLOSED（方案改动）**：Problem/D-GB00 改写；补偿改为预期书 DL-GB3 |
| KILL-GB04 | Q-CRITICAL 只定价不判断；证据支持默认 A；B 是第 11 次构造变更 | P1 | **CLOSED（方案改动）**：默认 A 写入 §0/§7.1/§12，B 的价钱写全 |
| KILL-GB05 | O-GB2 先验与价钱：单币 IC 不能推到书级；镜像是限额 1.8 倍；账本应 tsmom +2 且 flow +2；报 3 个 k 即 3 候选；1.5σ 门的动机在规则锚上不存在；−0.15σ 与 t≤−2 不等价 | P1 | **CLOSED（方案改动）**：先验改低；单一 k 由 guard 镜像事先定；账本重定价；预登记延后到 10-13 后 |
| KILL-GB06 | O-GB3 的"修剪 5–10%"无证据且符号反；E-GB12 名义高估 10–15% | P2 | **CLOSED（新证据）**：E-GB19；E-GB12 加注 |
| KILL-GB07 | 日 σ 把 11 小时的跌读小（1.89σ_11h）；频率应为 5–7 周；日报将有三个不对账的"回撤" | P2 | **CLOSED（新证据 + 方案）**：E-GB20；DL-GB0 加 11h 尺度与对账行 |
| KILL-GB08 | M-GB04 按方案反推；无能证伪 F2/F4 的停止条件 | P2 | **CLOSED（方案改动）**：M-GB04 移到 O-GB2 名下；M-GB01 加可证伪阈值 |
| KILL-GB09 | 数字卫生：+86/+135/−58 不自洽、bar 数、TP6%、ROE 用错 gross、E-GB11 靠别名未提 | P2 | **CLOSED**：逐项更正（ROE 15.0%；34 根周期；别名已注） |
| KILL-GB10 | DL-GB2 若做：live 当日锚在 01:00Z 周期，研究侧另有跨界约定；恢复腿受 `max_participation`/`margin_buffer`——KILL-027 形状 | P2 | **OPEN，N/A-until-build**：写入 DL-GB2 的测试要求 |
| KILL-GB11 | 先问操作者读的是哪个"8%"，再建 ROE 行 | P2 | **CLOSED（范围改动）**：ROE 行等 A-GB01（§12 Q2） |

### 8.3 Pre-Mortem / Inversion（审查者产出，压缩）

Pre-Mortem 的受损方一行：实盘记录与 tsmom 账本——Q-CRITICAL 答 B 正是重开冻结，对记录的补偿只有冻结本身。Inversion 里"当前已经在做"的三条：每次 <2σ 回吐都出一份 L 级分析并各长一个新分支（8 天 4 份）；每几天平仓/重启一次（46 次重启、≥4 次空仓重开，锚点不老化、M-010 不满期）；每次心情变就改 k（0.15→0.30→0.60 用了 10 天）。反向控制分别是：用 09-07 定的 > 2σ 门槛决定要不要分析；把"重启/平仓也计入"写成约束；默认 A + 书面理由。

### 8.4 G6 与 Final Kill Decision

审查时 **G6 PARTIAL**（无 P0；5 条 P1 各有关闭路径）；审查者的 Final Kill Decision：**PIVOT**，命中 H2、H5，未命中 H1/H3/H4/H7/H8；要求在冻结件被用于决策前更正"唯一能碰到事件的规则"与"没有手动干预"两句。本版已改，G6 记为 **PASS**（10/11 CLOSED，1 条 N/A-until-build）。

---

## 9. Phase 8 · Scope & Priority

| 优先 | 项 | In | Out |
| --- | --- | --- | --- |
| Must | O-GB0：两行仪表 + 预期书 | `reports.py` `noise_scale` + 渲染 + 测试；预期书一页 | 任何构造字段；ROE 行（等 A-GB01） |
| Should | 在 09-13 flatten 的 RESEARCH_LOG 条目补记「平仓重置全部退出锚」（E-GB18，文档改动，操作者定） | 一段 | — |
| Could | O-GB2 第 0 步预登记（**2026-10-13 后**，GAP-GB02 满期且 ≥ +1.6σ 的日子足够多时） | §11.2 草案 | 任何 overlay 代码 |
| Won't | O-GB3～O-GB7 | — | — |

Scope Firewall：2026-10-13T19:00Z 之前不改 `config/live.demo.yaml` 的任何构造字段，也不写 O-GB2 的预登记；第 0 步不通过不写 overlay 代码；不用 E-GB12 的任何数字选 k（k 只能来自既有 guard 的镜像或 GAP-GB02 的分布）。

---

## 10. Phase 9 · Delivery Contract 与 Source Trace

| DL | 来源链 | 实现 | 测试 | 验收 | Metric |
| --- | --- | --- | --- | --- | --- |
| DL-GB0 | Problem → E-GB14/16/20 → D-GB00(F2/F4) → O-GB0 | `noise_scale()` 加 `giveback_since_hwm_u`、`_in_design_sigma_d`、`_in_sigma_11h`；一行三尺子对账（权益 HWM / `risk_ladder.drawdown` / 当日 DL-EX0）；CEILING +≈15 并写理由；`ui_roe_pct` 等 A-GB01 | 单测：给定 `equity_hwm`、`equity` 的固定行，两个 σ 数等于手算 | 明日日报出现两行，今天的读数 1.28σ_d / 1.89σ_11h | M-GB01 |
| DL-GB3 | E-GB02/07/16/20 + 09-07 §12 异常表 → O-GB0 | 一页预期书「0.60 长什么样」：日 σ ±337 U = ROE ±14% = USDT 权益 ±6.7%；3σ 日 ±1,000 U；这次形状约每 6 周；异常线 > 2σ_d = 673 U；阶梯第一档 −49% | — | 操作者签字（或口头确认）后附在 RESEARCH_LOG | — |
| DL-GB4 | E-GB18 | RESEARCH_LOG 09-13 flatten 条目补一段：平仓重置全部退出锚 | — | 段落存在并引用 `exits.py` `_reconcile` | — |
| DL-GB1（延后） | Problem → E-GB12/17 → C-GB05 → O-GB2 | RESEARCH_LOG 预登记段（§11.2 草案），**2026-10-13 后**且 GAP-GB02 支持时 | — | 段落含判读阈值、账本声明（tsmom +2 / flow +2）、脚本名 | M-GB04 |
| DL-GB2（条件） | DL-GB1 通过 → O-GB2 网格 | overlay 参数 + 研究/实盘同形 + 指纹字段 + alias | 等价测试（研究 vs 实盘同一路径同一触发）；带算术测试（f ≤ 0.5） | D-017 双 universe + 换手条款 | M-GB03 |

---

## 11. Phase 10 · Learning Plan

### 11.1 Validation Plan

| Metric | 基线 | 阈值 | 窗口 | 停止条件 | Owner |
| --- | --- | --- | --- | --- | --- |
| M-GB01 | 今天 1.28σ | 报告，不作门；> 2σ 的跨日回吐才值得一问 | 每日 | — | 循环 |
| M-GB04（O-GB2 名下） | 未测 | 见 §11.2 | 一次，10-13 后 | 任一 universe 不过 → 关闭 | 操作者 10-13 后批准 |

### 11.2 O-GB2 第 0 步预登记草案（**[R 修订]** 不在本轮写入 RESEARCH_LOG；2026-10-13 后若 GAP-GB02 支持再定稿；数字出来后不改）

- **命题 C-GB05a**：在 shipped 构造（`vol_target 0.60`、registry 书、guards + exits）下，书级日净收益 r_t 满足 E[r_{t+1} \| r_t ≥ +k σ_设计] 显著为负：配对 t ≤ −2.0（单一判据；效应量只报告，因为 SE ≈ 0.09σ 时 t=2 对应 ≈ −0.18σ，两者不等价），**两个 universe 都成立**。
- **k 怎么定**：k 事先取 `daily_loss_pause` 的镜像——当日 +5% ≈ 1.6σ_设计（0.60 下），使规则成为一条既有 guard 的对称物，而不是对 09-14 的拟合（那天在这个锚上峰值 1.49σ，规则不会触发）。只报一个 k；k∈{1.0, 2.0} 不再"顺带报告"，报了就是候选。
- **同时报告（不作门）**：滞后 1 阶自相关（无条件）；镜像（r_t ≤ −5% 的次日均值，对照 `drawdown_throttle` 在 0.60 下的失败）。
- **判读**：两 universe 都过 → 进网格 DL-GB2（单一 k，f=0.5）；一过一不过 → REFUTED（跨 universe 不稳，P22b 的形状）；都不过 → REFUTED。
- **账本**：**tsmom +2 且 flow +2**（registry 书含 flow_short，本仓库按每策略计费；P23 第 0 步先例）；跑前先按口径 ④ 算门（现余量 +0.035，≈92 次）。
- **数据**：`--to 2026-09-13` 钉住，与 182325Z 同一 range_end。
- **脚本**：`scratchpad/gb_step0_book_daily_autocorr.py`（未写）。

### 11.3 Post-Launch Review 与分析校准

30 天后（0.60 下）回看 M-GB01 的分布：若 > 1.5σ_d 的跨日回吐每月 ≥ 3 次，本文"约每 6 周一次"的正态近似偏乐观，需改为经验分布并重审 F2。分析校准追加到 `docs/analysis/analysis-calibration.md`：本文判 REFUTED 的 C-GB01–04 若被推翻，最可能的原因是 E-GB12 的名义口径（用 targets 而非实际持仓，高估约一成）；本文已在同一天被独立审查推翻过一个头条数字（+135 U），校准记录里要写「作者提出的新分支，其 n=1 支撑要用规则自己的定义重算一遍」。

---

## 12. Final Decision

**PIVOT**（命中 **H2**：P0 Claim C-GB01「退出层失职」与 C-GB02「+8% → −1% 是异常」REFUTED，经独立复核与反向检验维持；命中 **H5**：唯一新分支 O-GB2 相对价值 Unproven，且其 n=1 支撑在审查中被撤销 → 只能以受控实验进入、且延后；采纳类动作在 2026-10-13T19:00Z 前受冻结测试与 K-EX14 阻断）。未命中 H1（P0 Kill = 0）、H3（UNKNOWN 的 C-GB05 是 P1）、H4、H7（G6 未 FAIL）、H8（demo 边界已知，未采纳任何东西）。

**先把不做的事说清楚**：止盈止损**不改**。不是因为它对，是因为它与这次事件无关——昨天没有一个仓位走到 3σ，把 09-07 已被否定的最紧档装上也一次不触发；而 6σ 从来就是几周的路，锚却每次平仓都被清到当前价。

- **对观察**：低点→HWM +5.09%（1.6 设计日 σ）、HWM→现在 −3.86%（1.3σ_d / 1.9σ_11h）是 `vol_target 0.60` 下的日常形态，约每 6 周一次这个形状；按 09-07 操作者自己接受的异常线（> 2σ_d = 673 U）这次 430 U 不到线。"8%" 是杠杆化的 UI 读数（ROE +15% 峰值），书按权益设计。
- **对"个别仓位 > 50%"**：AKE +22% 价格 = ROE +111% = 1.35σ，权重 1.6%，贡献 0.35% 权益；不异常。
- **对"怎么多拿收益"**：唯一确定有效的杠杆是 `vol_target`，代价是 CAGR（0.45：−18pp；0.30：−49pp，pit）和第 11 次时钟清零；默认维持 0.60。按仓位的任何规则在这次事件上锁不住东西；书级规则在它自己的锚上 n=1 为 0～+103 U、先验低、镜像已被判太贵，2026-10-13 后再议。
- **现在做什么**：O-GB0 两行仪表（需 Q3）+ 一页预期书（DL-GB3）；建议在 09-13 flatten 的记录里补一句锚点重置（DL-GB4，文档改动）。不改配置，不写预登记，不跑实验。

**需要你决定的三件事（一次问完，各带默认与价钱）**：

| Q | 问题 | 默认 | 若选另一边 |
| --- | --- | --- | --- |
| **Q-CRITICAL** | 维持 `vol_target 0.60`（日 σ ±337 U，3σ 日 ±1,000 U，这次形状每 6 周一次，阶梯 −49% 才降档）？ | **A 维持**：09-14 裁定原话"绝对收益、预算可重谈"并经更正措辞再确认；今日回撤 −3.9% 对第一档 −49% | B 降到 0.45 / 0.30：回吐 −25% / −50%；pit CAGR 126 → 108 / 77；重开 P32；第 11 次构造变更，M-010 / M-G06 / `realised_vol` 清零，改冻结常量；须书面理由，记为新裁定 |
| Q2 | 你看到的"8%"是币安哪个数？（持仓 ROE / USDT 权益 / 保证金余额） | 不写 ROE 行 | 答了就按那个口径写一行，与权益尺子并排 |
| Q3 | DL-GB0 ≈15 行 live 顶 KILL-R12 ceiling：抬（写理由）还是不做？ | 抬 | 不做：跨日回吐只留在本文与预期书里 |

**Checkpoint（2026-09-15）**：等级 L / Yellow；Phase 1–6 冻结于本文 §2–§7 初稿（已被 §8 审查后修订，修订处标 [R 修订]）；G6 PASS（P0 0 / P1 5 全 CLOSED / P2 6，5 CLOSED + 1 N/A-until-build）；开放 Claim C-GB05（UNKNOWN，P1，先验低）；Gap GAP-GB02（10-13 满期）→ GAP-GB01；待决 Q-CRITICAL / Q2 / Q3；下一动作 = 操作者回答后：Q3 通过 → DL-GB0；DL-GB3 预期书；DL-GB4 由操作者定。本文随本轮一并提交到 git；**未改任何配置**（`config/live.demo.yaml` 一个字符未动）、**未写试验账本**（`reports/research/trials.jsonl` 一行未动）、未跑任何计费实验、未重启任何进程。
