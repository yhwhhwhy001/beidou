# 分析校准记录

跨项目的深度分析事后校准表：每个分析项目一行，记它错在哪、哪个 Gate 该拦住而没拦住、当时缺的是什么证据、下次要改的一条流程。不写散文——目的是让下一次分析开工前扫一眼这张表，而不是重读旧分析全文。

新增一行时跟随现有列；没有内容就填"—"，不要删旧行、不要改旧行的结论（错的判断本身也是记录的一部分）。

2026-09-18 起多一列「结构缺失」（deep-analysis V3.8 附录 C.9）：记这次 skill 要求但没产出的产物。前六列记的是内容错，这一列记流程缺；此前八行只记前一种，同一种省略因此原样复制了七份。旧行只回填已核实的项，写「未逐行核」的就是没核。

| 日期 | 项目 | 分析当时判断/优选方案错在哪 | 误判最重的 Gate | 当时缺的证据 | 结构缺失（skill 要求但未产出的产物） | 下次改的流程 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-07 | 止盈止损自适应深度分析（`2026-09-07-exits-adaptive-tp-sl-deep-analysis.md`；结项证据：`docs/RESEARCH_LOG.md` P22/P22b/P23 三节） | 分析否决了操作者原本的框定 F1（"杠杆在止盈止损参数"），换成自己更偏好的 F2（"杠杆在信号自身的衰减"）与 F3（"这是尺度/预期问题"）。F3 活到了最后，结论"不要动 exit overlay"是对的；但**F2 被 P23 REFUTED 的方式和 F1 被 P22 REFUTED 一样彻底**——分析对着自己更偏好的替代方案，犯了它当初指责操作者原方案的同一种错：没测就当先验成立。结论对，理由当时是错的 | G2（Evidence Ledger）：把"更紧的退出已被证据排除"记成已结案的事实，没有核对预登记网格里 `take_profit` 实际只取过 `{0, 6}` 两个值——6σ 以内的止盈从未测过，而止盈侧恰是操作者抱怨的一侧。发现它的是对抗审查者（K-EX01），不是台 ledger 身；台账在被人读之前一直是错的 | 尺度不变性检查：把两个不同 `vol_target`（0.15 与 0.30）下的回撤改善量按**绝对百分点**直接相比，得到一个"控制行意外双赢"的假发现（0.30 上的 MDD 改善看起来是 0.15 上的 2–5 倍）；换成**相对量**（削掉的是各自基线回撤的百分之几）后，两个构造几乎完全重合——时点 4.5% vs 4.8%，静态 6.3% vs 6.2%。这条检查在写下"意外"那一刻没人做，是操作者事后追问"要不要改判"才补上的 | 未核（报告不在 `docs/analysis/`，2026-09-18 回填时未逐行读） | 引用一个既有的负结果时，把**产生它的网格本身**（参数取值集合、构造/`vol_target`）一起写进引用，不要只引用结论一句话——"更紧的止盈已被排除"这七个字，掩盖了"排除的只是 6σ 这一档、在 0.15 构造上"这两条关键限定 |
| 2026-09-12 | 成交频率深度分析 + P30 sleeve 敞口上限（`2026-09-12-fill-frequency-deep-analysis.md`；执行证据：`docs/RESEARCH_LOG.md` P30 三节） | 两处：①分析阶段给自己提出的两条路（top-30、日内诊断）定价比操作者的方案便宜，且「零 ledger 诊断」按仓库先例根本不是零 ledger——由独立审查在交付前抓到；②执行阶段接受了 P21「回撤按敞口走」的正确诊断，却配了错的工具（gross cap），而从同一诊断直接推出的 `f = (1/3) × 主书敞口/sleeve 敞口 = 0.211` 本来就可以先验预登记 | G3 相对价值（自提方案未按同一口径定价）；G2 证据（F5 写成「与三天前的报告逐位相同」，而数据每天同步，写下即不可满足） | 缺的不是数据是**对称性**：作者自己发起的 Option 没有被要求按操作者方案的同一把尺子定价；以及一条 falsifier 在写下之前没有被问「它在这个系统里能活多久」 | 未逐行核；已核三项：无 Phase 记录头（Prerequisites / Produces / Gate / Downstream）、无 MoSCoW 与 Scope Firewall、无 Checkpoint 文件 | 两条：①任何由作者提出的 Option，价钱栏必须与操作者方案同栏同单位（ΔSharpe / ΔMDD / Δ换手，不混用年化与 Sharpe）；②每条 falsifier 写完后自问「什么会在不违反命题的情况下让它触发」——依赖数据快照不变的条件一律改成同日对照 |
| 2026-09-14 | 因子挖掘及其使用深度分析（`2026-09-14-mining-deep-analysis.md`；Phase 7 由独立子代理执行） | 三处，全是读法不是数据：①把 `mined` 桶的 **2731 行**读成「2731 个候选」，而它只含 **676 个不同表达式**（4.04×），其中 1316 行新增零个假设——而 D-028 的门越过史上最好候选的交叉点 N=2170 正落在两轮「零新增」之间；据此把 C-001 判成 REFUTED，实际应为 **UNKNOWN**（四种可辩护的 N 口径里三种让它通过）。②E-011 标 `fraction 0.209`，却取了 `by_fraction.0.2000` 那一格的 `oos_mdd_worsening` 0.0130；该口径真值是 **0.025509**，超标从 30% 变 155%。③E-012 的 universe 写成 static，数值指纹逐位匹配 **pit** | G2（Evidence）：三处引用错都在证据台账里，且**没有一处是数据缺失**。G3 也误判——自提的 O-3 相对 O-0 的增量没有按给 O-1 用的那把先验折现尺 fold 过 | 缺的不是证据是**读法**：`trials.jsonl` 里 `param_key` 一直在盘上，`by_fraction` 三格一直在同一份 JSON 里，`basis` 族 0/18 的读数早在 09-10 就落盘了。另一类是**限定词遮蔽**：E-014 的 grep 加了「非测试调用点」，于是没看见住在 `tests/alpha/test_the_other_caliber_is_reported_not_applied.py` 里的禁止令，把一条已裁定、有测试执行的规则写成了待发现的空白，而 Q3 的授权正是基于那个空白给出的 | 未逐行核；已核四项：无 Phase 记录头（Prerequisites / Produces / Gate / Downstream）、无 MoSCoW 与 Scope Firewall、无 Checkpoint 文件、Claim 表无 Axiom 列 | 三条：①引用任何 JSON 数字必须带**完整路径**（`universes.pit.by_fraction.0.2090.marginal.oos_mdd_worsening`），不写字段名只写数值即视为未引用；②凡是「N 个候选/试验」这类计数，先分别报 **rows** 与 **distinct 假设键**两个数，两者不等时结论必须在两个口径上各写一遍；③grep 取证时**不得加缩小范围的限定词**（`grep -v test` 一类）——禁止令、豁免和守卫经常正住在被排除的那一半里 |
| 2026-09-17 | 阿尔法效率深度分析（`2026-09-17-alpha-efficiency-deep-analysis.md`；Phase 7 由独立子代理执行） | 冻结稿唯一的 Should（把挖掘候选 `594a12f9` 用「等风险合成」重新送 D-018 门）建在一个假前提上：「等风险合成从未跑过」。仓库 2026-09-07 已经量过（`docs/RESEARCH_LOG.md:2811-2870`：Sharpe 保住、回撤 +1.87pp 仍 > 1pp、尾部同向，并裁定「不能用来救这个候选」），`book_limits.py` 已实现该口径，09-14 挖掘分析的 C-005a 已 REFUTED。另两处：「SL 2.5σ 09-15 会在低点成交」是推理写成读数（收盘判定零触发）；替操作者把「效率」定义成 Sharpe 并给研究议程默认「是」 | G2（引用 09-14 挖掘分析只取候选履历，跳过其 Claim Register 的 C-005a/E-012）；G3（自提方案未查仓库是否已量就写「Unproven」） | 缺的不是数据是读法：RESEARCH_LOG 那一节、`book_limits.py` 的 `oos_mdd_worsening_equal_risk`、挖掘分析的 Claim 表三处都在盘上；另一处是把「已测网格全负」外推成「任何更紧都负」，没有在这两天的数据上重放 | 未逐行核；已核三项：无 Phase 记录头（Prerequisites / Produces / Gate / Downstream）、无 MoSCoW 与 Scope Firewall、无 Checkpoint 文件 | 三条：①写「从未测过 / 从未跑过」之前先 grep RESEARCH_LOG（等风险、同波动率、缩回、rescale）与 validation 代码；②引用一份既有分析时必须过一遍它的 Claim Register，不只取候选履历；③任何「会触发 / 会成交」必须来自按实盘口径（收盘判定）的重放，不得来自推理 |
| 2026-09-17 | alpha 模块整体审查（`2026-09-17-alpha-module-deep-analysis.md`；Phase 7 由独立子代理执行） | 三处，全是读法：①冻结稿把「候选进不了 registry」归因于判定与合成的机制，而唯一支撑它的案例 `594a12f9` 在仓库里有三处相反裁定（`RESEARCH_LOG.md:2850`「信号的性质」、09-14 分析 C-005a REFUTED、09-17 分析 O-AE2 No-Build）——引了数字没引结论，与 09-17 行同一种错；②把 embargo 720 对照重跑的 ledger 成本写成 0，依据是一条 09-14 的记忆条目「钉住 --to 重跑免费」，而 09-15 `PortfolioParams` 加了 `flat_inside_band`，今天的 `_construction_digest` 已不等于在架报告的；③把退出层写成「全部已判负」，而 TP<3σ / SL<2.5σ / TR<4σ 从未进网格——09-17 上午的 K-AE02 刚纠正过同一件事的反方向，下午我就把它倒回去了。另：把 D-045 的重启写成未发生（`state.json` 09-16T18:50Z 已重启），把 09-14 §7 的裁定写成未作出（口径 ④ 已落地） | G2（引用候选履历跳过 Claim Register；抄记忆条目不核字段演化；抄同日分析不核 `state.json`）；G1（Success Definition 里四条按方案反推） | 缺的不是数据是读法：三处裁定、`ad70e81a` 的新字段、`state.json.restarted_at`、`policy.py` 0.3.x 注释都在盘上；另一类是**并行会话同日合入**——写稿时 origin/main 已比本地多 6 个 PR（#32–#37），其中一个把我方案里的 O-2 跑完了 | 未逐行核；已核四项：无 Phase 记录头（Prerequisites / Produces / Gate / Downstream）、无 MoSCoW 与 Scope Firewall、无 Checkpoint 文件、Claim 表无 Axiom 列 | 四条：①引用任何候选前先读它在 `reopen.yaml` 与各分析 Claim Register 里的**裁定句**，不只读数字；②凡是「replay / 不计费」都必须当场用今天的配置重算 `_construction_digest` 并与 ledger 行比对，不引用早于最近一次 `PortfolioParams` 字段变更的结论；③写「已判负」必须点名网格，写「未测」必须 grep 同义词——两个方向都要；④定稿前 `git fetch` 并读 origin/main 相对本地的全部 diff，尤其 `docs/RESEARCH_LOG.md` 与 `reports/research/`，并行会话可能已经做了方案里的项 |
| 2026-09-17 | 策略因子信念深度分析（`2026-09-17-strategy-factor-belief-deep-analysis.md`；Phase 7 由独立子代理执行） | 三处，全是读法：①把书级判定（五 fold 全负、毛 Sharpe 为负）当成信号级结论，把 meanrev / breakout 判为「无信息」并据此把 P0 Claim 判 REFUTED，而仓库自己的信号级诊断（`RESEARCH_LOG.md:12` meanrev 1h/4h IC t 6.8 / 3.8）与裁决（`:6104` breakout「毛是正的、重开的是形式化」）说反，药方（时间止损）09-03 就写了、代码里零命中；②照抄 RESEARCH_LOG 复核节的「相关有构造下界 √占空比 ≈ 0.7」没有自己推一遍——它是独立门下的期望值，不是下界（corr = √门内方差占比，45 格实测跨度 0.18–0.87），并据此写了一条「事前判、不用跑」的规则；③把 2,073 行 ledger 写成 2,073 个表达式（不同表达式 676），与同文 914 的口径打架——09-14 校准行的同一条，再犯一次 | G2（证据）：头牌反例引的是书级数字，信号级数字就在同一份日志第 12 行；G1：两个 Success 指标（M-SF01 目标出 Scope、M-SF03 同义反复）按方案反推 | 缺的不是数据是读法：`regime_split_sharpes` 写好了但零调用者，所以没有任何报告带非日历状态的条件 Sharpe，作者就把「没有报告」读成了「所有状态都 ≤ 0」 | 未逐行核；已核三项：无 Phase 记录头（Prerequisites / Produces / Gate / Downstream）、无 MoSCoW 与 Scope Firewall、无 Checkpoint 文件 | 三条：①写「无信息」之前先找信号级诊断（IC-by-horizon、零成本 Sharpe），书级 FAIL 只能推出「这个形式化不行」；②引用仓库自己推导出的量（下界、翻转点、期望值）时重推一遍，不因为它写在日志里就免检；③「N 个候选 / 试验」先分别报 rows 与 distinct，两者不等时两个口径各写一遍——这条已在 09-14 行写过，本次仍犯，说明它该进 Phase 2 的检查清单而不是校准表 |
| 2026-09-17 | Q-SF1 residual 预登记的执行（`docs/RESEARCH_LOG.md`「Q-SF1 裁决」；预登记本身在 PR #49） | 预登记协议第 1 条把命令写反了：写「单点，不是网格……**不传 `--grid`**」，而 `_grid()` 在 `grid_json` 为空时取的正是 `DEFAULT_GRIDS[strategy]`（residual 8 格）——「不传」恰好会拿到这句自己要避开的东西，一次按 1 笔定价的运行会实计 8 笔，并把自己的门从 1.2046 推到约 1.29。跑之前读代码时发现并更正（`ce69db00` 14:50:00Z 早于运行 14:50:42Z），没有造成实际损失 | G7（Delivery）：预登记写的是「协议」，而协议里的命令行没有被对着实现核过一遍。写的时候脑子里想的是语义（单点），手上写的是一个与语义相反的旗标 | 缺的不是知识是核对动作：`validate-charges-one-trial-per-grid-cell` 与 `beidou-validate-default-grid-is-16-cells` 两条记忆都已经写着「不传 --grid 会按默认网格计费」，作者知道这件事，仍然写反了——因为没有在写完协议之后把每个旗标对着 `_grid()` 读一遍 | —（执行记录，不适用） | 两条：①预登记里任何要**花 ledger** 的命令，写完之后必须对着实现逐个旗标核一遍（尤其是「默认值 = 贵的那个」的旗标），核过的证据写进预登记；②报 ledger 计费一律同时写**原始行数**与**去重后 N** 两个数——本次预登记写的「桶 16 → 17」是去重口径，而 `grep` 数出来是 17 → 18，两个都对但读者不知道是哪个（这条已在 09-14 行写过一次，仍需在预登记模板里固化） |
| 2026-09-18 | 系统优化与因子模块深度分析（`2026-09-18-system-optimization-and-factor-module-deep-analysis.md`；Phase 7 由独立子代理执行） | 五处，全是读法：①把 residual 的网格值与其他六族的单点值并排，写成「门只能解释 7 个里的 1 个」，而 chanlun / flow 在 pit 上只跑过单点，单点 FAIL 不等于族 FAIL——与 09-17 信念分析被抓的同形，再犯一次；②把「bar 文件干净」写成「回测口径无悲观偏差」，漏了 N 含近重复、taker-only 成本、`compound` −1 地板三条；③把「门的功效没有合成实验」写成「无法回答」，而用报告自记方差三行就能算出解析功效（N=1 真 Sharpe 1.0 只有 50% 过线），且它说的与我的结论方向相反；④用未核验的 E3 文献把「清单内 7 个手写族新存活 0」改写成「9 活 1 是领域基线」，还把不在清单里的 tsmom / flow 算进分子；⑤用 8766 复算年化门与仓库的 8760 不一致，反把差异归给「仓库舍入」 | G2（证据）：五处里四处是把已有证据读窄或读反；G1：Success Definition 两条按方案反推（M-SY01 是 O-SY1 的交付物） | 缺的不是数据是读法：各族的 `grid_trials` 一直在 JSON 里，`panel.py:46` 的 8760 一直在代码里，解析功效只需要报告里已有的 `variance`；另一类是**自提方案的 Goodhart**——提议改 alpha_share 的尺子，恰好让自己推荐的工作算作 alpha | **V3.8 自检表 16/24 FAIL**（PASS 7、COMPRESSED 1；逐行见该报告末尾「V3.8 产物自检表回填」）：Axiom Trace 只是 `A2/A6` 标签、A4/A7 零挂载而 G4/G7 写 PASS（两道门空转）；无 Risk Register、Failure Modes、Phase 记录头、Scope Firewall、Checkpoint；Q-SY1–3 无验收项；Claim / Assumption / Evidence 表各缺 4–6 列；5W2H / JTBD / Scenario / Journey / Edge Cases 静默省略而非 COMPRESSED。这 16 项在 V3.7 的 12 项评分表下一项都不触发——评分表的 `source` 只列了 5.1/5.3/5.4/9/10 五节 | 四条：①写「门解释 x/N」之前先按族列出网格 / 单点，只跑过单点的族一律 UNKNOWN；②写「无悲观偏差」之前逐条列口径级偏差（N 的近重复、成本路径、收益地板），不只看文件级；③写「无法回答」之前先试解析解——报告自记的 `variance` 能算出的东西不算「没有数」；④复算与仓库不一致时先核代码常量再归因给对方；自提的任何规则变更先问「它会不会改变对本方案的评分」 |
| 2026-09-18 | Q-SY1 的执行：功效表接进预登记与 validate（`docs/PREREGISTRATION.md`、`beidou research power`、`oos_selection.power`） | 把功效表写进代码之后的**第一次运行**就证否了写它的那份分析里的一个数：09-18 分析 §5.2.1 第 2 点写「一次 16 格网格把 N 从 299 推到 315，功效再降约 2 个百分点」，实测 43.33% → 42.85%，是 **0.49** 个百分点，写大了约四倍。那句话是在写正文时心算的，而同一节里另外二十个数都是跑出来的 | G2（证据）：一张表里十九格是计算结果、一格是心算，而读者无从分辨哪一格是哪一种 | 不缺证据——`max_sharpe_quantile` 与报告自记的 `variance` 当时就在手边，缺的是把这一格也算一遍的动作 | —（执行记录，不适用） | 两条：①同一张表里不允许混入心算格——要么整张表由一段可重跑的代码产出，要么逐格标出哪些是跑的；②分析里凡是「再降 / 再升 x 个百分点」这类**差值**，都要与被减数、减数三个数一起写，只写差值时没人能复核它 |
| 2026-09-18 | Q-SY3 的执行：#27 上新回填 623 个标的后跑完 6 格事件研究（`docs/RESEARCH_LOG.md`「Q-SY3 裁「要」」；预登记在 2026-09-09） | 预登记本身的判定、网格、主格、falsifier 与「预期阴性、最大 \|t\| 落在 1.5 以下」全部对了（实测 1.31），**错的是它用来支撑那个预期的推理**：09-09 写「去掉偏差通常让效应更小而不是更大」，而去掉前视选择偏差之后，最大 \|t\| 从 0.82（被偏袒的 184 事件）升到 1.31（无偏的 799 事件），效应**变大**了。同一节还预测「主格符号不稳定」，实测六格全为正、两个半样本同号。结论没变（1.31 仍远低于 3），变的是它凭什么成立 | G2（证据）：把一条关于选择偏差方向的**一般性说法**当成这个样本上的读数写进预期，而当时手上没有无偏样本可以对照 | 不缺证据——无偏样本当时拿不到（正是触发条件 (1) 要补的那批数据），缺的是把「这是推理不是读数」标出来 | —（执行记录，不适用） | 两条：①「去掉选择偏差会让效应更小」是一个方向性直觉不是定律，写进预期时要标成 E5 推理，并写明它不成立时结论是否改变（本例：不改变，因为判据挂在绝对门 \|t\| ≥ 3 上而不是挂在「比有偏样本更差」上）；②预期里的每一条都要能单独判对错——本次预期有两半（幅度、符号稳定性），一半对一半错，如果只写一句「预期阴性」就看不出这件事 |
| 2026-09-18 | ledger 分母与四目标排序（`2026-09-18-ledger-denominator-and-four-objectives-deep-analysis.md`；Phase 7 由独立子代理执行） | 两条 P0，都在「怎么用数字」而不是「数字对不对」（审查独立复算 19 项**逐项一致**）：①**分母论证无效**——把 census 的分母与 correction 的分母当成同一个，在相邻两段里把同一组 ledger 行先缩（「19,772 行是**一次**配对搜索」）后涨（「一次 20,600 次的搜索必然产出走运候选」）；而 `ledger.py:215` 的 `ledger_scope()` 对手写族只返回 `(strategy,)`，八个族面对的 N ≤ 44，20,605 从不给它们开门。②**Falsifier 的命中数读错**——写「门降到 0.00 后只有 residual 能过」，引的是 `RESEARCH_LOG.md` 那一节的**标题**，而第 11567 行的**正文**写着「真正『只被多重检验门拦住』的是 flow（0.65 对 1.33，只挂这一条）与 residual」，**两个**；按 `registry.py:374` 收 `{PASS, WEAK_PASS}` 的上线口径，命中数是 2，Falsifier 满足，C-LD01 不该判 REFUTED。另四条 P1：把「新存活 0」当损失证据而它正是本文 E-LD03 预测的众数结果（循环）；flow 同时被计进「活 2」与「已判负的族」；C-LD04 用 standalone 判据 REFUTE「语法内已穷尽」，而本文 E-LD08 自己写了正确判据是 marginal（54 个正边际，最好的那个挂在 D-018 不是选择门）；定案所用的那次重跑**在测量的同时把 residual 的门抬了约 0.09** | **G2（Evidence）**：两条 P0 都是把手边证据读窄或读反，没有一条是数据缺失。**G6**：FAIL，2 条 P0 未关闭。**G1**：Success Definition 的 M-LD01 同时是推荐方案的一半、唯一的 Must 与验收条件——按方案反推，与 09-17 / 09-18 两行同形，**第三次** | 不缺数据：`ledger_scope()` 的实现、九份报告自记的 `n_trials`、两份报告的 `reasons` 字段、`verdict.py` 的判定阶梯、`RESEARCH_LOG.md:11567` 全在盘上。缺的是**读完整节而不是只读标题**——标题写「只有 residual」，正文写「flow 与 residual」，我引了前者 | **首轮输出整套框架产物缺失**（无 Axiom Trace、无 Claim / Assumption / Risk Register、无 Gate Summary、无 Phase 记录头、无四类 Contract、无自检表、无 Checkpoint、Phase 7 未执行），原因是把「已有同题报告」当成「可以只做核对」的理由，于是**静默省略而非标 COMPRESSED**——正是 V3.8 变更记录第一段描述的失败模式，由操作者当场指出后重跑。重跑版自检表 22/24 PASS、1 N/A、1 由审查降级；但**自检表本身有三行谎报 PASS**（Phase 1 产物不在文件内、§10 缺 Downstream 与 Gate Review、校准行标 PASS 时本文件尚未追加），三行都由审查者第 0 步核出 | 四条：①**引用 RESEARCH_LOG 的结论必须读完整节**，标题与正文不一致时以正文为准——本次的 P0 就是标题与正文差一个 flow；②凡是用「分母」做论证，先读 `ledger_scope()` 或等价的开门代码，确认**那个分母真的会被用来开门**；同一组行在一篇文里只能有一个口径；③**Falsifier 里的判定词必须在写下时就钉死定义**（本次「过」= PASS 还是含 WEAK_PASS 的上线口径，决定了 REFUTED 与 UNKNOWN 之差），定义写在 Falsifier 那一行里，不放在别处；④**自检表标 PASS 之前先核实物**——标 PASS 的那一刻校准文件还没写，`ls -la` 一次就能发现；这条与「`bash -n` 不算验证」是同一条 |
| 2026-09-18 | 四目标优化方案的执行（B1/B2/B3；`2026-09-18-optimization-plan.md` §9） | 两处，都不是「算错」而是「没核出处」：①**「10-13 裁定包有 12 笔名额」这个预算不存在，是我编的**——它是从 `RESEARCH_LOG.md:11317`「ledger 因此由 12 笔降为 8 笔」串过来的，那说的是 EXP-AE1 **单项**降价前的成本。真实的窗口预算在 `beidou_governance/policy.py`：`max_ledger_rows_per_window = 1700` / `window_days = 30`，14 笔在它面前毫无压力；真正卡人的是 `max_queued_to_probe_per_window = 1` 与 tsmom 的 52 笔 headroom。这个虚构的紧张被我在方案 §3、§7 与 PR #71 正文里复述了三次。②执行 B2 时用裸命令跑 `data pool history`，而 `--refresh` 默认 `MS`（月初），把一张 **2,042 行的逐日**成员表覆盖成 **69 行的逐月**表（并集 211 → 146 币）——而逐日正是 2026-09-04 特意换上去的口径，逐月带着值 0.07 OOS Sharpe 的 survivorship-adjacent bias | **G2（Evidence）**：两处都是引用了一个数却没回到它的定义处。①把别处的一个成本读成一个预算；②在 `--help` 输出里读到过 `[default: MS]` 却没有把它对着「原表是什么口径」核一遍 | 都不缺数据：`policy.py` 的四个常量、`RESEARCH_LOG.md:638` 把逐日与逐月两种口径的读数（2,042/211/0.36 对 69/146/6.68）逐位记过、`alpha_registry.yaml` 记着换口径的理由与代价。缺的是**在引用前回到定义处**这个动作 | —（执行记录，不适用） | 三条：①**任何「预算 / 名额 / 上限」类的数，引用前必须回到它的定义处**（代码常量、配置或预登记原文），不接受从别的段落串过来的数字；写下时带上出处路径。②**跑任何会覆盖既有产物的命令之前，先把它的全部默认值对着「现在那份产物是什么口径」核一遍**——校准表里「默认值 = 贵的那个」这条此前只写给了计费旗标，这次证明它对**破坏性旗标**同样成立，而且后者没有 ledger 留痕。③覆盖类操作做完立刻做一次**交叉核对**：拿新产物的某个特征量去对历史记录里的同一个量（本次是 `0.36 changes/refresh` 对 `RESEARCH_LOG.md:638`），对不上就是口径错了 |
