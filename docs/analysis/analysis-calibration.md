# 分析校准记录

跨项目的深度分析事后校准表：每个分析项目一行，记它错在哪、哪个 Gate 该拦住而没拦住、当时缺的是什么证据、下次要改的一条流程。不写散文——目的是让下一次分析开工前扫一眼这张表，而不是重读旧分析全文。

新增一行时跟随现有列；没有内容就填"—"，不要删旧行、不要改旧行的结论（错的判断本身也是记录的一部分）。

| 日期 | 项目 | 分析当时判断/优选方案错在哪 | 误判最重的 Gate | 当时缺的证据 | 下次改的流程 |
| --- | --- | --- | --- | --- | --- |
| 2026-09-07 | 止盈止损自适应深度分析（`2026-09-07-exits-adaptive-tp-sl-deep-analysis.md`；结项证据：`docs/RESEARCH_LOG.md` P22/P22b/P23 三节） | 分析否决了操作者原本的框定 F1（"杠杆在止盈止损参数"），换成自己更偏好的 F2（"杠杆在信号自身的衰减"）与 F3（"这是尺度/预期问题"）。F3 活到了最后，结论"不要动 exit overlay"是对的；但**F2 被 P23 REFUTED 的方式和 F1 被 P22 REFUTED 一样彻底**——分析对着自己更偏好的替代方案，犯了它当初指责操作者原方案的同一种错：没测就当先验成立。结论对，理由当时是错的 | G2（Evidence Ledger）：把"更紧的退出已被证据排除"记成已结案的事实，没有核对预登记网格里 `take_profit` 实际只取过 `{0, 6}` 两个值——6σ 以内的止盈从未测过，而止盈侧恰是操作者抱怨的一侧。发现它的是对抗审查者（K-EX01），不是台 ledger 身；台账在被人读之前一直是错的 | 尺度不变性检查：把两个不同 `vol_target`（0.15 与 0.30）下的回撤改善量按**绝对百分点**直接相比，得到一个"控制行意外双赢"的假发现（0.30 上的 MDD 改善看起来是 0.15 上的 2–5 倍）；换成**相对量**（削掉的是各自基线回撤的百分之几）后，两个构造几乎完全重合——时点 4.5% vs 4.8%，静态 6.3% vs 6.2%。这条检查在写下"意外"那一刻没人做，是操作者事后追问"要不要改判"才补上的 | 引用一个既有的负结果时，把**产生它的网格本身**（参数取值集合、构造/`vol_target`）一起写进引用，不要只引用结论一句话——"更紧的止盈已被排除"这七个字，掩盖了"排除的只是 6σ 这一档、在 0.15 构造上"这两条关键限定 |
| 2026-09-12 | 成交频率深度分析 + P30 sleeve 敞口上限（`2026-09-12-fill-frequency-deep-analysis.md`；执行证据：`docs/RESEARCH_LOG.md` P30 三节） | 两处：①分析阶段给自己提出的两条路（top-30、日内诊断）定价比操作者的方案便宜，且「零 ledger 诊断」按仓库先例根本不是零 ledger——由独立审查在交付前抓到；②执行阶段接受了 P21「回撤按敞口走」的正确诊断，却配了错的工具（gross cap），而从同一诊断直接推出的 `f = (1/3) × 主书敞口/sleeve 敞口 = 0.211` 本来就可以先验预登记 | G3 相对价值（自提方案未按同一口径定价）；G2 证据（F5 写成「与三天前的报告逐位相同」，而数据每天同步，写下即不可满足） | 缺的不是数据是**对称性**：作者自己发起的 Option 没有被要求按操作者方案的同一把尺子定价；以及一条 falsifier 在写下之前没有被问「它在这个系统里能活多久」 | 两条：①任何由作者提出的 Option，价钱栏必须与操作者方案同栏同单位（ΔSharpe / ΔMDD / Δ换手，不混用年化与 Sharpe）；②每条 falsifier 写完后自问「什么会在不违反命题的情况下让它触发」——依赖数据快照不变的条件一律改成同日对照 |
| 2026-09-14 | 因子挖掘及其使用深度分析（`2026-09-14-mining-deep-analysis.md`；Phase 7 由独立子代理执行） | 三处，全是读法不是数据：①把 `mined` 桶的 **2731 行**读成「2731 个候选」，而它只含 **676 个不同表达式**（4.04×），其中 1316 行新增零个假设——而 D-028 的门越过史上最好候选的交叉点 N=2170 正落在两轮「零新增」之间；据此把 C-001 判成 REFUTED，实际应为 **UNKNOWN**（四种可辩护的 N 口径里三种让它通过）。②E-011 标 `fraction 0.209`，却取了 `by_fraction.0.2000` 那一格的 `oos_mdd_worsening` 0.0130；该口径真值是 **0.025509**，超标从 30% 变 155%。③E-012 的 universe 写成 static，数值指纹逐位匹配 **pit** | G2（Evidence）：三处引用错都在证据台账里，且**没有一处是数据缺失**。G3 也误判——自提的 O-3 相对 O-0 的增量没有按给 O-1 用的那把先验折现尺 fold 过 | 缺的不是证据是**读法**：`trials.jsonl` 里 `param_key` 一直在盘上，`by_fraction` 三格一直在同一份 JSON 里，`basis` 族 0/18 的读数早在 09-10 就落盘了。另一类是**限定词遮蔽**：E-014 的 grep 加了「非测试调用点」，于是没看见住在 `tests/alpha/test_the_other_caliber_is_reported_not_applied.py` 里的禁止令，把一条已裁定、有测试执行的规则写成了待发现的空白，而 Q3 的授权正是基于那个空白给出的 | 三条：①引用任何 JSON 数字必须带**完整路径**（`universes.pit.by_fraction.0.2090.marginal.oos_mdd_worsening`），不写字段名只写数值即视为未引用；②凡是「N 个候选/试验」这类计数，先分别报 **rows** 与 **distinct 假设键**两个数，两者不等时结论必须在两个口径上各写一遍；③grep 取证时**不得加缩小范围的限定词**（`grep -v test` 一类）——禁止令、豁免和守卫经常正住在被排除的那一半里 |
| 2026-09-17 | 阿尔法效率深度分析（`2026-09-17-alpha-efficiency-deep-analysis.md`；Phase 7 由独立子代理执行） | 冻结稿唯一的 Should（把挖掘候选 `594a12f9` 用「等风险合成」重新送 D-018 门）建在一个假前提上：「等风险合成从未跑过」。仓库 2026-09-07 已经量过（`docs/RESEARCH_LOG.md:2811-2870`：Sharpe 保住、回撤 +1.87pp 仍 > 1pp、尾部同向，并裁定「不能用来救这个候选」），`book_limits.py` 已实现该口径，09-14 挖掘分析的 C-005a 已 REFUTED。另两处：「SL 2.5σ 09-15 会在低点成交」是推理写成读数（收盘判定零触发）；替操作者把「效率」定义成 Sharpe 并给研究议程默认「是」 | G2（引用 09-14 挖掘分析只取候选履历，跳过其 Claim Register 的 C-005a/E-012）；G3（自提方案未查仓库是否已量就写「Unproven」） | 缺的不是数据是读法：RESEARCH_LOG 那一节、`book_limits.py` 的 `oos_mdd_worsening_equal_risk`、挖掘分析的 Claim 表三处都在盘上；另一处是把「已测网格全负」外推成「任何更紧都负」，没有在这两天的数据上重放 | 三条：①写「从未测过 / 从未跑过」之前先 grep RESEARCH_LOG（等风险、同波动率、缩回、rescale）与 validation 代码；②引用一份既有分析时必须过一遍它的 Claim Register，不只取候选履历；③任何「会触发 / 会成交」必须来自按实盘口径（收盘判定）的重放，不得来自推理 |
