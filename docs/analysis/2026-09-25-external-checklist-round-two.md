# 外部清单第二轮：监控层重构、第二批补缺、预登记第九项、10-13 准备

2026-09-25。执行记录，不是裁定。行号以各 PR 的合入提交为准。

## 这一轮接的是什么

操作者 09-25 再次给出同一份外部 prompt 清单：X 账号 @TechElyra 的 10 条 prompt 加整理稿。
要求是「重构北斗」。

这份清单 09-23 已经用过一次：

- 清点：`docs/analysis/2026-09-23-external-prompt-checklist-vs-beidou.md`。
  100 项里已有 70、部分 27、空白 0、不适用 3。
- 第一批：`docs/analysis/2026-09-23-optimization-plan-from-external-checklist.md`。G1–G10 全部合入，
  G6 随重启 #56 进了实盘。

所以本轮先把现状摆出来，再给出四块带价的选项。操作者四块全选：

1. 拆监控层；
2. 第二批补缺；
3. 预登记加「实盘失效方式」；
4. 10-13 的准备。

**没做的那一种：按清单的 7 步流水线重排整个包。
** 北斗的包本来就按这条流水线排：data → alpha（signals → portfolio → overlays）→ live，
验证与治理另成一层。重排要改 51k 行测试的 import、29 个 scratchpad 复现脚本与文档里的行号引用，
却补不上任何一项清单能力。真正堆在一起的是监控层：清单五个领域的读数都在 `beidou_live/reports.py` 里，
一共 3,175 行。

## 结果一览

| 块 | PR | 做了什么 | 合入 |
| --- | --- | --- | --- |
| 1 | #135 | `reports.py` 按清单领域拆成八个模块，只剩五个组装函数；56 个地址原样再导出 | `d7c3d1ec` |
| 3 | #136 | 预登记模板加第九项「实盘失效方式（How this fails）」，附现有仪器表 | `fd4dfadc` |
| 4 | #137 | `run_live.sh` 在 bridge 到期那天会死在 bash 3.2 上，已修，并加两条测试 | `1c47623b` |
| 2 | #138 | 持仓间相关（清点 3.4 / 8.9） | `4492851a` |
| 2 | #139 | 逐单 TCA，事前估计离线重建（10.9 / 10.10） | `8a618721` |
| 2 | #140 | 多因子载荷（6.4 / 6.9），日报与 `report beta` 都印 | `3e66b927` |
| 2 | #141 | 平仓流动性（3.9） | `abda7726` |
| 2 | #142 | 两条在跑的状态规则的确认延迟（8.R），研究测量 | `f2e5d4ce` |
| 4 | #143 | 删掉一条已过期的 strict xfail：数据集门 09-19 就解开了 | `0875b34a` |
| 4 | #144 | 10-13 的准备：到期那一刻、D-041 与 k 的带价选项、G11 / G12 预登记草稿、裁定表 | `03e62ac0` |
| 2 | 本 PR | D-047：报告与证据不出图（2.10）；本记录 | — |

共同的约束：

- 不动构造：每个包都跑了两个构造测试。
- 不花 ledger：`reports/research/trials.jsonl` 的 sha256 始终是 `09245c34…99d`。
- 不重启实盘循环。
- 新读数只报告，不告警：上线前没有预登记阈值，看了读数再定的阈值不算阈值。

## 怎么验收的

- **#135 只搬代码。** 81 个定义连同上方的注释块，与父提交逐字相同。在实盘状态快照上，
  拆前拆后的 81 个报告产物逐字节相同：22 天日报、4 份周报、2 份 beta 报告，
  另加 `live status` 与引擎用的读数。唯一读墙钟的 `long_run_sharpe` 两边按同一办法冻结。
  复现：`scratchpad/reports_split_byte_identity.py`。
- **#138–#141 只多自己那一块。** 每个包都以当时的 main 为基线，在同一份快照上各出一遍报告。
  日报 json 每天只多一个键，其余键全等；md 只多一节；删行 0；其余 37 个产物逐字节相同。
- **合并方独立复核关键数字**，这一步不用各包自己的函数：
  - #138：7 天与 30 天的加权平均相关、effective bets、ETH 对 BTC 的相关，自己读 parquet 重算，
    逐位相同到小数点后 10 位。
  - #139：与 M-Q08 对账，全书与主书两个读数的差都是 0.0。
  - #140：只留市场一个回归元时，与 D-045 的 conditional beta 逐位相同。
- **#137 做了变异验证**：第 60 行改回旧写法，本机 bash 3.2.57 上文本检查与 0、+1 天两条行为检查变红。

## 新仪器读到了什么

快照是 09-24 的实盘状态：

| 仪器 | 读数 |
| --- | --- |
| 持仓间相关（#138） | 17 个多头、0 个空头。7 天加权平均两两相关 0.53，effective bets 2.10 个；30 天 0.59、1.77 个。第一主成分占方差 83% 与 87% |
| 多因子载荷（#140） | 397 根 bar：市场 0.92（t 14.8），BTC −0.02，size 0.09（t 3.8），低波 0.15（t 5.1），资金费 0.00。R² 0.92，只用市场 0.79。alpha 1.87 bps/h，t 1.51，不显著 |
| 逐单 TCA（#139） | 30 天 125 笔，实际滑点 +5.87 ± 1.81 bps。跳空 +0.01，成交 +5.93：滑点全在成交段。事前估计 +2.76，模型大约低估 3 bps |
| 平仓流动性（#141） | 整本书一次完全平仓，冲击 0.57 U。最难平的 TRUMPUSDT 只占小时成交额的 0.0088%，离 2% 上限约 227 倍 |
| 确认延迟（#142） | `short_gate` 只拿到事后门收益的 6.2% / 2.4%，净贡献与零分不开（t < 1）。阶梯从越档到生效要 3 根 bar，历史上没有样本 |

前两行说的是同一件事，也是 09-19 F4 在收益上量到的那件事：**这本书是一个杠杆多头篮子。**

- 17 个名字约等于 2 个 bet。
- 市场一个因子解释 79% 的方差。
- 另有低波与大成交额的倾斜，与逆波动率定价的方向一致（推理，没验证）。

清单能补的仪器，这一轮补上了，它们一致地说收益来自市场敞口。这不是重构能改的。
外部清单的整理稿自己也说：清单给不了 alpha，也不会告诉你什么时候该停手。

## 要操作者定的，按最晚日期排

| # | 事项 | 选项与价钱 | 最晚 | 什么都不定时 |
| --- | --- | --- | --- | --- |
| 1 | 主 checkout 快进 | 带进 #137 的 bash 修复，也把日报的五个新节带进每小时巡检。循环在跑的进程不受影响，下次按纪律重启才加载新代码 | bash 那条 10-12 | 10-13 后第一次启动死在 bash 上；每小时巡检读不到新节 |
| 2 | 挖掘窗口的第五轮 | 退回 4 并升 `POLICY_VERSION`，或宣布 5 常设。都是治理裁定 | 10-02 | 10-03T00:00Z 起 `test_the_single_window_mine_opening_is_returned` 红 |
| 3 | D-041 的去向，与 k 的重裁 | 选项 B1、C、E、F、G，逐个定价见 10-13 文档第 2、3 节 | 要重出的 10-08，其余 10-11 或 10-12 | 进程跑到下一次启动为止；之后起不来，持仓没有退出检查 |
| 4 | exemption（F2） | 随 #3 | 10-12 | 10-13 起至少 9 条测试红 |
| 5 | R8 回撤尺子重复计算 | 修：改变阶梯什么时候动，是构造变更，冻结结束后；或不修 | 冻结结束后 | 读数平均偏深 0.27pp、最多 1.58pp，阶梯略早动，方向偏保守 |
| 6 | 10-13 文档裁定表的其余几行 | 见那份文档第 6 节 | 各行自带 | 各行自带 |

第 1 项要特别说：它不改任何构造，只让主 checkout 追上 main。09-23 那一批也是由操作者定的快进，
见 `docs/analysis/2026-09-23-optimization-plan-from-external-checklist.md`「生效还差两步」。
写这一节时，主 checkout 停在 `451c7783`（#134）。

## 顺带发现

逐个 PR 描述扫过「顺带发现 / 未做 / 待定」，下表是全部：

| 发现 | 出处 | 状态 |
| --- | --- | --- |
| R8 的回撤尺子把已实现盈亏算了两次，高水位偏高，读数偏深 | #142 | 上表第 5 项，要操作者定 |
| 手续费分两档：13 个标的入账 4.0 bps，7 个 5.0 bps；`costs.yaml` 按 5.0 平收 | #139 | 冻结期内不动 config |
| 成交段主要是 demo 盘口上的成交成本（demo 开盘价到成交均价 +5.10 bps），不是两个场所的价差（+0.82 bps） | #139 | 读数的解释，不是缺陷 |
| 09-03 同一个 order_id 写了两行；`slippage_bps` 与 `live_turnover` 不按 order_id 去重 | #139 | 未修；那一行目前不在任何窗口里 |
| `ex_ante` 在 sleeve 空仓后每一行都等于 target，不带信息；持仓书的独立预测是 7 天 0.596、30 天 0.557 | #138 | 未加进日报 |
| cycle 行里没有逐币持仓字段，两个新读数都要从订单与 skip 行重建 | #138、#141 | 补它要改引擎并重启，未做 |
| `liquidity_window` 不在构造指纹里，`max_participation` 在 | #141 | 冻结期内未动 |
| `quantize_qty` 不按 MARKET_LOT_SIZE 的 maxQty 截断 | #141 | demo 规模下无关，未核 |
| D-045 页面的「\|t\| < 2」把竖线写进了表格单元格，那一行会被拆坏 | #140 | 修它会改已有输出，未动 |
| `short_gate` 的误报代价（−23.58%）比关晚（−10.54%）更大 | #142 | 是分类错，不是延迟，未往下分析 |
| p32d / p32h 的 bootstrap 没带尺子那 1 个周期，也没带重复计算 | #142 | 未重跑 |
| E-043 说截断「总是」生效，在 pit 上不成立（LUNA 2022-05 的 242 格） | #142 | 记录在案 |
| 09-19 那次 16 格少传了 `--prior-trials 152`，RESEARCH_LOG 把差归给了 ledger scope | #144 | 10-13 文档裁定表 #7 |
| `window_changes.yaml` 两条写 10-03，而冻结到 10-13 | #144 | 10-13 文档裁定表 #9 |
| `research power` 把显示值当字典键，1.5 与 1.5292 同传会撞键 | #144 | 未修 |
| `risk_budget.py:233` 引的 `reports.giveback_check` 在拆分前就不存在 | #135 | 未改 |
| 一个 worker 违反任务约束跑过一次只读的 `launchctl list` | #140 | 已如实写进 PR 描述 |

## 局限

- 新读数的样本都短：相关 7 / 30 天，TCA 30 天 125 笔，载荷 397 根 bar。它们描述的是这几周的书，
  不是这套构造的性质。
- 新读数只报告不告警。什么读数该响、响到哪里，要先预登记，再上线。
- 快照是 09-24 的。主 checkout 快进之前，每小时巡检里还没有这五个新节。

## 查漏补缺（同日）

操作者要求把这一轮查一遍。查了五个方向：新读数在生产上跑不跑得起来；文档有没有跟上；有没有别的
日期炸弹；清单的「部分」项各落在哪；新读数有没有算错。最后一项交给两个全新上下文的子代理做对抗式
审查，照外部清单的第三条使用纪律。行号以 `d657b16b` 为准。

### 操作者四条裁定与状态

裁定原文与落地细节在 `docs/RESEARCH_LOG.md`「2026-09-25 · 操作者四条裁定」一节。

| 裁定 | 状态 |
| --- | --- |
| 主 checkout 快进 | 已做：05:33:06Z 快进到 `54da72a8`。在跑的循环不受影响；#137 的 bash 修复从下一次 launchd 启动起生效。06:10:13Z 的巡检是第一次真读：日报四个新节都在、没有报错；各项结果与 05:10 那次（旧代码）相同，`report` 仍因原来那条滑点告警记 FAIL。07:29:40Z 又快进到 `034877b3`，带上 #150–#152 的修复。三个 `live run` 进程都在快进前启动，改到的模块它们要么已在启动时载入，要么根本不 import；新代码从 08:10 的巡检起生效 |
| 挖掘第五轮常设 | 已做：#147，`STANDING_MINE_ROUNDS` 4 → 5。没升版本号，治理摘要不变，巡检不会误报。10-03 的日期炸弹解除 |
| D-041 与 k 按建议 | 测完，等操作者选：诚实漂移下，现有抵押品给不出网格里的 k；换抵押品之后是 0.45。见下「k 的选项」 |
| R8 回撤尺子修 | 修复做成草稿 #149，与 10-13 的重启同批合入。重放 364 个读数：331 个变了，修复前平均深 0.27pp，最多深 1.58pp |

**D-041 与 k 多了一条前置。** 同日另一个会话的独立体检（#146，`docs/analysis/2026-09-25-backtest-guard-audit.md`）
第一条高危指出：K 表的 bootstrap 用的是全样本 argmax 那本书的净收益序列，它的 Sharpe 在 1.67–1.70 一档；
诚实的选择程序交付的样本外是 1.2306。bootstrap 保留均值，所以 K 表的 q95 回撤整体偏乐观，按它选出的
k 会偏高。所以 D-035 的规则要在「诚实漂移」那张表上套：把每根 bar 的净收益减去一个常数，使整条序列的
Sharpe 等于 1.2306，同一组 block 起点配对重跑。

### k 的选项

重测全文在 `docs/analysis/2026-09-25-k-remeasure-honest-drift.md`，要点记在 RESEARCH_LOG「k 的重测」一节。
D-035 的规则取决于两个问题：读哪条漂移，换不换抵押品。四种组合各给一个答案：

| 读法 | 不换抵押品 | 换抵押品（K-4） |
| --- | --- | --- |
| 原漂移（体检前的 K 表） | 0.25 | 0.60 |
| 诚实漂移（#146 体检建议读这张） | 网格里没有，要 0.20 以下 | 0.45 |

对应四个选项。下表的「构造变更」指 10-13 准备文档第 2.2 节的 G：在新 k 上重出证据。2 格加约 16 笔申报时，
N = 337，门 1.5855。

| 选项 | 代价 |
| --- | --- |
| **1. 换抵押品，k = 0.45（推荐）** | 操作者在交易所把 BTC 抵押品换成 USDT，这笔交易的时机与费用没有评估。构造变更，档位改成 ((−0.49, 0.3375), (−0.70, 0.225))，走 R10。按诚实漂移，CAGR 中位约 56%；k = 0.60 约 67%，但守不住 |
| 2. 不换，k = 0.25 | 读的是体检说偏乐观的那张表。按诚实漂移，0.25 的 q95 是 −79%，超线约 9pp。构造变更 |
| 3. 不换，k 取 0.20 以下 | 插值的守住线约 0.19（pit 0.2004、static 0.1935），CAGR 中位约 26–27%。另一个会话的 p32i 量过 0.15：q95 pit −55.4%、static −58.8%，CAGR 中位 19–21%。0.15 是新格子，要申报 prior-trials。构造变更 |
| 4. 换抵押品，k 留 0.60 | 不改构造，不清零任何时钟。但读的是原漂移：按诚实漂移，q95 约 −74%，超线 3–4pp。D-041 仍要另走准备文档的 B1 或 C |

推荐选项 1。它是唯一在诚实漂移那张表上守得住、又留在已看过网格里的选项。换完之后账户里没有 BTC 抵押品，
RISK-2（BTC 抵押品随书同跌）那条通道也就没有了，这一点是推理。有三件事不因选项而变：

- 1.2306 这个锚自身的 95% 区间约 0.36–2.10。
- 新 k 上的证据要重出。重出不过门时，要在 10-13T00:00Z 前退到准备文档第 2.2 节的 C、E 或 F。
- 最晚 10-08 要定。

### 漏掉的，已补

| 漏项 | 怎么发现的 | 补在 |
| --- | --- | --- |
| `reports.py` 的 docstring 与 ARCHITECTURE 的模块表，没跟上 #138–#141：`report_beta` 仍只写市场 beta，`report_risk`、`report_execution` 没列新读数 | 逐文件对照 | 本 PR |
| 日报小节标题不统一：别的写「#3.4」，平仓流动性写「3.9」 | 读日报 markdown | #152，连同钉它的一条测试。放在那里，是因为那条测试它也要改 |
| 预登记第九项的仪器表写在四个新读数合入之前，没列它们 | 对照 #136 与 #138–#141 的合入顺序 | 本 PR：补一行，写明只印、不告警 |
| RUNBOOK 没有 10-13 bridge 到期的处置 | 对照 10-13 文档第 7 节 | 本 PR：新一节，写 10-12 前核什么、起不来时先平仓、不要挪日期 |
| 10-03 还有一颗日期炸弹（挖掘窗口到期） | 复核 10-13 文档的 9 条红时扫到 | #144 的后续记下，#147 按裁定解除 |
| 跑实盘的机器上，一条过期的 strict xfail 一直在红 | 10-13 文档的推理，主 checkout 上实测 | #143 |

### 日期炸弹：没有别的

测试里读当前时钟的有 6 个文件，其中写着未来日期的只有三处：10-03 的挖掘窗口（已解除）、10-13 的
exemption（F2，跟着 D-041 的裁定走）、10-13 的构造冻结（到点后那条测试直接返回，不会红）。

### 清单覆盖：两轮之后，31 行「部分」各落在哪

31 行是 27 项加 4 条提醒，由脚本从清点的逐项表里抽出。

| 去向 | 行 |
| --- | --- |
| 已补上 | 1.10、1.R（G1、G2）；2.10（D-047）；2.R(b)（G8）；3.4、8.9（#138）；3.5、3.6（G4）；3.9（#141）；4.8（G5）；6.4、6.9（#140）；8.R（#142）；9.5、9.7、9.R（G6、#128）；9.9（G10）；10.8（G9）；10.9、10.10（#139） |
| 大半补上 | 8.3：危机窗口在 k=0.60 下重跑（G4），分 regime 的夏普进了 validate（G5）；逐资产类的图谱仍是一次性的 |
| 等冻结结束 | 1.7、3.8、5.2 → G11；6.1 → G12。两份九项的预登记草稿在 10-13 文档第 4、5 节 |
| 有既定决定或结构原因 | 5.7：选池不做深度打分；8.5：F4 讲过的结构空缺，没有低相关的第二本书；9.3：分钟级已决定不做 |
| 既没做，也没有「不做」的决定 | 8.10 事件驱动的调仓规则；9.4 缺口只检测不修；9.6 feature store。要操作者定 |

### 对抗式审查

四个新读数交给两个全新上下文的子代理审，两边互不通气。R1 审 #139（逐单 TCA）和 #141（平仓流动性），
R2 审 #138（持仓间相关）和 #140（多因子载荷）。两边的总结论一致：

- 读数都在实盘状态副本上逐位复现。
- 时序对齐，没有前视，也没有幸存者偏差。
- 四个块都不进 `daily_alerts`；块内出错整块兜住，不拖垮日报。
- 没有致命或高的发现。

中级 7 条，每条先核实再修：

| 读数 | 发现 | 修在 |
| --- | --- | --- |
| #139 | 按主机时钟的 `bar_open_ms` 查归档。D-025 容许整根 bar 的时钟偏移；主机快一根时，事前估计会读到下单时还没收盘的 bar | #151：决策 bar 改取周期行的 `as_of_ms`，并核 `decision_close` 等于归档收盘价。对不上的不拆也不估，单独计数 |
| #139 | 「参与率」用单根决策 bar 的成交额。同一份日报里，#141 用的是 24 根均值 | #151：改成 `max_participation` 的口径，近 24 根均值 |
| #141 | 「already submitted」行带着新计划的方向和旧单的成交量，被当成新成交叠进持仓 | #152 跳过这种行；#151 让 M-Q08、TCA 和日报按订单去重 |
| #138 | 1% 的覆盖率对账挡不住 1% 以下的缺仓，NaN 也能过这道检查 | #152：容差收到 0.2%，比较改成防 NaN 的写法，缺名义额的币逐个列出 |
| #138、#140 | 归档滞后写成「一到两小时」，实际是 1–25 小时：数据任务每天只在 17:20Z 同步一次，按决策 bar 算差 1–24 根 | #152、#150：两块都印归档止于哪根 bar、比回归样本或决策 bar 早几根 |
| #140 | 前视测试抓不到三种一根的前视：资金费多读一根、选腿用 t+1 的键、敞口用当根 | #150：fixture 加每小时结算的名字，扫 8 个 cutoff，补两条时点测试 |
| #140 | `report beta --out` 在因子页出错时两页都不写，是 #140 引入的退化 | #150：先写 D-045 那页 |

低级 16 条，修了 15 条，分在同样三个 PR 里。不修的一条是 R2 的「敞口条件化用 targets，不用持仓」：
这是 D-045 的既有口径，审查者自己也标了不急。要改得先做一份用重建持仓条件化的对比读数，不在这一轮。

修复时又核出两处要补充的细节：

- R1 说「already submitted」那一行的 `book_complete` 仍是 True。实际读 False，因为那一行的
  `gross_before` 是 0。道理仍然成立：`book_complete` 只核下单前的合计，看不见叠错的成交。
- R1 指出，#141 的「与 `LiveEngine._liquidity` 逐位相同」只在归档不滞后时成立。这句写在 PR 描述与
  source budget 的验收注释里，是同一份数据上的实测，本身没错。会被误读的是 docstring 的「line for
  line」，它读起来像对实盘也成立，已改成「公式相同，窗口是归档的」。

**#139 的一条结论要更正。** 它的 PR 描述写「实际成本不随参与率上升」，这只在单根 bar 的口径下成立。
R1 做了名义加权回归，滑点对 log10(参与率) 的斜率（bps/十倍）如下：

| 参与率口径 | 斜率 | 剔除 \|滑点\| 最大的 5 笔 |
| --- | --- | --- |
| 单根决策 bar | +1.61 ± 1.73 | — |
| 近 24 根均值（`max_participation` 的口径） | +4.06 ± 1.84 | +3.56 |
| ADV（冲击模型的口径） | +5.75 ± 2.02 | +5.52 |

同一标的内的斜率约为零：−1.03 ± 3.60 和 +2.92 ± 5.19。所以更正为：**成本随参与率在标的之间上升，
同一标的内看不出冲击。** 看得到的是标的之间的差别，不是单笔冲击。「这批数据校准不了冲击系数」仍然成立。

**「顺带发现」表里那条「未修」已修。** 09-03 同一个 order_id 写了两行，由 #151 修掉。新函数
`risk_budget.one_row_per_order` 按 (symbol, order_id) 只留第一行。M-Q08、`live_turnover`、周滑点、
日报的订单计数与成交额、`restart_cost` 都改走它。渲染比对只有 09-03 那天的日报变了：成交额少
112.41，FILLED 40 → 39。其余各天与各产物逐字节相同。实盘 258 行成交里，重复的只有这一对。

**审查没核的部分。** 要联网的说法没核：demo 盘口的表现、demo 成交额是主网的 6–22 倍、bookTicker
归档止于 2024-04。合入 #138–#141 时的逐字节比对和各 PR 自报的变异，审查者也没有重跑。三个修复 PR
各自重跑了渲染比对与变异，结果写在各自的描述里。

### 要操作者定的

| 事项 | 最晚 | 见 |
| --- | --- | --- |
| k 的选项。推荐 1：换抵押品，k = 0.45 | 10-08 | 上文「k 的选项」 |
| 8.10、9.4、9.6 做不做 | 没有硬期限 | 上文「清单覆盖」 |
| p32h 与 D3 那次的全精度输出要不要归档进仓库 | 那两个会话的 worktree 清掉之前 | RESEARCH_LOG「k 的重测」 |
