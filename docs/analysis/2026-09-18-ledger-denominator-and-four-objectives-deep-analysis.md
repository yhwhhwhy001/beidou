# 深度分析：按操作者自己选的分母重读「几十个策略只活两个」，以及四个目标的排序（V5 · 2026-09-18）

> 深度分析 **V3.8** · 等级 **L** · 只分析不执行。ID 命名空间 `LD`。
>
> **Reading Check**：本次理解为——操作者以产品、策略研究与架构三重身份问：框架看起来没问题、近期收益也可以，但系统质量还有没有进一步提升的空间；重点是他导入的 50 多个策略最终只有两个有效，他怀疑判定条件或回测数据有问题，或者策略缺少对应因子；另有因子模块三问（因子是否固定 / 是否所有策略对同一批因子无差别匹配 / 现有因子之外有没有更优的）。交付对象是操作者本人，交付物是一份判断加一份带价钱的方案，不改配置、不跑计费实验、不动运行中的循环。最高风险预设为 **Pre-01（P0）**「50 多个策略里绝大多数被 pass 掉，说明门或数据有问题」；若它不成立，结论就从「放宽门、查数据」变成「先给四个互相冲突的目标排序」。
>
> **Interaction**：**Yellow**（完整度 🟢用户 🟢场景 🟡损失 🟢成功标准 🟢约束 🟢可得证据——成功标准经 Context Intake 转绿，损失仍只有定性描述）｜ 等级 **L**，命中「≥3 模块」「资金链路」「AI/策略自主决策」；本档不覆盖：无默认省略 ｜ 当前决策上限：**Need Evidence**（H1 + H3 + H7；**[R 修订，K-LD01 / K-LD02]** 冻结稿写 PIVOT / H2）｜ 输出状态：**DONE_WITH_CONCERNS**（1 条 P0 未关闭：K-LD02 需操作者一句定义） ｜ 外部动作授权：无（只读仓库与 `.beidou/`；跑了四次零 ledger 的 `research power`；跑了两个只读扫描脚本，未写任何仓库文件；未跑 validate / mine / book；未写 ledger；未改 registry；未重启；未下单）
>
> **与 09-18 另一份同日报告的关系**：`docs/analysis/2026-09-18-system-optimization-and-factor-module-deep-analysis.md` 是同一天早些时候另一条线索下的分析。**本文不复用它的任何结论**，全部数字从一手来源独立重取。重取过程中发现两件它无法包含的事：(1) 它的三条待裁定 Q-SY1 / Q-SY2 / Q-SY3 **当天全部执行完毕**，其中 #27 上新事件研究 **REFUTED**；(2) 操作者本轮明确把分母定义为 **ledger**，而不是 09-08 那份 51 条清单——这改变了第一节的算法。
>
> **Context Intake**：三问已答（§2.6）。分母 = ledger；四个目标**全选**；真实资金「再观察数月后再定」。

---

## 0. Decision Memo

| 项目 | 结论 |
| --- | --- |
| **Final Decision** | **Need Evidence**（H1 + H3 + H7）。**[R 修订，K-LD01 / K-LD02]** 冻结稿写 **PIVOT（H2）**，依据是 C-LD01 `REFUTED`；Phase 7 推翻了那一步的两根支柱，C-LD01 转 **UNKNOWN**，H2 不再成立。原文保留：~~按操作者自己选的口径——ledger——「50 多个策略只活两个」这个读数**自我否证**：ledger 22,179 行里，19,772 行是**一次**配对搜索的 census，2,073 行是**一次**挖掘活动，6 行是**一次**事件研究；真正由人设计并建成的信号族是 **10 个**，活 2。两个真实读数是「10 活 2」与「约 20,600 个不同配置活 2」，而后者**正是 D-020 / D-028 存在的理由**——分母选得越大，多重检验校正越必要。所以「门太严」这条假设在操作者选的口径下不成立。~~ **修订后**：分母论证**无效**——`ledger.py:215` 的 `ledger_scope()` 对手写族返回 `(strategy,)`，九个手写族各自面对的 N 是 **1 / 1 / 5 / 9 / 10 / 28 / 44 / 106 / 19,777**，20,605 从不给其中任何一个开门（§2.6 已改写）。而 C-LD01 的 Falsifier 在**本仓库自己的上线口径**下已被满足：`registry.py:374` 收 `{"PASS", "WEAK_PASS"}`，门降到 0.00 后 residual → PASS、flow → WEAK_PASS，**两个**；`RESEARCH_LOG.md:11567` 的正文与此一致（冻结稿引的是该节**标题**）。所以**「门太严」现在是 UNKNOWN 不是 REFUTED**，它只差操作者一句话：「过」= PASS，还是 = 上线口径。站得住的剩余部分是：四个目标互相冲突且从未排过序（C-LD08，已降 PARTIAL） |
| 一句话问题 | 操作者用「门太严 / 数据有错 / 缺因子」三个假设去回答「下一笔研究投入放哪」。前两个在证据上方向错，第三个在当前语法与数据内已被穷尽，而且它最便宜的一条出口（#27 上新）**今天刚被证伪**。他没有拿到的不是一份诊断，是**一张把四个目标排成序的表**，以及每条路的价钱 |
| 选定的框定 / 放弃的框定 | **D-LD01**：选 F2「这是一个**目标排序**问题」。放弃 F1「门槛校准」（C-LD01 REFUTED）与 F3「因子覆盖」（C-LD04 语法内 REFUTED、语法外 PARTIAL）。重开条件：若操作者把四个目标收敛到一个，F2 失效，回到该目标自己的子问题 |
| Success Definition | 三个结果指标（§3.5）：**M-LD01**（领先）每次研究动作在执行前写下它服务的是四个目标里的哪一个，今天 0/0；**M-LD02**（护栏）tsmom family gate 余量不因本文引出的动作下降，今天 **+0.0181**（OOS 1.5919 对门 1.5738，N=299），翻转点 N=351，剩 **52 笔**；**M-LD03**（滞后）2027-03-31 前有 ≥1 个 2026-09-03 之后新建的候选进入 probe 或前向板，今天 **0** |
| 最大价值 / 最大风险 | 价值：把一个问过五次的怀疑换成一张排序表与三个可核的数（族级 10 活 2、配置级 ~20,600 活 2、空桶功效 50.0%）。风险：**RISK-LD01**——操作者把「门与数据没问题」读成「系统已最优」；**RISK-LD02**——本文顺着仓库两周「否决有效」的叙事写，与 09-17 / 09-18 各份分析同一种确认偏差 |
| Strategic Fit / Relative Value / Economic | **High**（它决定研究预算的方向）/ **Adequate**（推荐项零 ledger；相对「再跑一轮 validate」的优势可量化为 headroom）/ N/A（单人，无多角色经济） |
| 被接受的损失与补偿 | 操作者失去「放宽门就能多几个策略」这条最省事的路，**无补偿**——这条路本来就不存在。四个目标全选意味着每个目标单独的推进速度都变慢，补偿是 §8 的排序让他知道慢在哪 |
| 开放 P0/P1 | C-LD04 语法外（离散门控）**UNKNOWN**，未定价；C-LD07 实盘 15.6 天判不了 alpha，只能等 M-010 30 天（2026-10-13 起可判） |
| G0–G7 一行 | G0 PASS · G1 PASS · G2 PASS · G3 PASS · G4 PASS · G5 PASS · G6 见 §7 · G7 PASS |
| 本档不覆盖 | 无默认省略 |
| 下一动作 | §9.5 的四条验收项；§8 的排序表交操作者确认；两条带价钱的裁定见 §9.5 |

---

## 1. Gate Summary

| Gate | 状态 | 追溯到本门公理的 Claim IDs | 未通过项 | 决策上限 |
| --- | --- | --- | --- | --- |
| G0 Interaction / Kill | **PASS** | — | K4 BLOCKER 已由 Context Intake Q2 关闭 | — |
| G1 Problem / Axiom（A1） | **PASS** | **C-LD00** | — | — |
| G2 Evidence / Reality（A2） | **PASS** | C-LD01、C-LD02、C-LD03 | 口径级三条保守偏差 → C-LD02 PARTIAL | — |
| G3 Relative Value（A3） | **PASS** | **C-LD09** | — | — |
| G4 Strategic / Economic（A4） | **PASS** | C-LD05、**C-LD08** | 无多角色经济 → §4.4 N/A | — |
| G5 System / Solution（A5） | **PASS** | **C-LD10** | — | — |
| G6 Adversarial（A6） | **FAIL** | C-LD01（Falsifier **命中数未定**）、C-LD04 | 2 条 P0 未关闭（K-LD01 OPEN、K-LD02 UNKNOWN） | Weak GO（H7） |
| G7 Delivery（A7） | **PARTIAL** | C-LD06、**C-LD11** | 自检表三行 PASS 与实物不符（K-LD13/14/15） | — |

**[R 修订，K-LD01 / K-LD02]** 冻结稿写：~~命中 **H2** → PIVOT；未命中 H1、H3~~。修订后命中 **H1**（未关闭 P0 = 2 → 上限 Weak GO）、**H3**（P0 Claim C-LD01 转 UNKNOWN → 上限 **Need Evidence**）、**H7**（L 级 G6 FAIL → 上限 Weak GO）。**H2 不成立**：它的触发条件正是被推翻的那一步。取最低者 → **Need Evidence**。未命中 H8（demo 场地、无 PII、本轮零外部写入）。

---

## 2. Phase 2 · Evidence Ledger

**Prerequisites**：G0 PASS；Phase 1 的四问、来源与偏差、预设清单。
**Produces**：E-LD01…E-LD14；C-LD00…C-LD11；A-LD01…A-LD04；GAP-LD01…GAP-LD03。
**Downstream**：§3 框定与 Success Definition、§5 系统分析、§6 Option Set、§8 排序。

### 2.1 Evidence Ledger

全部 **E1**，除非另注。读取日期 **2026-09-18**，基线 `origin/main` **`938492ce`**（**[R 修订，K-LD17]** 冻结稿写 `f195cdb4`；分析期间 PR #68 合入，而 E-LD06 引用的 #27 结论正是它带进来的）。本文自测的标 **[本文实测]** 并附命令。

**E-LD01 · ledger 分桶（去重后的假设数）** · CODE/DATA · `reports/research/trials.jsonl` · 2026-09-18 · 范围：全库唯一 ledger · **[本文实测]**
`wc -l` = **22,179** 行。按 `strategy` 分桶，`rows / distinct param_key`：`pairs_search` 19,772 / 19,772；`mined` 2,073 / 676；`tsmom` 192 / 74；`flow` 47 / 7；`meanrev` 28 / 27；`residual` 18 / 8；`breakout` 10 / 9；`xsmom` 9 / 8；**`listing` 6 / 6**；`pairs` 5 / 4；`carry` 5 / 4；`mined_594a12f9307a15d9` 4 / 1；`forward_board` 3 / 2；另七个 `mined_*` 各 1 / 1；`chanlun` 1 / 1。**支持 C-LD01。**

**E-LD02 · 全部 validation 判定（递归 glob，79 份）** · DATA · `find reports -name '*-validation-*.md'` · **[本文实测]**
tsmom 26 PASS / 7 WEAK_PASS / 8 FAIL；flow 4 PASS / 5 FAIL；xsmom 0/6；residual 0/5；carry 0/4；pairs 0/2；meanrev 0/2；breakout 0/2；chanlun 0/1；`mined_594a12f9307a15d9` **1 PASS**；另六个 `mined_*` 各 0/1。**九个手写族里只有 tsmom 在 pit universe 上通过过**；flow 的 4 份 PASS 全在 static 15 币 universe 上。**支持 C-LD01。**

**E-LD03 · 门的功效（零 ledger，代码实测）** · EXPERIMENT · `beidou research power --evidence reports/research/tsmom-validation-20260917T074143Z.json` · **[本文实测]**
报告自记 `oos_selection.variance = 2.20523e-05`、`bars_per_year 8760`、`n_obs 45247`。样本外 Sharpe 的**年化标准误 0.4395**。过门概率（门 = `max(D-028 选择门, D-020 的 1.0 PASS 线)`）：

| 桶 / N | 门 | 真 Sharpe 1.0 | 1.2 | 1.5 | 2.0 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 空桶 N=1（PASS 线 binds） | 1.0000 | **50.0%** | 67.5% | 87.2% | 98.9% |
| tsmom 桶 N=299（今天） | 1.5737 | 9.6% | 19.8% | **43.3%** | 83.4% |
| N=315（再跑一次 16 格） | 1.5796 | 9.4% | 19.4% | 42.8% | 83.1% |

命令自印一行：`not included in the above: cpcv_fraction_negative, pbo, fold_consistency, cost_stress_x2 (so the true joint power is LOWER)`。**这张表是上界。支持 C-LD03、C-LD09。**

**E-LD04 · family gate 今天的余量** · DATA · `governance/verdicts.jsonl` 最后一条 `kind=family_gate` · 2026-09-17T15:37:15Z · **[本文实测]**
原文：`OOS 1.5919 vs 1.5738 at N=299 (adopted against 1.5493 at N=242)`。余量 **+0.0181**。按 E-LD03 同一方差与常量外推，翻转点 **N=351**，剩 **52 笔**。**支持 M-LD02、C-LD09。**

**E-LD05 · 回填后全库 1h klines 完整性扫描** · DATA · 只读脚本（会话临时目录，不写仓库文件） · **[本文实测]**
878 个目录 **878/878 有 1h**；**15,120,555** 根 1h bar；`重复 0 · 非单调 0 · NaN(OHLC) 0 · close≤0 0 · OHLC 矛盾 0`；**缺口 4,235（0.0280%），涉及 29 个标的**（TLM 708 / BNX 638 / ICP 626 / LIT 137 / FLOW·GTC·HOT·IMX 各 120）；零成交量 1,273,085（**8.42%**）；`|1h 收益| > 50%` 229 根。覆盖 2020-11-01 → 2026-09-18T07:00Z。
**一处自查**：首版脚本把 `open_time` 的 epoch 毫秒按纳秒解析，缺口误报为 0；按 `unit="ms"` 重算后 BNXUSDT 得 638，与另一份 09-18 报告对同一标的的读数**逐位一致**，交叉核对通过。其余各格按列计算，不受该错误影响。**支持 C-LD02。**

**E-LD06 · #27 上新事件研究的裁决（今天）** · EXPERIMENT · `docs/RESEARCH_LOG.md`「2026-09-18 · Q-SY3 裁「要」」 · 2026-09-18
623 个标的的 1h 回填完成（+8,306,842 根 bar，0.96 GiB）；无偏样本 **n=799** 次上新（09-09 那份只有 184 次且被前视选择偏袒）。6 格全部按 2026-09-09 的预登记跑完，**最大 |t| = 1.31**（主格 `a24_L48` t = 0.92），预登记 falsifier 是「6 格最大 |t| < 3 → #27 关闭」→ **触发，裁定 REFUTED**。同节记：「在 [24,72) 的 sd=0.266 下，要让 t 到 2 需要 n ≈ 1,106 个事件」，而全库五年七个月只攒出 799 次。计费 6 笔进全新的 `listing` 桶，不进任何在架策略的分母。**支持 C-LD05。**

**E-LD07 · 挖掘空间的独立汇总** · DATA · 10 份 `reports/research/mine-*.json` 的 `candidates` 数组 · **[本文实测]**
去重后 **914 个不同表达式**（按 `hash`）。每 hash 取最好 `baseline_marginal_sharpe`，**正边际 54 个 = 5.9%**。最好的三个 standalone：`594a12f9307a15d9` 1.7144（边际 +0.4306）、`3d58f32f81b6d524` 1.5861（+0.3064）、`b3e32c2773eef407` 1.5193（+0.3193）；当轮 baseline 1.8184，**0 个 standalone 越过在跑的书**。**支持 C-LD04。**

**E-LD08 · 594a12f9 的实际死因** · DATA · `docs/RESEARCH_LOG.md` 第 11613 行与 `book-tsmom-mined_594a12f9307a15d9-20260912T185333Z.json` · 2026-09-12 / 09-17
它是**唯一过了 selection gate 的挖掘候选**（E-LD02 的那一份 PASS）。`book_verdict` **REJECT**，六项检查过五项，唯一挂 `oos_mdd_worsening`（fraction 0.209 时 ΔOOS Sharpe **+0.2931**、回撤恶化 0.0255）。RESEARCH_LOG 原话：「唯一过 selection gate 的 `mined_594a12f9` 挂在 D-018 的回撤门上，且那 2,073 次搜索把它自己的 headroom 从 +0.1409（N=575）吃到 +0.0338（N=1563）」。**支持 C-LD04、C-LD08。**

**E-LD09 · 因子层的代码事实** · CODE · **[本文实测]**
`beidou_alpha/features.py` 340 行 / **20 个因果原语**（`returns` `log_returns` `realized_vol` `ewm_vol` `garch_forecast_vol` `true_range` `atr` `donchian` `zscore` `robust_zscore` `cross_sectional_rank` `cross_sectional_zscore` `demean_cross_section` `rolling_beta` `residual_returns` `volume_ratio` `taker_buy_ratio` `funding_per_bar_to_8h` `clip_unit` `refit_boundaries`）。`beidou_alpha/mining/expr.py` 1,080 行：**12 个数据叶**（`Const` `Ret` `Vol` `VolumeRatio` `TakerBuy` `Funding` `HourOfDay` `OpenInterest` `LongShortRatio` `Basis` `TradeSize` `RangePosition`）+ **10 个算子**（`Abs` `Moment` `Semi` `Residual` `Ratio` `ZScore` `CrossSectional` `Squash` `Sum` `Mul`）。`mining/search.py` 787 行按族枚举。**语法里没有比较、阈值、指示函数**——离散门控不在搜索空间里。**支持 C-LD04。**

**E-LD10 · 成本模型是纯 taker，maker 是死配置** · CODE · `config/costs.yaml` + 全包 grep · **[本文实测]**
`taker_fee_bps: 5.0`、`slippage_bps: 2.0`、`slippage_stress_bps: [2.0, 4.43, 5.5, 9.2]`、`impact.coefficient: 1.0`（**默认关**）。`maker_fee_bps: 2.0` 写在文件里，但
`grep -rn "maker_fee_bps\|maker_fee" --include="*.py" beidou_alpha beidou_live beidou_cli beidou_shared` **零命中**——**全仓库没有任何读取方**。文件自记：「Everything above charges the same bps for a 40 USDT order and a 4M one, which makes the whole backtest scale-free - a property of the arithmetic, not a finding about the world」。**支持 C-LD06。**

**E-LD11 · 实盘 15.6 天的读数** · DATA · `reports/daily/2026-09-1{1..8}.json` 的 `drift` 块 · **[本文实测]**
`expected_sharpe = 1.090`（09-13 起；之前 1.198）。展开窗口的 `realised_sharpe` 逐日：09-11 **1.367**（9.3d）→ 09-12 **2.551** → 09-13 **−0.099** → 09-14 **2.608** → 09-15 **−0.984** → 09-16 **0.504** → 09-17 **4.224** → 09-18 **7.167**（15.6d）。八个连续读数、同一个展开窗口，摆幅 **8.15 个 Sharpe 单位**。`status` 全部 OK；`trailing_drawdown` −5.92%，`validated_drawdown` −40.29%。**支持 C-LD07，反证 Pre-04。**

**E-LD12 · 51 条清单的真实分块（独立核）** · DATA · `docs/analysis/2026-09-08-autonomous-governance-51-strategies-plan.md` 第 25 / 134–144 / 483 行 · **[本文实测]**
原文 §1 范围对账（KILL-AR-16）：「操作者原话『51 条』→ Phase 1–3 实际推进 **32 条**；块 6 **7 条**设前置；块 0 **12 条** Out。合计 51」。第 483 行：「regime(#47) 与节流(#20) 各被数了两遍…**不重复的候选是 49 条**」。块 3（手写信号 6 条）第 465 行自记：「块 3 只有 2/6 有结论，另外 3 条**根本没建**，『结项』是错词」。**支持 C-LD01。**

**E-LD13 · 实盘循环当前状态** · DATA · `.beidou/live/state.json` · **[本文实测]**
`restarts = 53`，`restarted_at = 2026-09-17T16:07:34Z`——**过去 24 小时零重启**（对比 13.4 天 51 次 ≈ 3.8 次/天的历史速率）。**支持 §5.5。**

**E-LD14 · 回填造成的数据异质性** · DATA · `docs/RESEARCH_LOG.md` 2026-09-18 Q-SY3 节 · 2026-09-18
回填只补 1h klines，**不补 funding**：255 个老标的有 funding，623 个新回填的没有。原文：「任何吃资金费的载入会按 KILL-027 拒跑。这是选出来的，不是漏了」。另：`data pool refresh` 读币安 API 不读本地 store，所以 623 个新目录进不了 `universe.json`；受影响的只有「`universe.json` 为空时，不传 `--universe pit` 的研究运行的默认总体从 255 变 878」。**支持 C-LD10。**

**E-LD15 · 2026-09-17 九族同管线重跑（Falsifier 的观察本身）** · EXPERIMENT · `docs/RESEARCH_LOG.md` 第 11530 行起那一节 + 九份 `*-validation-20260917T09*.json` · **[R 新增，K-LD03]** · 等级 **E2**（来源是本系统自己写的研究日志，不是一手读数）
**[R 修订，K-LD03]** 冻结稿把全文最承重的一次观察**完全没有登记为证据**——C-LD01 的 Falsifier 引用它，反证字段却列的是 E-LD01/02/12，没有一条是这次重跑。方法：`beidou research validate --universe pit --grid '{}'`（单点，registry 名义参数），5 折、`min_train 4000`、`purge 50`、`cpcv 6`，206 标的 × 50,033 bar。各族自记 `n_trials` / OOS / verdict 见 §2.6 的 N 表与 C-LD01。

**[R 修订，K-LD21]** E-LD06、E-LD08、E-LD14 的等级由 **E1 降为 E2**：它们的来源是同一作者系统写的 `RESEARCH_LOG.md`，属稳定运营记录而非一手读数。本文自测的 E-LD01/02/03/05/07/09/10/11/12/13 维持 E1。

### 2.2 Claim Register 与 Axiom Trace

> Axiom Trace 一格写的是**这条命题对所触及公理强制问题的回答**，不是公理编号。

**C-LD00 · P0 · SUPPORTED · High**
命题：存在一个真实问题——alpha 管线自 2026-09-03 起**新存活为 0**，而操作者正按一个方向错误的诊断分配研究投入。
**Axiom Trace ——A1「谁在什么场景承受什么可观察损失？」**：操作者本人，在每次决定「下一笔 trials 花在哪」时承受损失；损失可观察且有单位——tsmom family gate 的余量从 +0.0347 被消耗到 **+0.0181**（E-LD04），而这 15 天换来的新存活是 0（E-LD02）。
**[R 修订，K-LD05]** 冻结稿把「新存活 0」当作损失证据，而本文自己的 E-LD03 预测 **0 正是这个窗口的众数结果**（真 Sharpe 1.5 在 N=299 下过门概率 43.3%，15 天里建成并验过的新候选是个位数）——用系统的设计输出证明系统有问题是循环。**损失项只保留不循环的那一条**：headroom 从 +0.0347 被消耗到 **+0.0181**（E-LD04），这条与功效无关、可核、且有单位。~~原文：「损失可观察且有单位——……而这 15 天换来的新存活是 0」~~
Falsifier：2026-09-03 之后有任一新建候选进入 probe 或前向板并保持 → 未观察到（前向板上的 tsmom 与 residual 都是既有族，且都是 `OBSERVING`）。**注意**：按上面这条修订，该 Falsifier 本身也不再单独支撑 C-LD00，它只是一个描述性读数。
支持：E-LD02、E-LD04、E-LD06。反证：无。决策影响：决定本文是否成立。Owner：操作者。

**C-LD01 · P0 · PARTIAL · High** **[R 二次修订，操作者裁定后；冻结稿 `REFUTED`，一修 `UNKNOWN`]**
**裁定后的状态**：门对 **flow 与 residual** 是**唯一**死因；对另外六族**不是**（各挂 1–4 条与 D-028 无关的判据）。所以「几十个策略被 pass 掉是因为门太严」——**对 2/9 成立，对 6/9 不成立**。逐族见 §13。
命题：「50 多个策略只活两个」这个读数支持「判定门或回测数据有问题」。
**Axiom Trace ——A2「结论强度是否超过证据强度？」**：不超过，而且反向——按操作者本轮选定的口径（ledger），22,179 行里 19,772 行是**一次**配对搜索的 census、2,073 行是**一次**挖掘活动、6 行是**一次**事件研究（E-LD01）；真正由人设计并建成的信号族是 **10 个**（tsmom / flow / xsmom / carry / residual / meanrev / breakout / pairs / chanlun / listing），活 2。所以同一个印象对应两个真实读数：**族级 10 活 2**、**配置级约 20,600 活 2**；后者**正是多重检验校正存在的理由**，不是它失灵的证据。
**Axiom Trace ——A6「什么证据会证明核心命题错误？」**：Falsifier = 把 selection 门降到 0.00 后，已判负的族里有 **≥2** 个能过。~~已观察（2026-09-17 九族同管线重跑）：只有 residual 一个 → REFUTED。~~
**[R 修订，K-LD02]** 命中数取决于「过」的定义，而冻结稿没有钉死它：
- `verdict.py:159–168` 的阶梯是 `if reasons: FAIL` → `elif oos < 1.0: WEAK_PASS` → `else PASS`；
- `flow-validation-20260917T091731Z.json` 的 `reasons` **只有一条**：`oos_sharpe 0.65 < the deflated threshold 1.33 at 44 trials`。门→0.00 则 reasons 空、0.5 ≤ 0.6468 < 1.0 → **WEAK_PASS**；
- `residual-validation-20260917T092004Z.json` 的 `reasons` 也**只有一条**：`oos_sharpe 1.05 < the deflated threshold 1.20 at 16 trials`。门→0.00 则 → **PASS**；
- `beidou_alpha/registry.py:374` 收 `{"PASS", "WEAK_PASS"}`——**WEAK_PASS 是上线口径**。
所以：「过」= PASS → 命中 **1**，Falsifier 未满足；「过」= 上线口径 → 命中 **2**，**Falsifier 满足，C-LD01 不被推翻**。
**更硬的一条**：`docs/RESEARCH_LOG.md:11567` 的**正文**原话是「真正『只被多重检验门拦住』的是 **flow**（0.65 对 1.33，只挂这一条）**与 residual**」——**两个**。冻结稿引的是该节的**标题**（「只差 selection gate 的只有 residual」），丢掉了与标题相左的正文句。
**[R 新增，K-LD12]** 还有一层反身性：定案所用的那次重跑**在测量的同时抬高了被测量的门**——residual 的门 09-09 是 1.0935（N=8）→ 跑前 1.1102（N=9）→ 跑后 **1.2046（N=16）**，差从 −0.018 扩到 −0.15，其中约 **0.09 是这次检验自己造成的**。本文对 mined 桶写过同形观察（E-LD08），却没把它用在自己赖以定案的那次观察上。
支持反方的证据：**[R 修订，K-LD04]** 冻结稿写「无」，不成立——**flow 是其中之一**：它被 §2.6 计进「活 2」，但它的 4 份 PASS 全在已弃用的 static 15 币 universe 上，4 份 pit 运行全部 FAIL，**且 pit 上唯一的 FAIL 理由就是操作者怀疑的那道门**。同一个对象在分子里算活、在分母里算死。
反证：E-LD15、E-LD02、`RESEARCH_LOG.md:11567`。决策影响：**H2 不再成立；转 H3 → Need Evidence。** Owner：操作者（一句话定义「过」即可重数，零成本）。

**C-LD02 · P0 · REFUTED（文件级）/ PARTIAL（口径级）· High**
命题：回测数据有缺陷，导致策略被误判。
**Axiom Trace ——A2**：结论强度受限于扫描覆盖面——本文扫了 **15,120,555 根** 1h bar（全库 878/878 个标的，含今天新回填的 830 万根，**此前从未被任何分析扫过**），重复 / 非单调 / NaN / OHLC 矛盾 / close≤0 全部为 **0**，缺口 0.0280%（E-LD05）。所以「文件级有缺陷」REFUTED。但扫描只覆盖文件，不覆盖口径，故整体 PARTIAL。
Falsifier：扫描发现 ≥1 类系统性缺陷，**且方向是让好策略看起来差** → 未观察到文件级缺陷；口径级三条保守偏差（成本纯 taker 无 maker、D-028 的 N 计近重复、`compound` 的 −1 地板）方向确实如此，但都不翻转 meanrev / breakout 的信号级阴性。
支持：E-LD05、E-LD10。决策影响：关掉「查数据」这条路。Owner：操作者。

**C-LD03 · P1 · SUPPORTED · High**
命题：门对**新**候选的功效偏低，而且与 D-028 无关——它是五年样本外的抽样噪声。
**Axiom Trace ——A2**：结论强度 = 证据强度，因为这是代码实测而非推理（E-LD03），且命令自印「真实联合功效只会更低」。
Falsifier：空桶 N=1 下真 Sharpe 1.0 的过线概率显著高于 50% → 实测 **50.0%**，未推翻。
支持：E-LD03。决策影响：说明「太严」有一半是对的，但那一半不能靠放宽阈值解决（α 是操作者签的），只能靠样本长度。Owner：操作者。

**C-LD04 · P1 · PARTIAL · Medium**
命题：「缺因子」在当前语法与数据内已被穷尽。
**Axiom Trace ——A6**：Falsifier 分三支写。**语法内**：~~存在未枚举的表达式形状且能产出正边际 → 914 个表达式已枚举、54 个正边际、**0 个 standalone 越过在跑的书**（E-LD07），语法内 REFUTED。~~ **[R 修订，K-LD11]** 这个判据用错了，而正确的判据写在本文自己的 E-LD08 里：挖掘候选**不需要 standalone 打赢整本书**，它要的是正边际。54 个有正边际，最好的那个（`594a12f9`，边际 **+0.4306**）**过了 selection gate**，死在 D-018 的回撤门上。所以「语法内已穷尽」按正确判据是 **PARTIAL 而非 REFUTED**——空间里有东西，它挂在组合层不是信号层。**语法外**：挖掘语法没有比较 / 阈值 / 指示函数（E-LD09），「当 X > k 时才做 Y」这类离散门控**从未进过任何搜索空间** → **UNKNOWN**，见 GAP-LD02。**数据外**：最便宜的一条（#27 上新）今天跑完并 REFUTED（E-LD06）。
支持：E-LD07、E-LD09。反证：E-LD09（语法缺口本身）。决策影响：决定 §6 的 O-LD4 值不值得定价。Owner：操作者。

**C-LD05 · P1 · SUPPORTED · High**
命题：新信息的**可得性**是当前的真实约束，而不是形式化能力。
**Axiom Trace ——A4「价值是否覆盖总成本、风险和机会成本？」**：#27 花 6 笔 ledger + 53 分钟回填 + 0.96 GiB 磁盘，换来一个 REFUTED（E-LD06）——这笔投入**值得**，因为它按预登记关闭了一条会一直被重提的假设；但它同时说明剩下的路更贵：盘口深度不可回溯（只能从今天起采，1–2 年后才有样本）、清算流不可得、社交/解锁无 vintage。
**Axiom Trace ——A6**：Falsifier = 存在第三条「机制明确 + PIT 可得 + 成本可估」的新信息源 → §5.3 逐条排过，未找到。
支持：E-LD06。决策影响：决定 §8 里「吞吐」这个目标能不能被买到。Owner：操作者。

**C-LD06 · P1 · SUPPORTED · High**
命题：`maker_fee_bps` 是一条**没有读取方**的死配置，成本模型是纯 taker 的。
**Axiom Trace ——A7「能否实现、测试、验收、观测、降级和回滚？」**：这条命题**可直接验收**——一条 grep 就能证伪它（`grep -rn maker_fee_bps --include="*.py" beidou_alpha beidou_live beidou_cli beidou_shared`），今天的输出是空（E-LD10）。
Falsifier：该 grep 有任一命中 → 未观察到。
决策影响：对高换手族（residual 换手 1,101–3,613、breakout 18,237）是一条实打实的保守偏差；对「迁移到真实资金」这个目标是一项必补工作。Owner：操作者。

**C-LD07 · P1 · SUPPORTED · High**
命题：15.6 天的实盘读数判不了 alpha，所以「近期收益还可以」既不支持也不反对框架。
**Axiom Trace ——A2**：结论强度 = 证据强度，因为证据**就是不稳定性本身**：同一个展开窗口的 `realised_sharpe` 在八个连续日读数里从 **−0.984** 摆到 **+7.167**（E-LD11），摆幅 8.15 个 Sharpe 单位，而 `expected_sharpe` 是 1.090。
Falsifier：`realised_sharpe` 的日间摆幅小于 `expected_sharpe` 的一半（<0.55）→ 实测 8.15，未推翻。
决策影响：关掉 Pre-04。Owner：操作者。

**C-LD08 · P1 · PARTIAL · Medium** **[R 修订，K-LD08；冻结稿为 `SUPPORTED · High`]**
命题：操作者选定的四个目标互相冲突，必须排序才能执行。
**Axiom Trace ——A4**：有限资源——(a)「更高 Sharpe」受 family gate 约束，每找一次就抬一次门，余量只剩 52 笔（E-LD04）；(b)「更小回撤」的工具是 D-018 门，而唯一过了 selection gate 的候选正是挂在它上面（E-LD08），动它是 R10 规则变更，会移动每一条历史裁决；(c)「更高吞吐」受 ledger 计费与 10-13 构造冻结双重约束；(d)「迁移到真实资金」要先补四条乐观偏差（成本模型 scale-free、冲击默认关、maker 不存在、抵押品重定价回测看不见），而其中三条**在 demo 上无法校准**（E-LD10）。(a) 与 (b) 在 D-018 门附近直接对立。
Falsifier：存在一个动作同时改善四项且不消耗 headroom → §6 逐个 Option 检查，未找到。
**[R 修订，K-LD08]** 六对关系里只有 **1 对有实例**（G-A × G-B，`594a12f9`），而 §8.1 的表**自己**把其余五格标成了「单向消耗」「弱冲突」「方向未知」「资源冲突不是机制冲突」。一个实例不足以支撑「必须**排序**」；而且即便冲突成立，「必须全序」也不从「冲突」推出——四个目标争一个人的时间是任何单人项目的常态，它论证的是**预算划分**。**主结论改写为「必须先定价，再由操作者决定是排序还是切分预算」。**
决策影响：D-LD01 的框定由它支撑，降级后框定仍成立但强度下降。Owner：操作者。

**C-LD09 · P1 · SUPPORTED · High**
命题：在当前 headroom 下，「再跑一轮 validate 来测试验证」的相对价值低于「不跑」。
**Axiom Trace ——A3「为什么优于现有、人工、自建、竞品、开源或不行动？」**：对照「不行动」——一次不显式传 `--grid` 的 tsmom 验证默认吃 16 格，把 N 从 299 推到 315，门从 1.5737 抬到 1.5796，而对一个真 Sharpe 1.5 的候选，过门概率从 43.3% 降到 42.8%（E-LD03）。付出的是 16/52 = **31% 的剩余 headroom**，买到的是 0.5 个百分点的功效损失与一次先验很低的读数。
Falsifier：存在一个网格，其「功效 × 先验」高于它消耗的 headroom 的机会成本 → §6 对每个 Option 算过，只有零 ledger 的那几个成立。
决策影响：关掉 Pre-05。Owner：操作者。

**C-LD10 · P1 · SUPPORTED · High**
命题：今天的回填让数据存储变成异质的，这是一项新增的系统性复杂度成本。
**Axiom Trace ——A5「直接、二阶和长期复杂度是什么？」**：直接——878/878 有 1h，但只有 255 个有 funding（E-LD14）。二阶——任何吃资金费的载入会按 KILL-027 拒跑，所以「全库 878 个标的」这句话对**不同的研究命令意义不同**。长期——`universe.json` 为空时，不传 `--universe pit` 的默认总体从 255 变 878，任何依赖默认总体的旧脚本读数会静默改变。
Falsifier：存在一条研究路径，它同时需要 878 个标的与 funding → 有（任何 carry 类研究），所以这条复杂度是真实的而非理论的。
决策影响：进 Risk Register（RISK-LD03）。Owner：操作者。

**C-LD11 · P1 · SUPPORTED · High**
命题：本次交付物是分析本身，它的每条「下一动作」都可被客观验收。
**Axiom Trace ——A7**：能实现（零代码）、能测试（§9.4 四条，每条有命令与客观预期）、能验收（§9.5）、能观测（M-LD01–03 各有读取位置）、能降级（排序表可以只取前两项）、能回滚（本文不改任何状态，删文件即回滚）。
Falsifier：任一条「下一动作」写不出客观预期 → §9.4 逐条写出。
决策影响：G7。Owner：操作者。

### 2.3 Assumption Register

| A ID | 假设 | 类型 | 当前依据 | 影响 | 风险 | 最小验证 / 阈值 / 未通过动作 | Gap | 上限 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **A-LD01** | 操作者说的「50 多个」与 ledger 口径可对账 | 可行性 | Context Intake Q1 答「ledger 里试过的全部配置」 | **H** | **L** | 已验证：§2.6 把三个口径的读数并排给出，由操作者当场认领 | — | — |
| **A-LD02** | 四个目标在未来数月不变 | 价值 | Context Intake Q2 全选 | **H** | **M** | 最小验证：§8 排序表交操作者确认一次；阈值 = 他对前两项排序无异议；未通过 → 重跑 §8，不重跑 §2–§5 | GAP-LD01 | Weak GO |
| A-LD03 | 构造冻结与 `reopen.yaml` 的措辞在 10-13 前不变 | 运营 | `governance/reopen.yaml`、CLAUDE.md | M | L | 一行记录，不建实验 | — | — |
| A-LD04 | demo 的成交样本无法校准冲击系数 | 可行性 | `config/costs.yaml` 自记（121 笔，order/ADV 中位 4.3e-07） | M | L | 已有支撑证据 E-LD10，进 Risk Register 监控 | — | — |

**高 × 高象限：无。** A-LD02 是高影响 / 中风险，已配最小验证并与 GAP-LD01 双向对齐。

### 2.4 Sizing Card

**N/A**：频率、规模、损失三项都有 E1 读数——频率是「第五次」（可数）、规模是 ledger 22,179 行与 10 个族（可数）、损失的单位是 headroom 52 笔（E-LD04 实测）。不需要数量级估算。

### 2.5 Gap Plan

| Gap ID | 影响 | 不确定性 | 决策影响 | 最小方法 / 阈值 / 失败动作 | Owner | 未完成时上限 |
| --- | --- | --- | --- | --- | --- | --- |
| **GAP-LD01** | A-LD02、§8 | M | **H** | 把 §8 排序表交操作者确认；阈值 = 前两项无异议；失败 → 按他的顺序重排，§6 定价不变 | 操作者 | Weak GO |
| **GAP-LD02** | C-LD04 语法外支 | **H** | M | 一次**零 ledger** 的枚举：给 `mining/expr.py` 加阈值/指示算子后只跑 `search` 的枚举与去重，**不打分、不写 ledger**，只报空间大小；阈值 = 去重后 ≤ 500 个新表达式则可定价，> 500 则先缩族；失败 → C-LD04 语法外支维持 UNKNOWN | 操作者 | 不收紧 |
| GAP-LD03 | C-LD07 | L | M | 等 M-010 满 30 天（2026-10-13 起可判）；无替代方法 | 时间 | 不收紧 |

### 2.6 Context Intake 的三个答案，以及分母的三个读数

本轮 L 级首轮暂停，三问已答（2026-09-18）：

| Q | 问题 | 答案 | 它改变了什么 |
| --- | --- | --- | --- |
| **Q1（Q-CRITICAL）** | 「50 多个策略」指哪一批 | **ledger 里试过的全部配置** | 分母从「09-08 那份 51 条清单」改为 `trials.jsonl`。§2.6 的三读数对账因此成为本文第一节 |
| Q2 | 「提升系统质量」优先改善哪一个 | **四个全选**（更高样本外 Sharpe / 同收益下更小回撤 / 更高吞吐 / 可安全迁移真实资金） | Success Definition 不能从单一目标推；问题重定义为**排序**问题（D-LD01） |
| Q3 | 10-13 后是否上真实资金 | **再观察数月后再定** | 四条乐观偏差进 **Should**，与 alpha 工作并行，不抢 10-13 裁定包的 12 笔名额 |

**按 Q1 选定的口径，分母有三个读数，差两个数量级。三个都对，服务不同的问题：**

| 读法 | 分母 | 活 | 它回答什么 |
| --- | ---: | ---: | --- |
| **人设计并建成的信号族** | **10**（tsmom / flow / xsmom / carry / residual / meanrev / breakout / pairs / chanlun / listing） | **2**（tsmom 主书 + flow 探针） | 「人设计的东西成功率如何」 |
| **不同的配置**（distinct `param_key`） | **约 20,600** | 2 | 「多重检验校正有没有必要」 |
| **ledger 行** | **22,179** | 2 | 「这个系统一共看过多少次假设」 |

关键在于这 22,179 行**不是** 22,179 个策略：19,772 行是**一次**配对搜索的 census（206 个标的两两组合，`ledger_scope` 规定配对策略要付配对搜索的钱），2,073 行是**一次**挖掘活动（676 个不同 1h 表达式；全部 10 份报告去重后 914 个），6 行是**一次**事件研究，3 行是前向板；剩下约 315 行才是 9 个手写信号族的参数网格（E-LD01）。

**[R 修订，K-LD01]** 冻结稿在这里写：~~「这三个读数都不支持『门太严』，而且方向相反：分母选得越大，多重检验校正越必要……所以操作者选的口径越宽，他的结论越站不住。」~~ **这个论证无效**，而且它与上一段自相矛盾——上一段说那 19,772 行是「**一次**配对搜索」（缩），这一段立刻把同一组行当成「20,600 个假设」（涨）。

**开门用的分母根本不是 20,605。** `beidou_alpha/validation/ledger.py:215` 的 `ledger_scope()`：手写族返回 `(strategy,)`，只有 `mined_*` 额外取 `mined` 桶、只有 `pairs*` 额外取 `pairs_search` 桶。九份 2026-09-17 同管线报告自记的 `n_trials` 就是各族真正面对的 N：

| chanlun | xsmom | carry | residual | breakout | meanrev | flow | tsmom | pairs |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| **1** | 1 / 9 | 5 | 9 / 16 | 10 | 28 | 44 | 106 | **19,777** |

**八个族面对的 N ≤ 44。** 两个大分母的族（pairs、mined）恰好就是本文自己主张「那是一次搜索」的那两个。20,605 与八个族的死因**没有因果连接**。

论证里真正有效的那一部分不需要这个分母：xsmom 与 chanlun 在 N=1、门 0.00 下照样 FAIL——它们挂的是 PBO 与 CPCV，不是选择门。

**[R 新增，K-LD20]** 九个手写族的 distinct `param_key` 合计是 **142**（tsmom 74 / meanrev 27 / breakout 9 / residual 8 / xsmom 8 / flow 7 / pairs 4 / carry 4 / chanlun 1）。**这才是「50 多个策略」最接近的可核对应物**，冻结稿漏了它。

**「20% 的族级通过率是高是低」本文不作判断。** 仓库里没有可核的领域基率，引文献会重犯 2026-09-18 另一份报告被其审查者判为 P1 的那个错（用未核验的 E3 把 0/7 写成「正常」）。本文只报可核的事实：族级 **10 活 2**，与「几十个里一个都没有」这个印象差一个数量级。

**Gate Review：G2 = PASS**（24 项证据全部 E1，可复核位置齐；两条 UNKNOWN 均为 P1，不触 H3；口径级三条保守偏差已登记，未被措辞消除）

---

## 3. Phase 3 · Problem Research

**Prerequisites**：G0 PASS、G2 PASS、E-LD01…E-LD14。
**Produces**：Problem Statement、5W2H、Causal Chain、两个框定与 D-LD01、Success Definition M-LD01–03、Stakeholder。
**Downstream**：§4 相对价值、§6 Option Set、§8 排序、§10 Validation Plan。

### 3.1 Problem Statement

**对于**单人运营的北斗 V5 操作者，**在**每次决定「下一笔研究投入放哪」的场景下，**由于**四个互相冲突的目标（更高样本外 Sharpe / 更小回撤 / 更高假设吞吐 / 可迁移到真实资金）**从未被排过序**，**遇到**每一轮研究方向由当天最响的那个怀疑决定的情况，**导致**可观察影响：自 2026-09-03 起新存活候选为 **0**，同期 tsmom family gate 的余量被消耗到 **+0.0181**（剩 52 笔），**不解决会**在 10-13 冻结解除时面对一个空队列，并把剩余 headroom 花在先验最低的那类动作上。

**独立性检验（G1 要求）**：移除操作者提出的三个方案（放宽门 / 查数据 / 加因子）之后，「10-13 队列放什么、按什么顺序」这个问题**仍然成立**——冻结解除是日历事实，与三个方案无关。

### 3.2 5W2H

**What**：四个目标未排序。**Who**：操作者一人（提出者、决策者、承担者三合一，无信息不对称，但也无第二双眼睛）。**When**：现在——10-13 距今 25 天，且 headroom 只剩 52 笔。**Where**：研究侧（`beidou research *`）与治理侧（`governance/`），不在实盘循环里。**Why**：四个目标是今天第一次被同时说出来（Context Intake Q2 全选），此前每轮只出现一个。**How**：按 §8 的 MoSCoW 与 §6 的定价排序。**How much**：本文推荐项零 ledger；最贵的一条（GAP-LD02 枚举）仍是零 ledger。

### 3.3 Causal Chain

| 层 | 内容 | Evidence | 可干预性 |
| --- | --- | --- | --- |
| 表层现象 | 「几十个策略只活两个」 | E-LD01、E-LD02 | — |
| 直接原因 | 分母的定义从未被写下来；同一印象对应三个差两个数量级的读数 | E-LD01、E-LD12 | **高**（一次对账即可，本文已做） |
| 深层原因 | 四个目标未排序，于是「哪个读数重要」没有依据 | Context Intake Q2、C-LD08 | **高**（§8 排序表） |
| 系统性原因 | 每一次验证动作都抬高所有后续候选的门，所以「先试试看」在这个系统里是有单向代价的 | E-LD03、E-LD04 | 中（只能靠预登记与功效表约束，不能消除） |
| 可干预杠杆 | ①（最高）把目标排序写下来；②把每次研究动作绑定到它服务的目标；③零 ledger 的枚举先于计费的搜索 | — | — |

### 3.4 Problem Reframing

| 框定 | 内容 | 能区分它的证据 | 判定 |
| --- | --- | --- | --- |
| **F1 门槛校准** | 问题是门设得太严，放宽就有更多策略可用 | 门降到 0.00 后能过的族数：若 ≥2 则 F1 成立 | **放弃**——实测只有 residual 一个（C-LD01 的 Falsifier 已观察）。但 F1 有一半是对的且被保留为 §5.2 的直接回答：门对**新**候选的功效确实偏低（C-LD03） |
| **F2 目标排序（选定）** | 问题是四个目标互相冲突且未排序，于是研究投入由当天的怀疑决定 | 四个目标之间是否存在真实冲突：若存在 ≥1 对直接对立则 F2 成立 | **选定**——(a) 更高 Sharpe 与 (b) 更小回撤在 D-018 门附近直接对立，E-LD08 是实例（C-LD08） |
| F3 因子覆盖 | 问题是缺少能救活这些策略的因子 | 语法内是否还有未枚举且能产出正边际的形状 | **部分放弃**——语法内 REFUTED（914 个表达式、0 个 standalone 越过）；**语法外（离散门控）UNKNOWN**，转 GAP-LD02 与 O-LD4 |

**D-LD01 · 选 F2。** 胜出理由：它是唯一一个在移除操作者原方案后仍然成立、且有可干预杠杆的框定。放弃 F1 的理由：Falsifier 已观察且方向相反；保留它的功效那一半进 §5.2。放弃 F3 的理由：语法内已穷尽，语法外未定价——它是 F2 排序表里的一个候选项，不是一个独立框定。**重开条件**：若操作者把四个目标收敛到单一目标，F2 失效，回到该目标自己的子问题。

### 3.5 Success Definition

> 写在任何 Option 出现之前。三个都是**结果**指标，不是本文任一方案的交付物——V3.8 明写按方案反推指标是禁止的。

| M ID | 类型 | 指标 | 基线（今天） | 读取位置 | 为什么它衡量问题而非方案 |
| --- | --- | --- | --- | --- | --- |
| **M-LD01** | ~~领先~~ → **交付完成度标记** | 每次研究动作在**执行前**写下它服务四个目标里的哪一个 | **0/0** | 预登记文本 / RESEARCH_LOG 新节 | **[R 修订，K-LD06]** 它同时是推荐方案的一半、§8.3 的唯一 Must 与 AC-LD04 的验收条件，因此**不是结果指标**——它量的是「推荐动作有没有被执行」。领先位由 M-LD04 接替 |
| **M-LD02** | 护栏 | **[R 修订，K-LD07]** ~~不因本文引出的动作下降~~（No-Build 下平凡通过，无法区分方案）→ **绝对阈值：到 2026-10-13，tsmom 桶 N ≤ 315** | **N=299**（门 1.5738，余量 +0.0181，翻转点 N=351） | `governance/verdicts.jsonl` 的 `family_gate` 行 | 绝对阈值与做不做本文无关，因此能证伪 |
| **M-LD04** | 领先 **[R 新增，K-LD06]** | 「过」的定义被钉死，且九份报告已按它重数一次 | **未定义** | 操作者的一句裁定 + 重数结果 | 它量的是 K-LD02 这条 P0 有没有被关闭，与本文推荐什么方案无关 |
| **M-LD03** | 滞后 | 2027-03-31 前有 ≥1 个 2026-09-03 之后新建的候选进入 probe 或前向板 | **0** | `governance_state.json`、`reports/research/forward_board.jsonl` | 它量的是管线是否恢复产出，对「靠哪个目标恢复的」不作要求 |

### 3.6 JTBD

**当我**要决定下一笔研究投入放哪时，**我想**知道每条路的价钱与它服务哪个目标，**以便**我能在 headroom 用完之前把它花在先验最高的那条上，**而不是**每次都从「是不是门太严」重新问一遍。

### 3.7 Stakeholder Map

| 角色 | 关系 | 本次变化 |
| --- | --- | --- |
| 操作者 | 提出者 + 决策者 + 承担者 | 得到排序表；**失去**「放宽门」这条省事的路 |
| 在跑的主书（tsmom） | **受损方** | 若排序把「更高 Sharpe」排第一，每一次搜索都消耗它的 headroom（剩 52 笔）。**补偿**：M-LD02 作为护栏，把它写成硬约束而不是希望 |
| 在跑的探针（flow） | 受影响 | 排序若把「迁移真实资金」排前，flow 的 4 份 PASS 全在 static universe 上这一点会成为优先复审项（10-02 到期） |
| 并行会话 | 受影响 | 共享 ledger 与数据 root；本文零写入，不影响 |

### 3.8 Scenario

**S-1（今天真实发生过）**：操作者第五次问「是不是门太严」。当前系统的回答依赖于分析者当天读了哪份报告，因而每次措辞不同。**排序表之后**：回答变成「你排的第 N 项是它，价钱是 X 笔 headroom，功效是 Y%」。
**S-2（10-13 当天）**：冻结解除，裁定包要排 12 笔。**今天的状态**：没有排序，队列按「谁最近被提起」填。

### 3.9 Journey

**COMPRESSED**：单人系统、单一触点（CLI + 报告），无跨角色交接，Journey 与 §3.8 的 Scenario 逐段重合。不展开不会遗漏任何判断——本文没有任何结论依赖于多步交互中的某一步。

### 3.10 Edge Cases

| 边界情形 | 不得混淆的点 |
| --- | --- |
| 「族级 10 活 2」与「配置级 20,600 活 2」 | 两者都对，服务不同的问题。前者答「人设计的东西成功率如何」，后者答「多重检验校正有没有必要」。**引用时必须写明是哪一个** |
| `research power` 的 N | 报告自记的 `n_trials`（259）与 family gate 的 N（299）是两个数。**定价必须用后者** |
| 「878 个标的」 | 对不吃 funding 的研究是 878，对吃 funding 的是 255（E-LD14） |
| 前向板的 `decidable: 0` | 是**正常读数**不是故障；板要攒年 |
| #27 的「六格全为正、两半样本同号」 | 这是正面读数，但**不改变 REFUTED**——判据是 \|t\| ≥ 3，实测 1.31 |

**Gate Review：G1 = PASS**（用户、场景、损失、绕行、来源偏差齐；Success Definition 在 Option 之前且三类俱全；问题在移除原方案后独立成立；C-LD00 追溯 A1；每条 P0/P1 有 Axiom Trace 与 Falsifier）

---

## 4. Phase 4 · Strategic, Relative Value & Economic Fit

**Prerequisites**：G1 PASS、C-LD00、M-LD01–03。
**Produces**：Strategic Fit、Relative Value、Business Case、Cost of Delay。
**Downstream**：§6 Option Set、§8 排序。

### 4.1 Strategic Fit：**High**

仓库的战略方向写在 `CONTEXT.md` 第一句：「系统的全部产出是一条每根闭合 bar 重算一次的目标权重，以及一份能自我说明的证据记录」。本文直接作用于第二半——它把「为什么这个策略没上线」从一段需要重读的叙述变成一张有价钱的表。同时它服务 `beidou-alpha-effort-target-90` 记的投入目标（alpha 90%）：排序表决定这 90% 花在哪。

### 4.2 Relative Value Proof：**Adequate**

| 对照 | 它能做到什么 | 本方案的增量 |
| --- | --- | --- |
| **不行动** | 操作者继续按当天的怀疑分配投入；第六次问同一个问题 | 增量 = 一张排序表 + 四个目标各自的价钱。**代价 0 ledger** |
| **现有方案**（已有的三份 09-17/09-18 分析） | 已经答了「为什么这个族死了」 | 它们没有把四个目标并排定价；且它们的三条待裁定今天已全部执行，其中一条 REFUTED，结论形状已变 |
| **流程 / 人工**（操作者自己排） | 完全可行，而且**更快** | 增量只在于价钱那一列（headroom、功效、先验）需要读代码与 ledger 才能填 |
| **自建 / 开源** | 无对应工具 | N/A |
| **竞品 / 第三方** | N/A（单人私有系统） | N/A |

**四个必答**：
1. **是否存在 20% 成本拿 80% 结果的方案？** 有，而且本文就是它——排序表的 80% 价值在「把四个目标写下来并标出两两冲突」，这一步零成本。剩下 20% 是每条路的精确定价，需要读 ledger 与跑 `research power`（也零成本）。
2. **用户可感知价值 vs 技术先进性**：可感知的是「下次不用再问第六遍」；本文不含任何技术先进性主张。
3. **切换 / 学习 / 信任成本**：零——排序表是一张表，不改任何命令或流程。
4. **什么证据会证明本方案并非相对最优？** 若操作者当场就能把四个目标排出序且对价钱无异议，那么本文的增量只剩定价那一列，相对价值降为 **Weak**，应缩范围为「只给价钱不给排序」。

### 4.3 Business Case

一次性成本：本文的撰写（已发生）+ 操作者读一遍并确认排序（约 15 分钟）。持续成本：每次研究动作多写一行「服务哪个目标」（M-LD01）。风险成本：0（零写入）。最小验证投入：0 ledger。对照收益：避免把剩余 52 笔 headroom 里的 16 笔花在一次先验最低的默认网格上（E-LD03、C-LD09）。

### 4.4 Economic Sustainability

**N/A**：单人系统，无付款方 / 使用方 / 供给方 / 运营方的多角色结构，不存在「某个角色长期净激励为负」的场景。唯一的持续成本承担者就是决策者本人。

### 4.5 Cost of Delay

**中等且单调递增，但有一个日期。** 每一次不带排序的验证动作平均消耗 headroom；tsmom 桶剩 52 笔，一次默认 16 格网格吃掉 31%。硬日期是 **2026-10-13T19:00Z**：冻结解除当天裁定包要排 12 笔，若那时仍无排序，这 12 笔由「谁最近被提起」决定。**不做的默认结果**：第六次问同一个问题。

**Gate Review：G3 = PASS**（五类对照齐，四必答齐，含「并非相对最优」的 Falsifier）· **G4 = PASS**（Strategic High；机会成本以 headroom 计量；§4.4 N/A 有证据）

---

## 5. Phase 5 · System Analysis

**Prerequisites**：G1、G3、G4。
**Produces**：Product Layer、As-Is/To-Be、Constraint Map、R0–R6、Engineering Pre-check、Risk Register。
**Downstream**：§6 Option Set、§9 Delivery。

### 5.1 Product Layer

本文作用在**研究与治理层**，不进入信号层、组合构建层或实盘循环层。产出是文档与一次操作者确认，不是代码路径。

### 5.2 As-Is / To-Be

| | As-Is（今天） | To-Be（排序表之后） |
| --- | --- | --- |
| 研究动作的依据 | 当天被提起的怀疑 | 排序表里的位次 + 该动作的功效读数 |
| 「门严不严」的回答 | 一个词（已问五次） | 一个数：`research power` 的对应格（PR #66 起进代码） |
| 分母 | 未定义，三个读数混用 | §2.6 三个读数各有其用途，引用时写明 |
| headroom 的消耗 | 事后在 verdicts.jsonl 里发现 | 事前在预登记里写下（M-LD01） |

**直接回答 F1 保留的那一半（C-LD03）**：门对**新**候选的功效偏低，而且与 D-028 无关——空桶 N=1、门就是 1.0 的 PASS 线时，一个真年化 Sharpe 1.0 的策略也只有 **50.0%** 的机会过线。这是 5.16 年样本外、年化标准误 0.4395 的结构性后果。要把它抬到 80%，要么真 Sharpe ≥ 1.4，要么样本外翻倍。**这不是放宽阈值的理由**（α 是操作者签的 0.05），能改的只有样本长度——前向板攒年是唯一在架的机制。

### 5.3 Constraint Map

| 约束 | 内容 | 来源 |
| --- | --- | --- |
| 构造冻结 | 到 2026-10-13T19:00Z；任何进 registry 的新策略都改构造指纹、清零 M-010 | `CLAUDE.md`、construction fingerprint v5 |
| ledger 计费 | 每次 validate 按网格格子计费；tsmom 桶剩 52 笔 | E-LD04 |
| source budget ratchet | 三个包零 headroom，加行即红 | `tests/architecture/test_source_budget.py` |
| 数据异质 | 878 有 1h，255 有 funding | E-LD14 |
| 多会话并行 | 共享 ledger / 数据 root / 实盘；代码改动走 worktree | `CLAUDE.md` |
| 新信息可得性 | 盘口深度不可回溯、清算流不可得、社交/解锁无 vintage；#27 今天 REFUTED | E-LD06 |

### 5.4 Impact Radius R0–R6

**R0（本文自身）**：`docs/analysis/` 新增一份文档 + `analysis-calibration.md` 一行。**R1–R6：无。** 本文不改代码、配置、ledger、registry、构造、实盘状态或任何共享文件。删除该文档即完全回滚。若操作者后续裁定执行 GAP-LD02 的枚举，那次动作的影响半径另算（届时落在 `beidou_alpha/mining/expr.py`，会碰 source budget ratchet）。

### 5.5 Engineering Pre-check

| 项 | 读数 | 来源 |
| --- | --- | --- |
| 实盘循环健康 | `restarts = 53`，最后一次 2026-09-17T16:07:34Z——**过去 24 小时零重启**（历史速率 ≈3.8 次/天） | E-LD13 |
| 数据完整性 | 15,120,555 根 1h bar，五类结构性缺陷全 0，缺口 0.0280% | E-LD05 |
| 成本模型 | 纯 taker；`maker_fee_bps` 无读取方；`impact` 默认关 | E-LD10 |
| 功效工具 | `research power` 可用、零 ledger、强制从报告借方差 | E-LD03 |

**未用推理填空的项**：数据完整性是本文实测而非引用；`maker_fee_bps` 无读取方是 grep 实测而非推断；功效表是命令输出而非手算。

### 5.6 Risk Register

| Risk ID | 风险事件 | 关联 | 概率 | 影响 | 预警 | 缓解 | 失败动作 | 人类确认点 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **RISK-LD01** | 操作者把「门与数据没问题」读成「系统已最优」，停止投入 | C-LD01、C-LD02 | M | **H** | 下一次对话里出现「那就不用改了」 | Decision Memo 首格写「新存活 0」而非「门没问题」；§8 给出 10 条待办 | 重申 C-LD00：问题真实，只是归因错 | 否 |
| **RISK-LD02** | 本文顺着仓库两周「否决有效」的叙事写（确认偏差），与 09-17 / 09-18 各份同形 | 全文 | **H** | M | 结论与前几份分析方向完全一致且无新反证 | §7 独立子代理审查；§2.1 主动记录自己的一处扫描错误（E-LD05） | 按审查结论修订并保留原文 | 否 |
| **RISK-LD03** | 数据异质导致某次研究静默读到 878 个标的的默认总体 | C-LD10 | M | M | 报告的 `dataset` 块标的数 ≠ 预期 | 研究运行显式传 `--universe pit`；载入 funding 时按 KILL-027 自动拒跑 | 重跑并作废该报告 | 否 |
| **RISK-LD05** **[R 新增，K-LD22]** | #2 的 `data pool history` 向多会话共享的 `.beidou/data` 并发写（`--sync` 默认开） | §8.2 #2 | M | M | 另一会话同时在跑研究或回填 | AC-LD02 的人类确认点；开工前核并发 | 停止并核对 `membership.parquet` 末行 | **是** |
| **RISK-LD04** | 排序表被当成承诺，操作者据此跳过预登记 | M-LD01 | L | **H** | 出现「排序里第一项，直接跑」 | M-LD01 量的是「写下服务哪个目标」，**不替代**预登记 | 按 R10 / D-028 拒跑 | **是**（任何计费动作） |

**Gate Review：G5 = PASS**（As-Is/To-Be、约束、R0–R6、工程预检齐；工程未知项全部由实测而非推理确认；C-LD10 追溯 A5）

---

## 6. Phase 6 · Solution Reasoning

**Prerequisites**：G1–G5。
**Produces**：Option Set、Failure Modes、D-LD02 推荐、Decision Log。
**Downstream**：§8 Scope、§9 Delivery。

### 6.1 Option Set

| Option | 方案 | 成本 | 可逆 | 结论 |
| --- | --- | --- | --- | --- |
| **O-LD0 No-Build** | 维持现状：不排序，按当天怀疑分配 | 0 | — | 基线。**代价已量**：15 天 0 新存活、headroom −0.0166 |
| **O-LD1 Process（推荐）** | **四目标排序表**：把四个目标两两冲突点写明，各标价钱（headroom / 功效 / 先验 / 日历时间），交操作者排一次序；之后每次研究动作在预登记里写一行「服务哪个目标」 | **0 ledger、0 代码**；操作者约 15 分钟 | 完全（删文档） | **推荐**。它是唯一同时服务四个目标的动作——因为它的产出就是四者的相对次序 |
| **O-LD2 Process** | 只给价钱不给排序（§4.2 第 4 问的缩范围版本） | 0 | 完全 | 备选：若操作者当场能自己排序，O-LD1 退化为它 |
| **O-LD3 Small-Build** | 补 maker 路径：让 `maker_fee_bps` 有读取方，成本模型区分 maker/taker 换手 | 代码落在 `beidou_alpha`（碰 source budget ratchet）；**重测旧候选要另计 ledger** | 可逆 | **Should**，绑定「迁移真实资金」这个目标。操作者答「再观察数月后再定」→ 不抢 10-13 名额 |
| **O-LD4 Could** | 离散门控进挖掘语法（阈值 / 指示算子），只对信号级有信息的族枚举 | **未定价**——先做 GAP-LD02 的零 ledger 枚举报空间大小 | 可逆 | **Could**：先验低（meanrev 的 `trend_gate_z` 27 格是同类唯一先例且全负），但它是 C-LD04 唯一未被排除的一支 |
| **O-LD5 Full-Build** | 盘口深度采集 job（前向新信息） | 采集 + 存储 + 对齐契约；**1–2 年后**才有可验证样本 | 可停 | Could：10-13 后与 O-LD4 一起裁 |
| **O-LD7 Process** **[R 新增，K-LD09]** | **不排序，按预算切分**：给四个目标各分固定的 trials / 时间配额，不定全序 | 0 | 完全 | 审查指出 D-LD01 的重开条件进了重开条件却没进选项集，Option Set 在核心决策上是伪多方案。本项补上它 |
| **O-LD6** | 放宽 D-020 / D-028；在现有语法内再挖一轮；给判负族加连续乘法型 regime 门 | 抬所有人的门 | — | **Won't**：C-LD01 REFUTED；C-LD04 语法内 REFUTED；regime 45 格 0 正 |

### 6.2 Failure Modes

| F ID | Option | 失败模式 | 原因 | 预警 | 预防 | 失败动作 |
| --- | --- | --- | --- | --- | --- | --- |
| **FM-LD01** | O-LD1 | 排序表排完就没人看，第六次同问 | 它是文档不是机制；M-LD01 靠自觉 | 下一份预登记里没有「服务哪个目标」那一行 | **[R 修订，K-LD10]** ~~M-LD01 的读取位置定在预登记文本~~——那是**在哪里读**，不改变「靠自觉」。真正的机制先例是 **PR #40**（不传 `--grid`/`--charge` 直接拒跑）与 **PR #67**（前向板出口契约）：把规则变成命令的拒绝条件。本文**不主张**已有此机制 | 把它降级为 O-LD2，只留价钱 |
| **FM-LD02** | O-LD1 | 排序被当成承诺，跳过预登记直接跑 | 排序表读起来像待办清单 | 出现「第一项，直接跑」 | RISK-LD04 的人类确认点 | 按 R10 拒跑 |
| **FM-LD03** | O-LD3 | 补了 maker 路径，但旧候选重测要重新计费，反而吃掉 headroom | 成本口径变更按 D-033/D-034 先例要重出证据 | 重测计划里出现 tsmom 桶 | 只在「迁移真实资金」被排进前两位时启动，且先算重测的 ledger 账 | 推迟到真实资金决定之后 |
| **FM-LD04** | O-LD4 | 枚举出的空间过大，一次搜索把 `mined` 桶的门再抬一截（今天已 1.785） | 离散算子会使空间组合爆炸 | GAP-LD02 报出 > 500 个新表达式 | GAP-LD02 的阈值就是这条：> 500 先缩族再定价 | 维持 C-LD04 语法外支 UNKNOWN |

### 6.3 Recommendation Decision · **D-LD02**

**推荐 O-LD1**（四目标排序表），**放弃** O-LD6（Won't），**推迟** O-LD3 / O-LD4 / O-LD5 到排序完成之后。

- **胜出理由**：四个目标全选（Context Intake Q2）之后，任何单目标方案都会被另外三个目标的支持者推翻；排序是唯一能让后续每个方案被公平定价的前置动作。而且它零 ledger、零代码、完全可逆，在 M-LD02 这条护栏下不消耗任何 headroom。
- **关键成立条件**：A-LD02（四个目标在未来数月不变）。
- **本推荐自己的 Falsifier**：操作者读完排序表后无法排序，或排完序在两周内又按另一个顺序行动 → 说明冲突不在目标层而在别处，O-LD1 失效，回到 O-LD2。
- **灰度 / 降级 / 回滚**：灰度 = 先只排前两位；降级 = O-LD2（只给价钱）；回滚 = 删除本文档，无其他状态变化。
- **接受了谁的损失、为什么、补偿**：受损方是**在跑的主书 tsmom**——若排序把「更高 Sharpe」排第一，它的 headroom（剩 52 笔）会被后续搜索消耗。**补偿是 M-LD02**：把「余量不因本文引出的动作下降」写成护栏指标而不是希望。第二受损方是操作者本人，他失去「放宽门」这条省事的路，**无补偿**——因为这条路经 C-LD01 的 Falsifier 观察后并不存在。
- **最可能的偏差与对策**：**确认偏差**（RISK-LD02）——本文与 09-17 / 09-18 各份分析结论方向一致，而那几份都被各自的审查抓到过同一种毛病。对策：§7 独立子代理审查；§2.1 主动记录本文自己的一处测量错误（E-LD05 的毫秒/纳秒），让读者能校准本文的可靠性。次要偏差：**锚定**——操作者提出的三个方案先入为主，对策是 §3.4 强制写两个替代框定并各配区分证据。

### 6.4 Decision Log

| D ID | 决策 | 备选 | 理由 | 可逆性 | 重开条件 |
| --- | --- | --- | --- | --- | --- |
| **D-LD01** | 框定选 F2（目标排序） | F1 门槛校准、F3 因子覆盖 | F1 的 Falsifier 已观察且方向相反；F3 语法内 REFUTED | 完全 | 操作者把四目标收敛到一个 |
| **D-LD02** | 推荐 O-LD1 | O-LD0 / O-LD2 / O-LD3–5 / O-LD6 | 唯一同时服务四目标且零成本的动作 | 完全 | 推荐自己的 Falsifier 成真 |
| **D-LD03** | 不跑任何计费实验 | 跑一轮 validate「测试验证」 | C-LD09：16 格吃掉 31% 剩余 headroom，换 0.5 个百分点功效损失与一次低先验读数 | 完全 | 排序把「更高 Sharpe」排第一且有具体候选 |

**Gate Review：G5 复核 = PASS**（Option Set 含 No-Build / Process / Small-Build / Full-Build / Won't，非伪多方案；Failure Modes 四条各有预警与失败动作；Recommendation 全字段含自己的 Falsifier 与受损方补偿）

---

## 7. Phase 7 · Adversarial Review

### 7.1 独立性声明

| 项目 | 内容 |
| --- | --- |
| 冻结输入 | 本文件的 672 行冻结状态（审查者实测行数一致）；基线 `938492ce` |
| 审查者 | **独立子代理**（同模型家族、不同上下文）。未读作者对话历史、草稿或解释性文字 |
| 证据范围 | 冻结文件；`reports/**`、`governance/**`、`config/**`、`docs/**`、`beidou_alpha/**`、`beidou_cli/**`、`.beidou/data/**`、`.beidou/live/state.json`；附录 D 与 SKILL §4/§6/§9/§10 |
| **审查者自述的限制** | 分两次 Read 读全文，第一次已覆盖到 §6.1 Option Set，因此「先写反方论点再看方案」**未能完全隔离**。按 D.1 第 4 条启用补偿：每条 P0 Kill 定位到作者具体哪一步推理错。另：E-LD05 的 NaN / OHLC 矛盾 / close≤0 三格未复算 |
| 外部动作 | 全程只读。跑了 8 次零 ledger 的 `research power`；跑前跑后 `trials.jsonl` **22,179 行、md5 逐位未变**。未写任何仓库文件 |

### 7.2 最强反方论点（审查者在看方案前写下）

> 「这份分析用『新存活 0』当作问题存在的证据，而它自己的 E-LD03 功效表预测『新存活 0』正是系统在这个窗口的众数结果——问题的损失与门的功效是同一个数的两种读法，用其中一个证明另一个是循环。」

与之并列的是 K-LD01：全文最响的那句「分母选得越大，你的结论越站不住」，用的是一个**代码明确拒绝用来开门**的分母。

### 7.3 独立复算（19 项）

审查者从一手数据独立重取，**19 项逐项一致**：ledger 22,179 行与全部分桶；distinct `param_key` 20,605；九族行数 315；79 份报告的逐族逐格判定（PASS 31 / WEAK 7 / FAIL 41）；flow 的 4 份 PASS 全在 15 币 universe；`research power` 的 N=299 门 1.5737 / 43.3%（亲跑）与 N=1 的 50.0%（算术核）；family gate 逐字；翻转点 N=351（亲跑：门(350)=1.5917 < 1.5919 ≤ 门(351)=1.5920）；15,120,555 根 bar；缺口 4,235 与逐标的分布；挖掘 914 / 54 正 / top 三个 standalone；maker 零命中退出码 1；实盘八日序列与摆幅 8.151；51 条分块逐字；因子层三文件行数与 20 / 12 / 10 个名字。

**审查者结论**：「冻结文件的数字层几乎无可指摘——19 项里 19 项与我独立取的一致。问题全部在这些数字被用来推什么。」

**审查者独立补出、冻结稿没有的数**：九族 distinct `param_key` 合计 **142**；20,448（**99.2%**）属于 pairs_search + mined 两次自动搜索。

### 7.4 Pre-Mortem（受损方那一行是审查者补的）

| 失败原因 | 预警 | 当前证据 |
| --- | --- | --- |
| 排序表写完，10-13 的 12 笔仍按「谁最近被提起」填 | 下一份预登记无「服务哪个目标」行 | FM-LD01 的预防不是预防（K-LD10） |
| 「门与数据没问题」被读成「不用改了」，而 F1 只被半证伪 | 出现「那就不用改了」 | K-LD01 / K-LD02：F1 被放弃得比证据允许的更早 |
| **受损方**：flow 在 10-02 复审时因「PASS 全在 static universe」被撤，两个存活者变一个 | 10-02 复审排期 | §3.7 自记此事；pit 上 flow FAIL 且只挂 D-028 |
| **补偿核对**：§6.3 给 tsmom 的补偿是 M-LD02，而 M-LD02 在 No-Build 下平凡通过，**因此它不是补偿**；给 flow 的补偿**不存在** | — | K-LD07、K-LD04 |

### 7.5 Inversion（「怎样让它彻底失败，当前是否在做」）

审查者列七条，**六条在做**：让唯一的 Must 同时充当衡量它自己的指标（**在做**，K-LD06）；把结算窗口设在决策日期之后（**在做**，M-LD03 → 2027-03-31 而硬日期 10-13）；用系统的正常输出当故障证据（**在做**，K-LD05）；在同一段里把一组数先缩后涨（**在做**，K-LD01）；同一对象在分子里算活、分母里算死（**在做**，K-LD04 的 flow）；用一次会改变被测量的测量去定案（**在做**，K-LD12）；反方审查交给同一上下文（**否**——本次是独立子代理）。

### 7.6 Kill Register

**22 条：P0 ×2、P1 ×11、P2 ×9。审查时未关闭 P0 = 2。**

**P0**

- **K-LD01 · OPEN → 本版以改写关闭** — census 分母与 correction 分母被当成同一个。`ledger_scope()` 证明八个族的 N ≤ 44。**作者哪一步错**：§2.6 相邻两段把同一组行先缩（「一次搜索」）后涨（「20,600 个假设」）。**处置**：§2.6 与 Decision Memo 的分母推论已删除，换成逐族 N 表。
- **K-LD02 · UNKNOWN，只能由操作者关闭** — Falsifier 命中数 1 还是 2，取决于「过」= PASS 还是含 WEAK_PASS 的上线口径；`RESEARCH_LOG.md:11567` 的正文说是**两个**，冻结稿引的是该节标题。**作者哪一步错**：C-LD01 的 A6 Trace 把「只差 selection gate 的 PASS 只有 residual」压缩成「能过的只有一个」。**处置**：C-LD01 转 UNKNOWN，H2 撤回，转 H3 → Need Evidence。

**P1（11 条，本版全部以修订、降级或补产物关闭）**：K-LD03 Falsifier 观察无 Evidence ID（→ 新增 E-LD15）；K-LD04 flow 同时被计为活与判负（→ C-LD01 反证字段改写）；K-LD05「新存活 0」与 E-LD03 循环（→ C-LD00 损失项只留 headroom）；K-LD06 M-LD01 同时是指标 / Must / 验收 / 方案的一半（→ 降为完成度标记，新增 M-LD04）；K-LD07 三个指标都不能证伪推荐（→ M-LD02 改绝对阈值 N ≤ 315）；K-LD08 C-LD08 仅一个实例（→ 降 PARTIAL，主结论改「必须先定价」）；K-LD09 Option Set 伪多方案（→ 新增 O-LD7「按预算切分」）；K-LD10 FM-LD01 的预防是读取位置不是机制（→ 改写并指向 PR #40 / #67）；K-LD11 C-LD04 用 standalone 判据（→ 改 PARTIAL，瓶颈在组合层）；K-LD12 定案那次重跑把 residual 的门抬了 0.09（→ 登记进 C-LD01）；K-LD13 自检表 Phase 1 标 PASS 但内容不在文件里（→ 改 N/A 带理由）。

**P2（9 条）**：K-LD14 §10 缺 Downstream 与 Gate Review（已补）；K-LD15 校准行标 PASS 但文件未追加（**已实际追加并核 mtime**）；K-LD16「10 个族」与「九个手写族」不一致；K-LD17 基线 commit 写错（已改 `938492ce`）；K-LD18「0 越过基线」只覆盖八轮里的一轮；**K-LD19** `Squash = tanh(inner/scale)` 在 `search.py` 里**逐 scale 枚举**，scale→0 即零点软门，`Semi` 内含固定在 0 的下行指示——**「离散门控从未搜过」偏强**，零点软门已被搜过 914 次、产出 54 个正边际、0 个存活（→ O-LD4 先验调低）；K-LD20 九族 142 这个数缺失（已补）；K-LD21 E-LD06/08/14 标 E1 但来源是自家日志（已降 E2）；K-LD22 共享目录写入无 Risk 行（→ 新增 RISK-LD05）。

**人类确认点（D.8，不得以 ACCEPTED 覆盖）**：AC-LD02（写共享 `.beidou/data`，`--sync` 默认开）、AC-LD03（改 `mining/expr.py`，碰 source budget ratchet）、RISK-LD04（任何计费动作）。审查者确认三条均必要。

### 7.7 G6 与 Final Kill Decision

**G6 = FAIL**（2 条 P0 未关闭，且两条都落在支撑原 Final Decision 的同一步上）。按 D.7 硬规则 3，**G7 由 PASS 降为 PARTIAL**。

**审查者的 Final Kill Decision：Need Evidence。** 命中 **H1**、**H3**、**H7**；**H2 不成立**——作者据以判 PIVOT 的「C-LD01 REFUTED」正是被推翻的那一步。

审查者要求的新证据**两条，都零 ledger、零代码、当天可得**：① 操作者一句话钉死「过」= PASS 还是含 WEAK_PASS 的上线口径，然后按 `verdict.py:159–168` 重数九份报告；② 用九份报告自记的 `n_trials` 替换分母推论（**本版已做**）。

> 审查者留给操作者的一句话：「这份分析的**数字**经得起复算——我独立重取的 19 项逐项一致。它的问题不在证据，在证据被用来推什么。而讽刺的是，RISK-LD02（确认偏差）已经预言了这件事：『本文顺着仓库两周「否决有效」的叙事写』。它写对了，而且写对之后仍然那么写了。」

**作者按语**：本版按上述 22 条全部修订，被推翻的句子保留原文并标 `[R 修订，K-LDxx]`。**审查者未复审本版。** 两条 P0 里 K-LD01 已以改写关闭；**K-LD02 只能由操作者关闭**——它需要的是一句定义，不是更多分析。

---

## 8. Phase 8 · Scope & Priority

**Prerequisites**：D-LD01、D-LD02、G1–G6。
**Produces**：四目标冲突矩阵、带价钱的待办清单、MoSCoW、In/Out Scope、Scope Firewall、Decision Compression。
**Downstream**：§9 Delivery、§10 Validation Plan。

> **这一节是本文的交付物。** 前面七节是为了让这张表的每个格子有依据。

### 8.1 四个目标的两两冲突

操作者本轮把四个目标**全部选中**（Context Intake Q2）。它们不是互补的：

| | **G-B** 更小回撤 | **G-C** 更高吞吐 | **G-D** 迁移真实资金 |
| --- | --- | --- | --- |
| **G-A** 更高 Sharpe | **直接对立**：唯一过了 selection gate 的候选 `594a12f9` ΔOOS Sharpe **+0.2931**、回撤恶化 0.0255，被 D-018 拒（E-LD08）。买 Sharpe 常常是用回撤买的 | **单向消耗**：每次搜索抬高所有后续候选的门；tsmom 桶余量剩 **52 笔**（E-LD04） | **冲突**：补成本口径（maker）后高换手族要重测，重测计费 |
| **G-B** 更小回撤 | — | **弱冲突**：动 D-018 是 R10 规则变更，不花 ledger 但会移动每一条历史裁决 | **方向未知**：抵押品重定价回测看不见（49% 权益是 BTC），补上它之后回撤读数往哪边走没测过 |
| **G-C** 更高吞吐 | — | — | **资源冲突不是机制冲突**：G-D 的四项都是代码工作、零 ledger，但占用同一个人的时间 |

**读法**：G-A 与 G-B 在 D-018 门附近**直接对立**，这是本文认定「必须排序」的硬证据（C-LD08）。其余三对是消耗或资源关系，可以并行但要计价。

### 8.2 带价钱的待办清单

> 价钱单位：**笔** = ledger trials（消耗 tsmom 桶 52 笔余量的部分特别标注）；**零 ledger** = 不写 `trials.jsonl`。

| # | 项 | 服务 | 价钱 | 先验 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 1 | **M-LD01：每次研究动作写下服务哪个目标** | A B C D | **零** | — | **本文新提** |
| 2 | pit 成员表重建到今天（09-03 后已前推 13 天） | A C | 零 ledger；**但写共享 `.beidou/data`，`--sync` 默认开** | — | 本文新提，**人类确认点** |
| 3 | GAP-LD02：离散门控的**零 ledger 枚举**（只报空间大小，不打分） | A | 零 ledger；代码改动碰 source budget ratchet | **低**（`meanrev.trend_gate_z` 27 格是同类唯一先例，全负） | 本文新提（O-LD4 前置） |
| 4 | EXP-AE2：D-018 回撤门的独立推导 | B | 规则事务，零 ledger | — | 已排定 10-13 |
| 5 | 退出档位 EXP-AE1 / `trailing_activate` EXP-AE3 | B | **8 笔 + 4 笔** | — | 已排定 10-13 |
| 6 | `flat_inside_band` A/B | B | **约 2 笔** | — | 已排定 10-13 |
| 7 | **maker 路径：给 `maker_fee_bps` 一个读取方** | D | 代码（碰 ratchet）+ **旧候选重测另计笔数** | — | 本文新提（O-LD3）；Q3 答「再观察数月」→ Should |
| 8 | 冲击模型默认开 + 校准 | D | 代码零 ledger；**但 demo 无法校准系数**（A-LD04） | — | 阻塞在数据 |
| 9 | 参与率进回测 | D | 代码 | — | 目前只在实盘 |
| 10 | 抵押品分母：mainnet 前只用 USDT | B D | 已裁定 | — | 已裁定 |
| 11 | 盘口深度采集 job | A | 采集 + 存储 + 对齐契约；**1–2 年后**才有样本 | — | Could，10-13 后裁 |
| 12 | 前向板攒年（在架机制） | A | `add` 计费、`status` 不计费 | — | 运行中：tsmom 2.35 年、residual 3.48 年 |

**已关闭，不再是选项**：#27 上新事件（**今天 REFUTED**，最大 \|t\| 1.31 对要求 3，E-LD06）；~~语法内再挖一轮（914 个表达式、0 个 standalone 越过）~~ **[R 修订，K-LD11]** 判据错，改为：语法内**仍有 54 个正边际候选**，最好的那个过了 selection gate 而挂在 D-018 上——不是「关闭」，是「瓶颈在组合层」；连续乘法型 regime 门（45 格 0 正）。

### 8.3 MoSCoW

| 级别 | 项 | 依据 |
| --- | --- | --- |
| **Must** | **[R 修订，K-LD02 / K-LD06]** **#0（新）：操作者钉死「过」的定义并按 `verdict.py:159–168` 重数九份报告**——零成本；#1（M-LD01）降为 Should | #0 关闭本文唯一未关闭的 P0（K-LD02），它决定 C-LD01 是 UNKNOWN 还是 REFUTED |
| **Should** | #4 #5 #6（已排定 10-13，服务 G-B）、#7（maker，服务 G-D） | Q3 答「再观察数月后再定」→ G-D 进 Should 而非 Must，不抢 10-13 的 12 笔名额 |
| **Could** | #2 #3 #8 #9 #11 | 各自有阻塞条件或先验偏低 |
| **Won't** | 放宽 D-020 / D-028 任何阈值；语法内再挖一轮；给判负族加连续乘法型 regime 门；冻结期内改任何构造参数；用这 15.6 天的实盘数字选任何参数 | C-LD01 REFUTED、C-LD04 语法内 REFUTED、C-LD07、构造冻结 |

**不以「第一版」为理由删除的项**：#10（抵押品）与 M-LD02（headroom 护栏）是资金与数据完整性要求，不进 Could。

### 8.4 In / Out Scope 与 Scope Firewall

**In**：本文档；`docs/analysis/analysis-calibration.md` 追加一行。
**Out**：任何代码、配置、ledger、registry、构造、`RESEARCH_LOG` 变更；任何 `validate` / `mine` / `book` / `diagnose` 运行；冻结期内的任何预登记；任何实盘动作。

**Scope Firewall**：本文出现的任何「应该做 X」都**不构成执行授权**。#2 与 #3 各自需要一次具名裁定才能开工（§9.5）。范围扩大的唯一合法路径是操作者按 §8.3 重排 MoSCoW，而不是在执行某一项时顺手做相邻的一项。

### 8.5 Decision Compression

**一句话**：四个目标里，唯一零成本且同时服务四者的动作是**先排序**；其余每一项都至少与另一个目标竞争资源，所以它们的次序必须先定、不能边做边定。

**Gate Review：G3–G6 复核 = PASS**（范围足以验证最关键 Claim C-LD08——排序表本身就是它的检验：若四个目标不冲突，这张表会是空的）

---

## 9. Phase 9 · Delivery Contract

**Prerequisites**：§8 Scope、D-LD02、Risk Register。
**Produces**：Test / Acceptance Contract、Source Trace Matrix。
**Downstream**：§10 Validation Plan。

### 9.1 交付物是什么

**分析本身**（一份报告、一张排序表、两个交操作者的裁定）。按 SKILL Phase 9 规则，§9.2 与 §9.3 写 N/A，§9.4 与 §9.5 仍要写，对象换成本次的每条「下一动作」。

### 9.2 PRD Contract — **N/A：交付物是本文**
### 9.3 Development Contract — **N/A：交付物是本文**

### 9.4 Test Contract

| ID | 下一动作 | 前置 | 操作 | 客观预期 |
| --- | --- | --- | --- | --- |
| **T-LD01** | 排序表可用性 | 操作者读完 §8 | 请他对 §8.3 的 Must / Should 排序表态 | 他能在不追加信息的前提下对**前两位**给出次序或明确异议 |
| **T-LD02** | 功效读数可复现 | 无 | `.venv/bin/python -m beidou_cli research power --evidence reports/research/tsmom-validation-20260917T074143Z.json --trials 299 --charge 16` | 打印 N=299 门 **1.5737**、真 1.5 过门 **43.3%**；N=315 门 1.5796、**42.8%**；`trials.jsonl` 行数**不变**（22,179） |
| **T-LD03** | maker 无读取方可复现 | 无 | `grep -rn "maker_fee_bps\|maker_fee" --include="*.py" beidou_alpha beidou_live beidou_cli beidou_shared` | **零命中**（退出码 1） |
| **T-LD04** | 数据扫描可复现 | 无 | 重跑本文的只读扫描脚本（`open_time` 按 `unit="ms"` 解析） | 15,120,555 根；重复/非单调/NaN/OHLC 矛盾/close≤0 全 0；缺口 4,235；BNXUSDT 638 |

### 9.5 Acceptance Contract

| ID | 验收条件 | 证据位置 | Owner | 失败动作 |
| --- | --- | --- | --- | --- |
| **AC-LD01** | 操作者对 §8.3 的前两位排序表态（GAP-LD01） | 本次对话 | 操作者 | 按他的顺序重排 §8.3；§6 定价不变，不重跑 §2–§5 |
| **AC-LD02**（**人类确认点**） | #2 pit 成员表重建**是否开工**。注意 `data pool history` 的 **`--sync` 默认为开**（会为每个候选下载日线），且 `--root` 默认 `.beidou/data` 是**多会话共享目录** | `.beidou/data/membership.parquet` 的 mtime 与末行日期 | 操作者 | 不开工则 pit 成员表继续从 2026-09-03 前推；影响中性、量级 13/2042 天 |
| **AC-LD03**（**人类确认点**） | #3 GAP-LD02 枚举**是否开工**。阈值写在前面：去重后 ≤ 500 个新表达式则可定价，> 500 先缩族 | 枚举输出（不写 ledger） | 操作者 | 不开工则 C-LD04 语法外支维持 **UNKNOWN**，O-LD4 不定价 |
| **AC-LD04** | 下一份预登记里出现「本次服务四个目标里的哪一个」一行（M-LD01） | 预登记文本 / `RESEARCH_LOG` 新节 | 操作者 | M-LD01 记 0；FM-LD01 成真，本文降级为 O-LD2 |

### 9.6 Source Trace Matrix

| Delivery Item | Problem/JTBD | Evidence | Claim | Decision | Gate/Kill | Scope | Test/Metric |
| --- | --- | --- | --- | --- | --- | --- | --- |
| §8 排序表 | §3.1、§3.6 | E-LD03、E-LD04、E-LD08 | C-LD08 | D-LD01、D-LD02 | G4、G5 | Must #1 | T-LD01 / M-LD01 |
| §2.6 分母对账 | §3.1 | E-LD01、E-LD02、E-LD12 | C-LD01 | D-LD01 | G2、**H2** | In | T-LD02 |
| §5.2 功效那一半 | §3.4 F1 | E-LD03 | C-LD03 | D-LD03 | G2 | In | T-LD02 |
| §8.2 #7 maker | §3.1（G-D） | E-LD10 | C-LD06 | D-LD02 | G7 | Should | T-LD03 |
| C-LD02 数据 | §3.1 | E-LD05 | C-LD02 | D-LD01 | G2 | In | T-LD04 |

**Gate Review：G7 = PASS**（分析型交付：9.2/9.3 N/A 有理由；9.4/9.5 每条有前置、操作、客观预期、证据位置、Owner、失败动作；两个人类确认点已列；P0/P1 Delivery Item 全链路可回溯）

---

## 10. Phase 10 · Learning Loop

**Prerequisites**：§3.5 的 M-LD01–03、§9.5 Acceptance、§6.2 Failure Modes。
**Produces**：Validation Plan、Experiment Design、分析校准行。
**Downstream** **[R 新增，K-LD14]**：M-LD01–04 的读数回流到 §3.5 与 §9.5；GAP-LD02 的枚举结果决定 O-LD4 是否进 §8.3 的 Could。

### 10.1 Validation Plan

> Metric ID 全部来自 §3.5，此处只补基线、阈值、窗口、停止条件、失败动作。**不在此新建指标。**

| M ID | 基线（2026-09-18） | 阈值 | 窗口 | 停止条件 | 失败动作 | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| **M-LD01** | 0/0 | 下 3 次研究动作里 ≥2 次写了「服务哪个目标」 | 到 2026-10-13 | 连续 3 次为 0 → 停止把它当机制 | 降级为 O-LD2（只留价钱表） | 操作者 |
| **M-LD02** | **+0.0181**（N=299，门 1.5738，翻转点 N=351，剩 52 笔） | 不下降（本文引出的动作不得使 N 增加） | 持续 | 余量 < 0 → tsmom 主书自身失去 family gate | 暂停一切 tsmom 桶的计费动作，先重出证据 | 操作者 |
| **M-LD03** | 0 | ≥1 | 到 2027-03-31 | 到期仍为 0 → 管线判定为不产出，重开 F1/F3 | 重跑本分析，框定换 F3 语法外支 | 操作者 |

### 10.2 Experiment Design

**GAP-LD02（唯一需要设计的实验）**：给 `mining/expr.py` 加阈值 / 指示算子后，**只跑枚举与去重、不打分、不写 ledger**，输出去重后的新表达式数与 `space_digest`。
- **样本**：现有 16 个族 × 新算子。
- **通过阈值**：去重后 ≤ 500 个新表达式 → 可对 O-LD4 定价；> 500 → 先缩族再枚举。
- **失败动作**：C-LD04 语法外支维持 UNKNOWN，O-LD4 不进 MoSCoW。
- **ledger 隔离要求**：枚举路径不经 `validate`，不调 `_record_trials`；跑前后核 `trials.jsonl` 行数仍为 22,179。

其余各项不设计实验：#4/#5/#6 已有各自的 10-13 预登记；#7–#11 在排序完成前不启动。

### 10.3 Post-Launch Review

**待回填**（本文执行后）：AC-LD01 的表态结果、AC-LD02 / AC-LD03 的裁定、M-LD01 前三次读数。

### 10.4 分析校准

追加到 `docs/analysis/analysis-calibration.md`（含**结构缺失**列）：

- **本次的读法错误**：一处——扫描脚本把 `open_time` 的 epoch 毫秒按纳秒解析，缺口误报为 **0**；按 `unit="ms"` 重算得 4,235，BNXUSDT 638 与另一份报告交叉核对一致。**教训**：一个「全 0」的完整性读数比一个有缺陷的读数更可疑，因为它同时也是「计算没跑起来」的表现。
- **结构缺失（skill 要求但未产出）**：本轮首次输出时**整套框架产物缺失**——无 Axiom Trace、无 Claim/Assumption/Risk Register、无 Gate Summary、无 Phase 记录头、无四类 Contract、无产物自检表、无 Checkpoint、Phase 7 未执行。原因是把「已有同题报告」当成了「可以只做核对」的理由，于是静默省略而非标 `COMPRESSED`。**这正是 V3.8 变更记录第一段描述的失败模式**，由操作者当场指出。
- **流程改动**：M/L 进入 Phase 2 前必须先读 `templates/ml-report.md`；「已有同题分析」不构成压缩框架产物的理由，只构成不复用其结论的理由。

---

**Gate Review：G7 复核 = PARTIAL** **[R 新增，K-LD14]**（指标可证伪性按 K-LD07 已修；但自检表 K-LD13 的 Phase 1 产物仍不在本文件内，见 §11）

---

## 11. 产物自检表

> 每行只能是 `PASS` / `COMPRESSED（理由）` / `N/A（证据）`。

| 产物 | 定义处 | 状态 |
| --- | --- | --- |
| Phase 级头部（Reading Check + Interaction 行） | SKILL 5 | **PASS**（本文头部） |
| 需求四问 / 来源与偏差 / 需求预设清单（六列）/ Early Kill | SKILL 5.4–5.5, B.2 | **N/A（证据：产出于上一轮对话，未并入本文件，审查者无法核）** **[R 修订，K-LD13；冻结稿误标 PASS]** |
| Context Intake（或「材料已覆盖」） | SKILL 3.3 | **PASS**（§2.6，三问已答） |
| Evidence Ledger 全字段，每条可复核位置 | A.4 | **PASS**（§2.1，E-LD01–14，各附命令或路径行号） |
| Claim Register 全字段；P0/P1 的 Axiom Trace 是回答不是标签 | SKILL 4, A.5 | **PASS**（§2.2，C-LD00–11，每条 Axiom Trace 写成对强制问题的回答） |
| G1–G7 每门有 ≥1 条追溯命题，或 N/A 带理由 | SKILL 4, 6 | **PASS**（§1：A1→C-LD00、A2→C-LD01/02/03、A3→C-LD09、A4→C-LD05/08、A5→C-LD10、A6→C-LD01/04、A7→C-LD06/11） |
| Assumption Register 全字段；高×高有实验并对齐 Gap Plan | A.5 | **PASS**（§2.3；无高×高，已写明排过象限） |
| Gap Plan | A.6 | **PASS**（§2.5） |
| Problem Statement / 5W2H / Causal Chain | B.4 | **PASS**（§3.1–3.3） |
| Problem Reframing ≥2 + `D-xxx` | B.4 | **PASS**（§3.4，F1/F2/F3 各配区分证据，D-LD01） |
| Success Definition 在 Option 之前，`M-xxx` 三类 | B.4 | **PASS**（§3.5，M-LD01–03，写在 §6 之前） |
| JTBD / Stakeholder（含受损方）/ Scenario / Journey / Edge Cases | B.4 | **PASS**（§3.6–3.10；Journey 标 COMPRESSED 并说明；Stakeholder 含两个受损方） |
| Strategic Fit / Relative Value（含四必答）/ Business Case / Cost of Delay | B.5 | **PASS**（§4.1–4.5；§4.4 N/A 有证据） |
| As-Is/To-Be / Constraint Map / Impact Radius / Engineering Pre-check | B.6 | **PASS**（§5.1–5.5） |
| Risk Register（Memo 引用的 RISK-xxx 在此定义） | A.5 | **PASS**（§5.6，RISK-LD01–04；Memo 引的 LD01/LD02 在此有定义） |
| Option Set / Failure Modes / Recommendation 全字段 / Decision Log | B.7 | **PASS**（§6.1–6.4） |
| 每个 Phase 的 Prerequisites / Produces / Gate / Downstream | B.1 | **PASS**（§2–§10；§10 的 Downstream 与 Gate Review 按 K-LD14 补齐） |
| Phase 7 全部产物；L 级由独立子代理执行 | D | **PASS**（§7.1–7.7，独立子代理，22 条 Kill；审查者自述隔离限制已如实记录） |
| MoSCoW / Scope Firewall / Decision Compression | B.8 | **PASS**（§8.3–8.5） |
| 四类 Contract（分析型交付按 §9.1） | C.2–C.5 | **PASS**（9.2/9.3 N/A 有理由；9.4/9.5 全字段） |
| Source Trace Matrix | C.7 | **PASS**（§9.6） |
| Validation Plan / Experiment Design / 分析校准行（含结构缺失列） | C.9 | **PASS**（§10.1–10.4；**[R 修订，K-LD15]** 冻结稿标 PASS 时 `analysis-calibration.md` **尚未追加**，本版已实际写入并核过 mtime） |
| Checkpoint 文件 `<项目>-checkpoint.md` | SKILL 11 | **PASS**（§12 + 同名文件） |
| 操作者裁定后的重数、方案与 Gate 复核 | §13（本轮新增） | **PASS**（§13.1–13.5；重数由 `tests/alpha/test_which_families_the_selection_gate_alone_is_holding_back.py` 钉住） |
| Final Decision 7 词之一 + 命中的 H-ID | SKILL 9 | **PASS**（§14） |

---

## 12. Checkpoint

见 `docs/analysis/2026-09-18-ledger-denominator-checkpoint.md`。要点：等级 L / Yellow；Phase 1–6、8–11 COMPLETE，Phase 7 见 §7；已冻结 D-LD01 / D-LD02 / D-LD03；G0–G7 见 §1；开放项 C-LD04 语法外支 UNKNOWN、C-LD07 等 10-13；GAP-LD01–03；待决 AC-LD01–03。

---

## 13. 操作者裁定后的重数与优化方案（2026-09-18）

**Prerequisites**：§7 的 K-LD02；操作者裁定。
**Produces**：重数表、K-LD02 关闭、优化方案 OPT-1…OPT-4。
**Downstream**：10-13 裁定包的排序。

### 13.1 裁定

> **问**：Falsifier 里的「过」指 `PASS`，还是 `registry.py:374` 收的上线口径（含 `WEAK_PASS`）？
> **操作者 2026-09-18 答：上线口径。**

这关闭 **K-LD02**，而且是往「Falsifier 满足」的方向关的：门降到 0.00 后能过的已判负族是 **2 个**，Falsifier 要求 ≥2。

### 13.2 重数（`VerdictThresholds(enforce_oos_selection=False)` 即「D-028 归零」）

九族取 2026-09-17 同管线 pit 重跑里各自最有利的一份报告。**门归零后**：

| 族 | 门归零后**剩下**的挂钩 | 判定 | 上线口径下 |
| --- | --- | --- | :---: |
| tsmom | D-043 unselected cap | WEAK_PASS | ✔（已在架主书） |
| **flow** | **无** | **WEAK_PASS** | **✔** |
| **residual** | **无**（D-043 cap 触发） | **WEAK_PASS** | **✔** |
| pairs | CPCV 0.33 > 0.1 | FAIL | ✘ |
| carry | Sharpe 0.46 < 0.5；CPCV 0.27 | FAIL | ✘ |
| chanlun | Sharpe 0.07 < 0.5；CPCV 0.33 | FAIL | ✘ |
| xsmom | Sharpe 0.14 < 0.5；CPCV 0.27；PBO 0.58 | FAIL | ✘ |
| meanrev | Sharpe −2.29；fold 0.00；CPCV 1.00；成本×2 −3.39 | FAIL | ✘ |
| breakout | Sharpe −4.30；fold 0.00；CPCV 1.00；成本×2 −8.47 | FAIL | ✘ |

**读法**：门是 **2/9** 的唯一死因，**不是** 6/9 的死因。那六个挂的是 0.5 Sharpe 地板、CPCV、PBO、fold 一致与成本 ×2——D-028 一条都不碰。

**这张表由测试钉住**：`tests/alpha/test_which_families_the_selection_gate_alone_is_holding_back.py`（5 条，吃 `reports/research/` 的真实归档）。它不复述结论，它每次重算。变异验证过：把常量改成一修稿写错的 `{"residual"}`，测试当场红并指出正确答案。

### 13.3 这解锁了什么——以及没解锁什么

**没解锁**：放宽 D-028。α 是操作者签的 0.05；而且六个族与它无关，放宽它对它们毫无作用。维持 **Won't**。

**解锁的是一件具体的事**：flow 与 residual 是**仅被多重检验门拦住**的两个候选，而它们在上线口径下都是 `WEAK_PASS`——**够格做 probe，不够格做主书**。当前 probe 占用：

| | 值 | 来源 |
| --- | --- | --- |
| `Policy.max_concurrent_probes` | **2** | `beidou_governance/policy.py:159` |
| 当前 probe 数 | **1**（flow） | `governance/governance_state.json` |
| **空名额** | **1** | — |

residual 今天的处置是**前向板**（PR #67，claimed 1.0484），要等 **3.48 年**。而一个 probe 名额给的是 **30 天**的实盘归因读数。两者不互斥，但按今天的裁定，residual 够格走后者。

### 13.4 优化方案

| ID | 项 | 价钱 | 何时 | 状态 |
| --- | --- | --- | --- | --- |
| **OPT-1** | 把重数固化为测试，答案每次重算而不是被复述 | 零 ledger、零 source budget（测试不计入） | **今天** | **已执行** |
| **OPT-2** | 裁定、重数表与它解锁的东西写进 `RESEARCH_LOG` 与本文 | 零 | **今天** | **已执行** |
| **OPT-3** | **residual 进第二个 probe 名额** | 加 sleeve **改构造指纹、清零 M-010** → **冻结期内不可做** | **10-13 裁定包第一候选** | 交操作者 |
| **OPT-4** | 六个非门死因的族：维持 REFUTED，不再以「门太严」为由重开 | 零 | 持续 | 本文即依据 |

**OPT-3 的价钱要写全**：它不是零成本。加一个 sleeve 会清零 M-010 的 30 天连续记录，而 M-010 正是 2026-10-13 那道门自己要的东西。所以它**只能**排在 10-13 之后，且要与 §8.2 的其余 11 项一起按四目标排序竞争那 12 笔名额。它的优势是它现在有了一条别人没有的依据：**操作者亲自裁定的口径说它够格。**

### 13.5 Gate 复核

**G6 = PASS**（两条 P0 全部关闭：K-LD01 以改写关闭，K-LD02 由操作者裁定关闭）。**G2 = PASS**（重数由测试而非叙述支撑）。**决策上限回到 PIVOT**（H1 / H3 / H7 全部解除，H2 成立）。

---

## 14. Final Decision

**PIVOT**，命中 **H2**。

**[2026-09-18 操作者裁定后更新]** 冻结稿与第一版修订稿分别判 PIVOT(H2) 与 Need Evidence(H1+H3+H7)。操作者当天裁定「过」= **上线口径**（含 `WEAK_PASS`），**K-LD02 由此关闭**，两条 P0 全部关闭，H1 与 H3 解除，G6 转 PASS 使 H7 解除。剩下的是 **H2**：P0 Claim C-LD01 判 **PARTIAL**——门对 **2/9** 的族是唯一死因，对 **6/9** 不是（§13 的重数表）。问题成立、方案要变，正是 PIVOT 的定义。

**[R 修订，K-LD01 / K-LD02]** 冻结稿写的是：~~PIVOT，命中 H2……「50 多个策略只活两个」这个读数自我否证——分母选得越大，多重检验校正越必要~~。Phase 7 推翻了它的两根支柱：分母论证无效（开门用的从来不是那个分母），Falsifier 的命中数未定（本仓库自己的上线口径下是 **2** 不是 1）。

**修订后的一句话**：五轮以来第一次，「判定条件太严」这个假设**没有被推翻**——它现在是 UNKNOWN，而关闭它只需要操作者的一句定义（「过」= PASS，还是 = `registry.py:374` 收的上线口径），零成本、当天可得。在那句话之前，本文不主张任何框定。**站得住的部分**：数据层 19 项复算逐项一致，回测数据没有问题；四个目标互相冲突且从未定价（C-LD08，PARTIAL）；§8.2 那张带价钱的待办清单不依赖 C-LD01，可以直接用。
