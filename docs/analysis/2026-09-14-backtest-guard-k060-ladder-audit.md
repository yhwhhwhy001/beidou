# backtest-guard · k=0.60 与 R8 阶梯的独立对抗审查（Phase 7）

审查触发：`scratchpad/k-ladder-frozen.md` 第 1 行请求「供 Phase 7 独立对抗审查」，且当日
`72034790` 把 registry 的 evidence 指针移到 `tsmom-validation-20260913T182325Z`，armed 启动闸
因此放行。审查者未读作者会话记录，只读代码、提交、冻结产物与 `.beidou/live/cycles.jsonl`。

受检对象：k=0.60 的决策链（P32 / P32b / P32c）+ 解闸证据（`72034790`，tree 为 `f4efa93a`）
扫描：12 个源文件、2 个提交、1 个冻结产物、295 个实盘周期
复现脚本：`scratchpad/audit_p32_ladder_bootstrap.py`

> **补记（同日，见文末「第二轮」）**：本文正文的致命项成立且已被受检方接受（`4a8cfd18`），
> 但它给 shipped 档位记的 14pp 尾部好处是在**盯市 ruler** 下量的。第二轮把 R8 真正读的那把
> 尺子量出来了，结论要一起改：正文的「建议优先级」已被文末那一份取代。

总评：🔴 致命 1 项 · 🟠 高危 3 项 · 🟡 中 2 项（含逻辑项 4 项）

判语：**工程链条比多数生产系统干净，但支撑「重标 R8」这个建议的那一个数，是用一把量不到
它自己的尺子量出来的——方向是反的。** k 的选择本身站得住；O-2 与 O-3 的排序不站得住。

---

## 第一遍 · 工程审查（均带 文件:行）

### [🔴 致命] P32c 的 q95 对「已经节流过的序列」做 bootstrap，没有在重抽路径上重放阶梯

位置：`scratchpad/p32c_rescaled_ladder.py:47-53`（`q95_of`），调用点 `:81`

问题：`q95_of(thr, ...)` 把 `thr`——节流标量在**历史顺序**上决定好的序列——切块重抽。阶梯是
路径依赖的：一条 q95 级别（−70%）的重抽路径会比历史路径（IS −39.8%）更早、更深地触发阶梯，
而 method A 把那次触发钉死在历史位置上。于是 shipped 臂的保护在它自己的 q95 里是缺席的。
docstring `:14-15` 已经指出「throttle 会改变它要控制的那条尾巴」，实现只做了一半。

复现与对照（`scratchpad/audit_p32_ladder_bootstrap.py`，block=168、draws=2000、seed 与 P32c 同）：

| universe | 臂 | A：现行方法 q95 / P(破50%) | B：路径内重放阶梯 q95 / P(破50%) |
| --- | --- | ---: | ---: |
| pit | shipped R8 | −70.8% / 57.5% | **−52.6% / 11.8%** |
| pit | rescaled R8 | −69.9% / 55.2% | −66.5% / 55.5% |
| static | shipped R8 | −69.9% / 55.8% | **−52.6% / 13.5%** |
| static | rescaled R8 | −69.9% / 55.8% | −66.7% / 55.9% |

method A 的四个数与 `scratchpad/p32c-{pit,static}.json` 逐位一致，所以差异只来自这一处方法。

后果，逐条对上冻结产物的断言：

- **C-006「采纳 k=0.60 必须同时重标 R8，否则付 16.3pp 换到更差的 q95」——方向反了。**
  在 B 下 shipped 的 q95 比 rescaled **好 14.0pp**（pit）/ **14.1pp**（static），
  P(破 −50%) 是 12% 对 55%。
- **O-2 不被 O-3 严格支配。** 真实的交换是：重标 R8 买到中位 21.3pp CAGR（q90 41.2pp，
  draws=1000 pit），代价是 P(破 −50%) 约 12% → 55%。两边都仍在已裁定的 −70% 预算内，
  所以 O-3 **可以**成立——但要以「用 43 个百分点的破位概率换 21pp 收益」的名义成立，
  不能以「q95 反而更差」的名义成立。
- **E-010 的「刀刃」（pit 触发付 16.3pp、static 不触发付 0）是 method A 的产物。** 在 B 下
  两个 universe 给出几乎相同的数（−52.6% 对 −52.6%），刀刃消失。
- 16.3pp 是单条历史路径的样本内数；B 的中位是 21.3pp，历史路径偏轻。

修复方向：`q95_of` 接受未节流的 `net` 与 policy，在每条 draw 上先 `replay_ladder` 再算 MDD。
遗留偏差两臂共担且已记：A-003 的周块 bootstrap 破坏多月 regime 结构（所有 q95 偏乐观），
以及 P32b 的近似（标量乘在已实现净值上而非重推权重路径）。

### [🟠 高危] `liquidation_touches` / `min_margin_buffer` 是恒等式，不是测量

位置：`beidou_alpha/backtest.py:368-370`

问题：`buffers[t] = (1 + realised - charge) / (gross * maintenance_margin_rate)`，而
`gross ≤ max_gross = 2.0`（`config/live.demo.yaml:173`）、`maintenance_margin_rate = 0.005`
（`backtest.py:236` 默认）。于是 buffer ≥ 100 × (1 + r − c)：**要触到 1.0，需要单根 1h bar
亏掉约 99%。** 全网格 `liquidation_touches = 0`、`min_margin_buffer ≥ 94.75` 不是关于爆仓
风险的证据，它是 `max_gross` 与 mmr 的算术。

※ 作者已披露结论本身（提交信息「限制的不是爆仓只是回撤」、C-004），**不降级**：问题不在
结论，在于 C-004 把一个恒等式列为 P1 SUPPORTED 的证据，并在 k 的每一档重复它，读起来像
「跨 k 复核过的安全裕度」。真正的爆仓通道是 RISK-2：权益 52.65% 是 BTC 抵押品，回测建模的
抵押品是零，且它随 k 同步放大——那条通道这套仪器看不见。

修复方向：要么把这两个字段标注为「结构上不可达，仅作不变式检查」，要么换成能动的量
（`min_liq_distance` 已在实盘 cycle 里）。

### [🟠 高危] CPCV 的 embargo 是 50，而特征回看是 720 / 1442

位置：`beidou_alpha/validation/cpcv.py:47-57`（自述），门在 `beidou_alpha/validation/verdict.py:96`

问题：`cpcv_splits` 的 docstring 自己写明：embargo 应按特征回看定（tsmom 的 `max(horizons)`
= 720，`AlphaModel.warmup_bars` = 1442），实际调用方传 `embargo = purge = 50`，于是每个
「after」组的训练集里留着约 670 根被污染的 bar，而 `cpcv_evaluate` 正是在这些 bar 上选参数。
`fraction_negative ≤ 0.10` 是 `decide` 的**硬门**，本次读数 0.0000。

※ 作者已披露——但只披露在代码里。报告 artefact（`tsmom-validation-...md` 的 CPCV 段）、
registry 注释与提交信息都直接引用 CPCV 数字，没有带这条注记。**披露与引用自相矛盾**：
一个被标注为 OPEN 的边界，其产物被当作通过条件使用。

修复方向：把这条注记随数字写进报告的 CPCV 段（一行），使引用它的人不必读 `cpcv.py`。

### [🟠 高危] 实盘 R8 的 ruler 与回放的 ruler 不同，而两条路现在都靠它定价

位置：`beidou_governance/policy.py:173`（硬编码 rungs）、`beidou_live/engine.py:1493`、E-015

问题：实盘 `attributed_drawdown_state` 只累计 income 行（已实现 + 资金费 + 手续费），
**不含未实现盈亏**；P32b/P32c 与本次审查的重放用的是逐 bar 盯市净值。作者把它记为
「限制，不是发现」，净效应未量化。

在本审查的结果出来之后，它从限制升级为**决定性**：shipped 臂全部的价值（q95 −52.6%、
P(破50%) 12%）来自阶梯在深回撤里真的触发。一个只看已实现 P&L 的 ruler，在动量书持有浮亏
时**天然晚触发或不触发**。若实盘 R8 的触发显著少于重放，则 shipped 臂的保护是账面上的，
而 rescaled 臂省下的 21pp 也是账面上的——两边都被这同一把尺子定错价。

修复方向：在决定 O-2/O-3 之前，用 `cycles.jsonl` 的 income 行重建一条 attributed 回撤序列，
与盯市序列并排，量出触发次数差。这比再跑一次 bootstrap 更有信息量。

### [🟡 中] PBO 在 2 格网格上被当成「变差了」的证据

位置：`beidou_alpha/validation/verdict.py:99-103`

问题：`decide` 自己写明 `grid_trials < 4` 时 CSCV「is a coin flip that only says the two trade
places across sub-periods. Reported, not enforced.」本次 `grid_trials = 2`。而
`72034790` 的提交信息与 registry 注释把 `PBO 0.0856 → 0.2082` 列在「这次加注的价钱」里。
不是错误，是把一个自己声明为无信息的数当成了代价。

修复方向：引用时标注 `(grid_trials=2, 不enforce)`，或不引用。

### [🟡 中] D-028 的门等价于一个 Šidák 校正的 t 检验，而它对 N 的斜率给得出一个可算的预算

位置：`beidou_alpha/validation/multiple_testing.py:246-305`

复核：`threshold_annual = z_Šidák(N) · √(bars_per_year / (n_obs−1)) · 矩修正`
≈ **z_Šidák(N) / √(OOS 年数)**。用报告自己的输入独立重算得 1.5512，报告值 1.5493（差 0.1%，
来自矩修正——在 1h 频率上 skew/kurt 修正只值约 0.5%，所以这道门实际上是 IID 正态 t 检验）。
`p_family` 同理：我算 0.0354，报告 0.0348。门本身没问题，它就是它声称的东西。

**可以直接用的那个数**：registry 注释写「在 k=0.60 下再对 tsmom 做任何搜索之前必须先算门」。
把余量按 N 折算——保持 OOS 1.5919 不变时，门在 **N = 351** 追上它：

| N | 242（今天） | 280 | 300 | 350 | 484 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 门 | 1.5493 | 1.5662 | 1.5741 | 1.5918 | 1.6282 |
| 余量 | +0.0426 | +0.0257 | +0.0178 | +0.0001 | −0.0363 |

即 **+0.0426 恰好值 109 次新试验**。RISK-5 计划申报的 38 格会吃掉其中约三分之一。
（前提：新试验不改变被选中的配置；若某个新格子胜出，OOS 本身也会动。）

### 已排除（命中可疑模式，读上下文后判为合法用法）

- `beidou_alpha/backtest.py:254` `decided.shift(1)` —— 决策在 t、成交在 t+1 的 open→close，因果正确
- `beidou_alpha/overlays/exits.py` 全文件 0 处 `high`/`low` —— 止盈止损不用 bar 内极值，无触达式乐观成交
- `beidou_alpha/validation/metrics.py:43` 年化用 `bars_per_year`（本次 8760，1h crypto 正确），
  `compound` 用 `prod(1+r)` 非 cumsum
- `beidou_alpha/validation/walk_forward.py:70` purge 正确；`oos_is_full_sample_tail` 是自己打出来的
  （DL-R4 已随证据一路标注）
- `beidou_cli/research_cmd.py` 的 `_membership` / `_resolve_symbols` 只读 parquet 与 universe，
  非 armed 跑法不改写共享文件
- 成本与滑点有建模且有压力（×2 → 1.5550；9.2bps → 1.5516）；`impact_costs` 的两个输入都 `.shift(1)`

### 偏差影响（方向判断，非收益预测）

上面没有一条是「回测看到了未来」。本次的偏差集中在**风险一侧**：
q95 与 P(破 −50%) 在 shipped 臂上被系统性地评差，`liquidation_touches` 给出虚假的安全裕度，
CPCV 的硬门跑在被污染的选择上。方向是：**真实的尾部比 O-3 的表格显示的更依赖那条阶梯**。

---

## 第二遍 · 逻辑对抗审查（非代码证据）

### [🔴 致命·逻辑] 现在在跑的就是 O-2，而唯一给 O-2 定过价的那张表方向是反的

维度：Ⅲ 失效场景 / Ⅴ 运维

依据：`.beidou/live/cycles.jsonl` 最后一行（2026-09-13T19:00:25Z）
`risk_ladder = {base_vol_target: 0.6, enforced: true, ruler: "attributed_pnl", scalar: 1.0}`，
`policy.py:173` 仍是 shipped rungs。295 个周期里在 k=0.60 上跑过的是 **1 个**。
冻结产物的关键成立条件写着「只改 k 不改 R8 即落入 O-2」——当前状态正是 O-2。

失效场景（可证伪）：若按 C-006 把 R8 重标为 `((−0.49, 0.45), (−0.70, 0.30))` 上线，则在
周块 bootstrap 下归因回撤破 −50% 的概率从约 12% 升到约 55%（两个 universe 同向，
method B）。若此后 90 天内出现一次 −50% 以上的归因回撤而 `risk_ladder.acting` 从未为真，
即为本条命中。

缓解：在重标之前先做 E-015 的 ruler 对照（见上）；若仍决定重标，把理由改写成
「用 P(破50%) 12%→55% 换中位 21pp CAGR，预算 −70% 容得下」，而不是「shipped 的 q95 更差」。

### [🟠 高危·逻辑] edge 是自述的行为性 edge，半衰期没有估计

维度：Ⅰ.4 结构性还是行为性

依据：`beidou_alpha/signals/tsmom.py:7-11` 自述对手盘是「late leveraged long」，
「Behavioural, not structural … should be expected to decay」。这是这个仓库里少见的、
真的回答了「谁在对手盘」的地方。但没有任何地方给出衰减半衰期的估计，而 k 刚翻倍——
加注的是一个自述会衰减且衰减速度未知的东西。

失效场景：若 edge 的半衰期短于 M-G06 的 547 记录日时钟（E-014，因构造改动已清零），
则治理时钟会在衰减被判定之前先跑完，判定依据是一段已经衰减过的样本。

缓解：给出半衰期的可证伪估计（例如按年分段的 OOS Sharpe 斜率），或把 M-010 的 30 天
归因窗口明确为唯一的衰减仲裁者并接受它的功效。

### [🟠 高危·逻辑] 容量曲线的系数是假设不是测量，而 k 翻倍把换手抬到 540

维度：Ⅱ 容量与规模

依据：`beidou_alpha/backtest.py:77-89` 自述 `ImpactModel.coefficient` 取自股票文献、
声明 E5，且「too small to SEPARATE from spread」；实盘只有 121 笔 demo 成交（54–762 USDT）。
`ParticipationModel` 在回测里**只测量不施加**（`:130-145`）。换手 415 → 540、平均毛敞口
0.849 → 1.345。

失效场景：若真实资金进场（09-08 裁定仍 Out of scope），冲击成本在 k=0.60 的换手下开始弯曲，
则本次 +0.0426 的门余量会被成本吃掉。**口径提醒**：报告的 cost stress 是**全样本** Sharpe
（1.6725 → x2 时 1.5550，降 0.1175 / −7.0%），门比的是 **OOS**（1.5919 对 1.5493），两者
不可直接相减。但同比例的退化施加到 OOS 上是 1.5919 → 约 1.48，**在门之下**——也就是说
成本翻倍这一件事，单独就比整个 +0.0426 的余量大一个量级。

缓解：把「成本 ×2 与 slip 9.2bps 下重算的 OOS 序列对门的余量」作为报告项随证据输出
（今天只有全样本 Sharpe，不能回答这个问题）；真实资金进场前重算门。

### [🟡 中·逻辑] 压力期覆盖缺 2020-03

维度：Ⅲ.3 历史压力期

依据：回测区间 2021-01-31 → 2026-09-13，含 2021-05 杠杆挤兑、2022-05 LUNA/UST、
2022-11 FTX、2024-08 日元套息平仓；**不含 2020-03 COVID 崩盘**。RISK-1 的 A-003 另外承认
周块 bootstrap 破坏多月 regime 结构、所有 q95 偏乐观。

缓解：不是要求补数据（1h 数据可能没有），是要求在 −70% 预算的裁定旁边写明这一点。

### [🔵 低·逻辑] Ⅴ 类运维：有实现，按第一遍规则给位置

- kill switch / 启动闸：`registry_evidence_problems`（本次正是它放的行）
- 熔断：`policy.py:173` R8 阶梯 + `guards.daily_loss_pause`（`config/live.demo.yaml:389`）
- 对账与心跳：`cycles.jsonl` 每周期 registry digest（KILL-Q15 的收口）
- 已知未修且正在修：R8 第一档的 scalar 被 `no_trade_rel_band 0.40` 吃掉
  （`config/live.demo.yaml:138-141` 自述；并行会话分支 `claude/nostalgic-carson-0411a0` 在修）

风险评级：🟠 **高**

---

## 上实盘加注前，必须先回答的三个问题

1. **实盘 R8 用 income 行做 ruler，在一次盯市 −40% 的回撤里会触发几次？** 这个数不出来，
   O-2 与 O-3 的价钱都是假的。
2. **重标 R8 的理由，改写成「P(破50%) 12%→55% 换中位 21pp CAGR」之后，还成立吗？**
   （预算 −70% 容得下，所以这是裁定不是计算。）
3. **接下来 38 格搜索之后，门会在 N≈280 到 1.5662；OOS 若不涨，余量只剩 +0.0257。跑之前
   算过了吗？** 余量的总额是 109 次试验。

## 建议优先级

先修 P32c 的 q95 方法（一处函数签名）→ 用 income 行重建 attributed 回撤序列做 ruler 对照
→ 把 CPCV embargo 注记写进报告 artefact → 再决定 O-2/O-3。
**在第 1 项修完并重跑之前，不建议把 R8 重标上线**：当前 O-2 状态在尾部一侧是更保守的一侧，
而把它换掉的依据是一个方向错误的数。k=0.60 本身不在本次审查的反对之列。

---

免责：本文是工程与逻辑的尽职调查，不构成投资建议，不预测收益。「失效场景」是条件式推演
（若 X 则 Y），不是概率预测。所有 bootstrap 数字继承 A-003 的偏乐观偏差与 P32b 的标量近似。

---

# 第二轮（同日）：把 R8 真正读的那把尺子量出来之后

正文的 🟠「实盘 R8 的 ruler 与回放的 ruler 不同」当时只能写成「方向已知、量级未量」。操作者
要求按建议顺序修复，第二项就是量它。量完之后，**正文的致命项仍然成立，但它的结论需要一起
限定——包括我自己给 shipped 档位记的那 14pp。**

脚本：`scratchpad/p32e_ruler_divergence.py`（两把尺子的分歧）、
`scratchpad/p32d_ladder_bootstrap_pathwise.py`（档位 × ruler 四臂，draw 内重放，取代
`p32c.q95_of`）。

## 两把尺子，代码里的出处

`beidou_live/risk_budget.py:223` `attributed_drawdown_state`：
`path = base + cumsum(attributed)`，`attributed` 只累计 income 行（已实现 P&L、资金费、手续费），
**不含未实现盈亏**；`base` 是账户**总权益**（今天 52.65% 是 BTC 抵押品）；path 是线性的，不复利。
P32b/P32c 以及正文第一轮的重放读的是书的**盯市净值**——每一根不利 bar 当场计入。

一本持有浮亏的动量书，两把尺子必然分叉。分叉多少（k=0.60，2021-01→2026-09）：

| | pit | static |
| --- | ---: | ---: |
| 盯市最深回撤 | −39.85% | −35.29% |
| **已实现 ruler 最深回撤** | **−21.27%** | **−24.13%** |
| 越过第一档（−35%）的 bar：盯市 / **已实现** | 785 / **0** | 1 / **0** |
| 盯市首次触档那一刻两把尺子的读数 | −35.1% / −16.2% | −35.3% / −21.8% |
| 阶梯触发的 bar 占比：盯市 / **已实现** | 7.52% / **0%** | 0% / **0%** |

**五年七个月，R8 按它真正读的尺子一次都没触发过。** 实盘记录同向：108 个同时带两个读数的
周期里权益尺子更深的 105 个，比值中位 4.82×。

用 base = 书自身权益（1.0）是**偏向触发**的假设：实盘的 base 是更大的账户总权益，同样的已实现
亏损占它的比例更小，所以实盘只会比这里更晚触发。偏差方向与结论一致。

## 四臂（2000 draws，draw 内重放，报告的回撤始终是盯市回撤）

| 档位 | ruler | q95 pit/static | P(破50%) | 中位 CAGR | 中位代价 |
| --- | --- | ---: | ---: | ---: | ---: |
| shipped | 盯市 | −52.6% / −52.6% | 11.8% / 13.5% | 107.8% / 100.4% | 20.5pp / 20.0pp |
| **shipped** | **已实现（真实）** | **−69.3% / −69.3%** | **54.1% / 53.9%** | 127.6% / 120.6% | **0.0pp** |
| rescaled | 盯市 | −66.5% / −66.7% | 55.5% / 55.9% | 128.5% / 121.1% | 0.4pp |
| **rescaled** | **已实现（真实）** | **−69.9% / −69.9%** | **55.0% / 55.7%** | 129.6% / 122.0% | **0.0pp** |

## 结论，包括对我自己第一轮的更正

1. **O-2 与 O-3 在实盘里几乎是同一个决定。** 真实 ruler 下两者 q95 差 0.6pp、P(破50%) 差约
   1pp、CAGR 代价都是 0.0pp。E-009 的 16.3pp、我第一轮算的 21.3pp、以及我给 shipped 记的
   14pp 尾部好处，**全部只存在于盯市 ruler 下**。
2. **我第一轮的建议「在 q95 方法修完之前不要把 R8 重标上线」现在要改。** 那句话的前提是重标
   会拆掉一个有效的刹车。按真实 ruler 它不是有效的刹车，所以重标与否本身不是一个高风险动作
   ——**但也不再是一个能买到 21pp 的动作**。两边都别按原来的理由做。
3. **真正的发现换了一个**：k=0.60 下这个账户基本没有回撤刹车，两个选项都一样。q95 −69.3% /
   −69.9% 贴着已裁定的 −70% 预算，余量 0.1–0.7pp，而 A-003 说这些 q95 **偏乐观**（周块
   bootstrap 破坏多月 regime 结构，真实熊市更长）。**预算是靠四舍五入达成的，不是靠控制。**
4. **要修的是 ruler，不是档位。** 在 ruler 对齐之前改 `policy.py:173` 的数字，改的是一个够不
   着的东西。三条候选，未定价，是操作者的裁定不是我的计算：
   - 让 R8 读含未实现盈亏的书级净值（与回测同口径，但 KILL-AR-05 的理由要重新裁——它当初正是
     为了把抵押品噪声排除在外才选的已实现口径）；
   - 或把档位按已实现口径重标（本次数据上 −35% 的已实现约等于盯市 −70%，即档位要压到约 −17%
     才等价）；
   - 或承认 R8 是事后记录而不是刹车，另建一个真的刹车（`daily_loss_pause` 已经是盯市口径的，
     它在 k=0.60 下从 51 根涨到 575 根，是现成的落点）。

## 修复后的建议顺序（取代正文那一份）

1. ✅ 已做：`q95_of` 的 draw 内重放 → `p32d`；`p32c` 数字原样保留并标注取代关系。
2. ✅ 已做：两把尺子的分歧已量化 → `p32e`，本节。
3. ✅ 已做：CPCV embargo / margin buffer 恒等式 / PBO 两格无信息，三条注记随数字进 artefact。
4. **未做，操作者裁定**：R8 的 ruler 对齐（上面三条候选）。这一条之前，O-2/O-3 不必急着选
   ——它们在实盘里差 0.6pp。
5. **未做，报告项**：成本 ×2 与 slip 9.2bps 下重算 **OOS 序列**对 D-028 门的余量。今天报告只有
   全样本 Sharpe（1.6725 → 1.5550），不能回答「成本翻倍后还过不过门」。

## 本轮自身的限制

`p32e` / `p32d` 继承 P32b 的近似（标量乘在已实现净值上而非重推权重路径）与 A-003（周块
bootstrap 偏乐观）；已实现/未实现的拆分本身路径依赖，所以重抽 block 时每个 block 带着它在
历史上的拆分。已实现口径用「减仓按比例结转」的均价约定，脚本断言了拆分守恒
（`cumsum(已实现) + 未实现 == cumsum(盯市)`）。`base` 取 1.0 的偏差方向已在上面说明。
