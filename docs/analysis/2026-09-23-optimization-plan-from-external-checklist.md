# 北斗优化方案：外部清单缺口的第一批落地

2026-09-23。依据 `docs/analysis/2026-09-23-external-prompt-checklist-vs-beidou.md`（下称「清点」）。
行号以 `e80b1335` 为准。

## 这份方案接的是什么

清点列出 12 个缺口（G1–G12）和 5 条「说法与行为不符」。清点之后同日合入的 PR 已经关掉其中一部分。
这份方案把剩下的分成三类：

- 冻结期内现在就能做的，本批并行落地；
- 要操作者先裁的，给出选项与价钱；
- 要等冻结结束的，写明前置条件。

## 现状

| 项 | 状态 | 出处 |
| --- | --- | --- |
| G2：family gate 失败的后果 | 已做 | #102 |
| G5：分 regime 的夏普进 validate | 已做 | #104 |
| G7：组合层「打乱未来」因果测试 | 已做；79 个 cutoff 的全扫待定价 | #105、#107、#108 |
| 「说法与行为不符」第 1、2 行 | 已做 | #102 |
| 「说法与行为不符」第 4 行：研究侧基准 | 已做：backtest 与 decompose 改取当期成员 | #113 |
| G1、G3、G4、G6、G8、G9 | 本批 | 下文「第一批」 |
| 「说法与行为不符」第 5 行：BNX 是退市还是重新计价 | 本批，并入 G6 | 同上 |
| G10、「说法与行为不符」第 3 行 | 要先裁 | 下文「要先裁的」 |
| G11、G12 | 冻结结束后 | 下文「冻结结束后」 |

## 约束

- **构造冻结到 10-13**（`tests/live/test_the_construction_is_frozen_until_the_holdout_matures.py:59`）。
  本批不改构造，不改 registry 的解析值。每个包都要过两个构造测试。
- **不花 ledger。** 本批不跑 `research validate`。`reports/research/trials.jsonl` 不进任何提交。
- **实盘只读。** 开发与验证都用 `.beidou/live` 的副本。本批不重启循环。
- **合入不等于生效。** 日报侧的改动（G1、G3、G4、G9）要等主 checkout 更新后，每小时巡检才读得到。
  G6 在循环里，要等下一次按纪律重启。这两步由操作者定。
- **行数预算几乎用完。** `beidou_live` 余 20 行，`beidou_cli` 与 `beidou_data` 为 0。加了行的包，
  在同一个提交里抬顶并写理由。
- **避开并行会话。** 多书 band 那个会话在改 `beidou_alpha/portfolio.py` 与 `research_book_*`。
  #110 在改 `docs/ARCHITECTURE.md`、`governance/reopen.yaml`、`beidou_live/rebalancer.py`、
  `beidou_live/config.py` 与 `beidou_alpha/validation/forward_board.py`。本批不碰这些文件。

## 第一批：六个工作包

| 包 | 做什么 | 验收 | 何时生效 |
| --- | --- | --- | --- |
| G1 | `decay_watch` 进 `daily_payload`。两个 30 天窗口都低于 q10 才告警，INSUFFICIENT_DATA 只显示 | 真实状态副本上告警集合不变；造出两窗都低于 q10 的读数时告警；读数与周报一致 | 主 checkout 更新后 |
| G3 | 把 `report beta` 的计算抽成共用函数。日报加一节 beta 与残差，不告警 | 日报与 `report beta` 逐位一致；重构前后 `report beta` 逐位一致；量出巡检耗时增量 | 同上 |
| G4 | k=0.60 下重跑五段崩盘窗口，保留 k=0.30 一列对照。取全历史日收益的 95%、99% 经验 VaR 与 ES，带出处写成常量。日报在 σ 尺子旁印出 | 对照表能按 RESEARCH_LOG 里的命令复现；日报读数来自真实状态副本；不计 ledger | 同上 |
| G6 | 循环拿到闭合 bar 后，查 OHLC 矛盾、冻结 bar 与异常跳变。只记录、只告警。顺带核实 BNX | 在全部归档上量假阳性率；BNX 那次必须抓到；检查永不抛错；构造测试绿 | 下一次按纪律重启 |
| G8 | validate 报告给出保本成本倍数。只报告，`verdict.decide` 不读 | 在该倍数处重跑，净收益均值为 0；遍历归档报告注入敌意值，判定不变 | 下一次 validate |
| G9 | M-Q08 补上换手仪器：实盘换手对回测同期，印出 ±25% 的带。滑点按周分桶，带标准误 | M-Q08 四项都有读数；实盘与回测的换手口径一致有测试；最小样本规则写明 | 主 checkout 更新后 |

六个包都不动构造，不花 ledger。每个包独立成一个 PR，PR 描述写验收读数。

## 执行方式

- 六个子代理并行开发，各用一个独立 worktree。过完四道门后在本地提交，不推送。
- 主会话逐个复核：读 diff，重跑关键读数，再推送、开 PR、打开 CI 监控。
- 按完成先后合入。冲突预计集中在两处：source budget 表，以及 `beidou_live/reports.py` 的
  `daily_payload`、`daily_markdown` 与 `daily_alerts`。后合的 PR 先并入 main，解决冲突，重跑四道门再推。
- 全部合入后，在本文件末尾补「执行记录」。

## 要先裁的

| 项 | 选项 | 价钱 | 建议 |
| --- | --- | --- | --- |
| 「说法与行为不符」第 3 行：M-010 的口径 | A：只改文字，写明两边口径不同。B：实盘改按盯市口径算归因夏普。C：回测侧也给出已实现口径的预期 | A 是文字，S。B 是代码 M，要按 sleeve 拆盯市盈亏。C 是代码 M–L，要改证据 | 10-13 的裁定会读 M-010，先定口径再读它 |
| G10：时点成员表定时重建 | A：排一个定时任务重建。B：维持手动，加一条「成员表多久没重建」的告警 | A 是一个 plist。但重建之后证据不再描述磁盘上的数据，启动门会挡（`config/alpha_registry.yaml:330`），每次重建都要跟着换证据。B 是代码 S，不动启动门 | B |
| G7 余项：扫全部 79 个 cutoff | 做，或不做 | 补上漏网的 3 处前视。这个测试文件从约 1 秒变成约 40 秒 | 看全套耗时的余量再定 |

M-010 的口径差有多大，以 flow 为例：已实现口径的 30 天 σ 是权益的 0.137%，盯市口径是 3.239%
（`beidou_live/probe.py:128`）。M-010 拿已实现口径的实盘夏普去比回测给的预期，而回测是盯市的。

## 冻结结束后

- **G11：净敞口或 beta 上限。** 这是构造变更，要走 validate，要花 ledger。先看 G3 上线后的 beta 读数，
  再定要不要上限、上限多少。
- **G12：size 与低波的横截面信号。** 要花 ledger。先跑 `research power`，看这两个方向在现有桶里的功效。

## 10-13 当天要面对的两件事

这两件不在本方案的执行范围里。列在这里，是因为日期相同。

- **D-041 放行到期。** `deploy/run_live.sh:47` 写死 `BRIDGE_UNTIL="2026-10-13"`。到期之后，`--armed`
  启动要过证据门，而 tsmom 的现行证据是 FAIL（`config/alpha_registry.yaml:351`）。按现状，那之后的
  任何一次重启都会被挡住。
- **k 的重裁。** #112 按实盘的 base 重跑了 k 扫描：k=0.30 守不住 70% 的可动用预算，第一个守得住的是 0.25。

## 执行记录（同日）

第一批七个包全部合入 main：原定的六个，加上操作者裁定后补进来的 G10。每个 PR 的描述写了验收读数。

| 包 | PR | 结果 | 何时生效 |
| --- | --- | --- | --- |
| G1 | #118 | 衰减规则进每小时巡检，只在 REVIEW 时告警。窗口改从当前构造起算，与 M-010 同一起点。M-010 的口径按裁定 A 写明 | 主 checkout 更新后 |
| G3 | #119 | `report beta` 的计算抽成 `benchmark.beta_reading`。日报与 `report beta` 共用它，两边逐位一致 | 同上 |
| G10 | #120 | `beidou data pool lag --check` 接进巡检。告警线 14 天，告警文字写明重建会挡住 armed 启动 | 同上 |
| G6 | #121 | bar sanity：查 OHLC 矛盾、frozen bar 与跳变，只记录、只告警。BNX 在 2023-02 是同名重新计价，2025-03 才退市 | 下一次按纪律重启 |
| G8 | #122 | validate 报告带保本成本倍数 m*。出厂书全样本 15.46，样本外 14.48。m* 不是门的余量 | 下一次 validate |
| G4 | #123 | k=0.60 下重跑崩盘窗口，保留 k=0.30 一列对照。回测一日 VaR 与 ES 写成常量，日报印在 σ 尺子旁 | 主 checkout 更新后 |
| G9 | #124 | M-Q08 的换手第一次有仪器，滑点按周读出趋势。换手读 2.07 ± 0.50，只有 4 个完整日，未判定 | 同上 |

七个包都不动构造，不花 ledger。每个包由子代理在独立 worktree 里开发，主会话复核后推送。按完成先后合入。
冲突集中在 source budget 表与 `beidou_live/reports.py`，都在推送前并入 main 解决，并重跑四道门。
`beidou_live` 的顶从 10_680 到了 11_903，每一格的理由写在常量旁。

### 操作者裁定（同日）

- **G10**：维持手动重建，另加「成员表多久没重建」的告警，落在 #120。
- **M-010 的口径**：选 A。比较保留，写明实盘是已实现口径、回测是盯市口径，不改计算，落在 #118。
- **G7 的全扫**：不做。#105 植入的 14 处前视漏了 3 处，记在 #105 的描述里。

### 还没定的

- **G10 的重报频率。** 现在超线后每小时重报。按现有的表，第一次告警在 10-01。若重建等到 10-13，
  最多约 288 条。可以改成这一条按天去重，约 5 行加 1 条测试。
- **G6 的真实行情跳变。** 有成交衔接的跳变现在也发告警，文字注明「像真实行情」。可以降为提示。
- **G9 的两处口径。** 判定滑点按哪本书分；14 个完整日的最小样本。

**后续（同日）**：操作者定了前两件。G10 改为每天推送一次，G6 有成交衔接的跳变降为提示。落地与测试见
`docs/RESEARCH_LOG.md` 同日「操作者两条裁定：G10 每天推送一次；G6 真实行情跳变降为提示」一节。G9 的两处口径仍待定。

**后续（同日，再）**：G9 的两处口径，操作者答「按建议」。判定滑点改为按每笔成交下单前后的周期分书，14 天的最小样本
不变。读数与测试见 `docs/RESEARCH_LOG.md` 同日「G9 两处口径：判定滑点逐笔按下单前后的周期分书；14 天最小样本不变」一节。
第一批再没有待定的项。

### 生效还差两步

- 写这一节时，主 checkout 落后 main 61 个提交。日报侧的五个包（G1、G3、G4、G9、G10）要等它更新之后，
  每小时巡检才读得到。
- G6 在实盘循环里，要等下一次按纪律重启。

### 顺带发现，未修

- `data sync` 不再同步已离开候选池、但循环仍持有的名字。CYSUSDT 的归档停在 09-04，TUTUSDT 停在 09-03，
  LSKUSDT 停在 09-18（G3、G9）。
- `pool history` 用普通的 `to_parquet` 原地写成员表，不是原子写。它的 `--refresh` 默认仍是 `MS`，
  重建时也不提示会挡住 armed 启动（G10）。
- `beidou_data/store.py` 说 PUMPUSDT 的第一根 bar 在 2025-07-10，归档里 2025-04-12 就有（G6）。
- `tests/alpha/test_the_other_caliber_is_reported_not_applied.py` 用非递归 glob，扫不到子目录里的报告（G8）。
- M-Q08 的实盘换手偏高，来自 6 次止盈。它们的入场锚都是 09-13T20:00Z 平仓后重建的那一根（G9）。

## 查缺补漏（同日）

对照本方案逐项核了一遍：每个包的验收条件、「生效还差两步」、执行记录，以及清点里没进方案的项。
各包 PR 描述里记下、却没进执行记录的顺带发现，也一并核了。行号以 `cc672d24` 为准。

### 验收条件：都满足

| 包 | 方案写的验收 | 核到的证据 |
| --- | --- | --- |
| G1 | 真实状态副本上告警集合不变；两窗都低于 q10 时告警；读数与周报一致 | #118 的描述；`tests/live/test_the_decay_rule_reaches_the_hourly_check.py` 各有对应的测试 |
| G3 | 日报与 `report beta` 逐位一致；重构前后逐位一致；量出巡检耗时增量 | #119：21 个浮点逐位相同；md 与 json 逐字节相同；配对差中位 +38 ms |
| G4 | 对照表按 RESEARCH_LOG 的命令复现；读数来自状态副本；不计 ledger | #123：重跑 46 秒，逐格一致；`trials.jsonl` 哈希不变 |
| G6 | 全部归档上量假阳性率；抓到 BNX；永不抛错；构造测试绿 | #121：878 个文件、15,127,202 根 bar；BNX 那次 ln −4.01；两层兜底；15 passed |
| G8 | m* 处重跑净收益均值为 0；归档报告注入敌意值，判定不变 | #122：独立重跑为 0；81 份报告注入 ±99，判定一字不变 |
| G9 | M-Q08 四项都有读数；换手口径一致有测试；最小样本规则写明 | #124；两处口径已由 #127 按建议落地 |
| G10 | 裁定 B：维持手动，加告警 | #120；每天推送一次，#126 |

清点里「说法与行为不符」五行、「陈述过期」二十五行，在 `cc672d24` 上逐条重核过，都有了去处：
改掉的，或写明不改的（#102、#103、#110、#113、#118、#121）。写明不改的只有一行：`config/costs.yaml:16`
那句带日期、同文件 `:20` 已就地更正，审计文档里那句按历史记录保留。例外一处：「说法与行为不符」第 5 行点名的
`docs/RESEARCH_LOG.md:565`，#121 只改了 `store.py`，这一处到#129 才补上更正标记（见下表）。

### 生效

- 主 checkout 快进了三次（reflog，+0800）：09-23 21:31 到 `4ebe0f10`，22:05 到 `9c2f5059`，09-24 00:14
  到 `cc672d24`（#127）。14:10Z 起，每小时巡检读得到 G1、G3、G4、G9、G10。15:10Z 那次写出的日报里，五节都在。
- #128 与#129 要等主 checkout 再快进一次，才进每小时巡检与 01:20 的日任务。
- G6 仍要等实盘循环按纪律重启。在那之前，日报「Bar sanity (G6, alert only)」一节读 `cycles_checked 0`，
  并写明循环还在跑加检查之前的代码。

### 漏掉的，已补

| 项 | 出处 | 补在 |
| --- | --- | --- |
| `data sync` 只按 24h 成交额取前 2N，掉出去的池内名字不再同步。LSKUSDT 在池里，K 线停在 09-18 | 执行记录「顺带发现」第 1 条 | #128：候选并入 `universe.json` 的池子与刚离池的名字 |
| `pool history` 原地写、默认 `MS`、不提示会挡 armed 启动 | 同上第 2 条 | #128 |
| `store.py` 的 PUMPUSDT 说法 | 同上第 3 条 | #128。改写时读了归档：同一个名字下有两段序列 |
| 非递归 glob，漏读子目录里的 2 份报告 | 同上第 4 条 | #129。改成递归后 81 份都过 |
| RUNBOOK 没写 bar sanity 告警怎么处置 | #121 的描述写着「未做」，执行记录漏列 | #129 |
| RUNBOOK 没写衰减规则判 REVIEW 时怎么处置 | G1 给规则接了读者，没写处置 | #129 |
| `_priced` 上方的旧注释说乘数只乘 `turnover_bps`，其实也乘 carry | #122 的顺带发现，执行记录漏列 | #129，等长改写 |
| 两处原文仍写「币安不发布历史深度」 | 清点「一处事实更正」，方案没接 | #129：RESEARCH_LOG 与 09-18 的分析各加一处更正标记，原文不动 |
| RESEARCH_LOG 09-04 的缺口表仍把 BNX 写成退市，把 PUMP 写成单纯的上市边界 | 清点「说法与行为不符」第 5 行 | #129：表后加一处更正标记，原文不动 |

### 漏掉的，不补

- **重放没有缓存**（#124 的顺带发现）。`report daily --check` 从 0.73 秒涨到 1.40 秒。它每小时跑一次，
  不值得为此加一层缓存。
- **M-Q08 的实盘换手偏高来自 6 次止盈**（执行记录第 5 条）。这是读数的解释，不是缺陷。

### 要操作者定的

- **G6 进实盘。** 按纪律重启一次实盘循环：整点后 5 分钟到下一个整点前 10 分钟之间，先跑两个构造测试，
  重启后记 RESEARCH_LOG。代价是一次重启可能吃掉一根 bar 的退出检查，09-15 量到的命中概率约 1.16%。
- **CYSUSDT、TUTUSDT 的历史缺口。** #128 只管以后。要补 09-03、09-04 到 09-16 那一段，得手动跑一次
  `beidou data sync --symbols CYSUSDT,TUTUSDT`。它写共享的 `.beidou/data`，走实盘循环那条代理，
  要按整点避让挑时间。补上之后，`report beta` 因缺价跳过的 236 个 symbol-bar 应当消失。
- **主 checkout 再快进一次**，把 #128 与#129 带进每小时巡检与日任务。

### 10-13 的清单补一条

`config/alpha_registry.yaml` 里 09-08 审计那段注释仍引 k=0.30 的崩盘窗口读数（FTX −3.3%）。
#123 在冻结期内有意不改，现行读数只在 RESEARCH_LOG 的 G4 一节。冻结结束、k 重裁之后一起更新。
