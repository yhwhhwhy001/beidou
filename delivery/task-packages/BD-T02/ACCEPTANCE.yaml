task_id: BD-T02
status_enum:
- PASS
- CONDITIONAL_PASS
- FAIL
- NOT_VERIFIABLE
blocking_priority: P0
dependencies:
- BD-T00
criteria:
- id: AC-BD-T02-01
  statement: 核心模块不直接 open config/env.testnet.yaml。
  mandatory: true
  evidence_required: true
- id: AC-BD-T02-02
  statement: UNKNOWN/缺失环境启动为 SAFETY_ONLY 且写交易能力为 false。
  mandatory: true
  evidence_required: true
- id: AC-BD-T02-03
  statement: 同一配置生成稳定 checksum；篡改后 checksum 或签名校验失败。
  mandatory: true
  evidence_required: true
- id: AC-BD-T02-04
  statement: 配置中不存在明文 API key/secret/password。
  mandatory: true
  evidence_required: true
commands:
- pytest tests/config -q
- pytest -q -k 'config and (unknown or missing or checksum or safety_only)'
- python scripts/migrate_config_v2.py --check config/env.template.yaml
failure_rule: 任一 mandatory criterion FAIL 或缺少关键证据时任务不得 PASS；存在 P0 时必须 FAIL。
