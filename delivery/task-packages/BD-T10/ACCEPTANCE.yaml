task_id: BD-T10
status_enum:
- PASS
- CONDITIONAL_PASS
- FAIL
- NOT_VERIFIABLE
blocking_priority: P0
dependencies:
- BD-T09
criteria:
- id: AC-BD-T10-01
  statement: 重复 fill 不重复更新仓位/PnL。
  mandatory: true
  evidence_required: true
- id: AC-BD-T10-02
  statement: 任意 fill 序列 replay 结果确定性一致。
  mandatory: true
  evidence_required: true
- id: AC-BD-T10-03
  statement: reduce-only 永不增加绝对敞口。
  mandatory: true
  evidence_required: true
- id: AC-BD-T10-04
  statement: 数据库重建仓位与独立 Testnet 快照一致或明确 MISMATCHED。
  mandatory: true
  evidence_required: true
commands:
- pytest tests/position -q
- pytest -q -k 'position and (partial or reverse or reduce_only or hedge or replay)'
failure_rule: 任一 mandatory criterion FAIL 或缺少关键证据时任务不得 PASS；存在 P0 时必须 FAIL。
