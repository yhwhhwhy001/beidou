task_id: BD-T03
status_enum:
- PASS
- CONDITIONAL_PASS
- FAIL
- NOT_VERIFIABLE
blocking_priority: P0
dependencies:
- BD-T01
- BD-T02
criteria:
- id: AC-BD-T03-01
  statement: Adapter 外无 Binance 网络库、URL 和原始端点字符串。
  mandatory: true
  evidence_required: true
- id: AC-BD-T03-02
  statement: 账户查询失败返回 UNKNOWN/ERROR，绝不返回 EMPTY。
  mandatory: true
  evidence_required: true
- id: AC-BD-T03-03
  statement: create/cancel/query 使用真实传输层，且 Testnet contract test 可切换执行。
  mandatory: true
  evidence_required: true
- id: AC-BD-T03-04
  statement: 同一 correlation_id 贯穿 adapter request/response/log/metric。
  mandatory: true
  evidence_required: true
commands:
- pytest tests/contracts/exchange tests/integration/exchange -q
- python scripts/check_forbidden_patterns.py --repo .
- pytest -q -k 'exchange and (error_taxonomy or rate_limit or clock or capability)'
failure_rule: 任一 mandatory criterion FAIL 或缺少关键证据时任务不得 PASS；存在 P0 时必须 FAIL。
