task_id: BD-T01
status_enum:
- PASS
- CONDITIONAL_PASS
- FAIL
- NOT_VERIFIABLE
blocking_priority: P0
dependencies:
- BD-T00
criteria:
- id: AC-BD-T01-01
  statement: 源码及模板中不存在默认签名密钥。
  mandatory: true
  evidence_required: true
- id: AC-BD-T01-02
  statement: 缺少密钥时 Testnet 风险增加请求确定性拒绝，错误码为 SIGNING_UNAVAILABLE。
  mandatory: true
  evidence_required: true
- id: AC-BD-T01-03
  statement: 无签名、过期、篡改、重放、错误 policy version 均不可通过。
  mandatory: true
  evidence_required: true
- id: AC-BD-T01-04
  statement: Paper 决策不具备交易写能力且无法被 Exchange Executor 接受。
  mandatory: true
  evidence_required: true
commands:
- rg -n 'default-key|testnet-default|beidou-default' beidou_* apps config
- pytest tests/unit/risk tests/security -q
- pytest -q -k 'approval and (tamper or replay or expire or missing_key)'
failure_rule: 任一 mandatory criterion FAIL 或缺少关键证据时任务不得 PASS；存在 P0 时必须 FAIL。
