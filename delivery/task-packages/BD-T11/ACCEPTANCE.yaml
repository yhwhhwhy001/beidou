task_id: BD-T11
status_enum:
- PASS
- CONDITIONAL_PASS
- FAIL
- NOT_VERIFIABLE
blocking_priority: P0
dependencies:
- BD-T03
- BD-T10
criteria:
- id: AC-BD-T11-01
  statement: 每个风险仓位在 SLO 内有 ACKED 原生保护证书。
  mandatory: true
  evidence_required: true
- id: AC-BD-T11-02
  statement: 保护缺失/拒绝/失联自动进入 EXIT_ONLY 或 LOCK。
  mandatory: true
  evidence_required: true
- id: AC-BD-T11-03
  statement: 重启后不会重复创建保护单。
  mandatory: true
  evidence_required: true
- id: AC-BD-T11-04
  statement: 不存在固定 5% 或 2x ATR 等不可追溯业务默认值。
  mandatory: true
  evidence_required: true
commands:
- pytest tests/protection -q
- pytest -q -k 'protection and (native or reject or restart or duplicate or quantity or slo)'
failure_rule: 任一 mandatory criterion FAIL 或缺少关键证据时任务不得 PASS；存在 P0 时必须 FAIL。
