# Agent prompt — data lineage implementer

Start after resume acceptance. Inventory the actual data interfaces and clocks; do not assume filenames imply PIT correctness.

Implement immutable content-addressed lineage and a sealed OOS access protocol bound into ExperimentRun and checkpoints. First make adversarial revision, universe, timestamp, leakage, and seal fixtures fail for the intended reason.

Never fill missing provenance with defaults, silently use latest data, open OOS to debug candidate choice, or call synthetic fixtures economic evidence. Any unresolved lineage is `NOT_VERIFIABLE`. Deliver access logs and independent manifest recomputation to the data-integrity reviewer.
