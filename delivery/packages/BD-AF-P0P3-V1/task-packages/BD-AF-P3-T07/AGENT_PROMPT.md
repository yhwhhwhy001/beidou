# Agent prompt — validation semantics implementer

Do not start without the named human Metric Owner and frozen policy digest. If any formula, family, threshold, uncertainty rule, or unit is ambiguous, return HOLD.

Build adversarial fixtures and an independent recomputation path first. Implement canonical WFO/CPCV/PBO/DSR/FDR/cost/capacity/stability/sample semantics without trusting stored PASS fields. Bind every output to lineage, OOS seal, candidate family, and policy digest.

Do not tune thresholds to fixtures, exclude failed candidates, ignore NaN/Inf, substitute mocks for evidence, or claim economic value. Preserve raw/recomputed/mutation outputs for an independent quantitative validator.
