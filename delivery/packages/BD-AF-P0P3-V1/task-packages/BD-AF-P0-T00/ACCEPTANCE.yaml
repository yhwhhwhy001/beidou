{
  "$schema": "../../schemas/acceptance.schema.json",
  "schema_version": "1.0.0",
  "task_id": "BD-AF-P0-T00",
  "dependencies": [],
  "preflight": {"worktree": "CLEAN_APPROVED_BASELINE", "dependency_status": "NOT_APPLICABLE_NO_DEPENDENCIES", "approval_ids": ["H0_BASELINE_ISOLATION", "H1_TASK_START_PER_TASK"], "network_policy": "DENIED"},
  "criteria": [
    {
      "id": "AC-BD-AF-P0-T00-01", "requirement_ids": ["AF-REQ-001", "AF-REQ-018"], "mandatory": true,
      "statement": "Approved isolated worktree exactly matches the immutable baseline and the original dirty checkout remains untouched.",
      "oracle": {"phase": "POST_IMPLEMENTATION", "argv": ["python", "{package_root}/scripts/baseline_custody.py", "--root", "{package_root}", "verify", "--original-repo", "{original_repo}", "--approved-repo", "{repo}", "--manifest", "{package_root}/baseline/source-custody-manifest.json", "--output", "{evidence_dir}/baseline-custody-verification.json"], "cwd": "REPO_ROOT", "timeout_seconds": 30, "network_policy": "DENIED", "expected_exit_codes": [0], "test_expectation": null},
      "evidence": {"kind": "MANIFEST", "artifacts": ["baseline-custody-verification.json"]},
      "failure_action": "HOLD every downstream task; preserve both manifests and request a new human baseline decision."
    },
    {
      "id": "AC-BD-AF-P0-T00-02", "requirement_ids": ["AF-REQ-001", "AF-REQ-017"], "mandatory": true,
      "statement": "The approved clean baseline compiles and the complete repository test suite collects and executes non-zero tests without hidden skip inflation.",
      "oracle": {"phase": "POST_IMPLEMENTATION", "argv": ["python", "-m", "pytest", "-q", "--junitxml={evidence_dir}/BD-AF-P0-T00-full.xml"], "cwd": "REPO_ROOT", "timeout_seconds": 1800, "network_policy": "DENIED", "expected_exit_codes": [0], "test_expectation": {"junit_path": "{evidence_dir}/BD-AF-P0-T00-full.xml", "minimum_collected": 1, "maximum_skipped": 0, "maximum_xfailed": 0}},
      "evidence": {"kind": "JUNIT", "artifacts": ["BD-AF-P0-T00-full.xml"]},
      "failure_action": "Mark baseline NOT_VERIFIABLE or FAIL; do not classify inherited failures as accepted debt without a separate human decision."
    }
  ],
  "completion": {"required_status": "PASS", "independent_review": true, "zero_open_p0_p1": true, "evidence_manifest_required": true},
  "rollback_rehearsal": {"required": true, "command": ["python", "{package_root}/scripts/baseline_custody.py", "--root", "{package_root}", "verify", "--original-repo", "{original_repo}", "--approved-repo", "{repo}", "--manifest", "{package_root}/baseline/source-custody-manifest.json", "--output", "{evidence_dir}/baseline-custody-rollback.json"], "timeout_seconds": 30, "network_policy": "DENIED", "expected_exit_codes": [0], "test_expectation": null, "evidence": {"kind": "REHEARSAL", "artifacts": ["baseline-custody-rollback.json"]}, "success_oracle": "Original checkout tracked-change custody hash is unchanged and the approved isolated baseline is clean at the manifest commit.", "failure_action": "Stop and escalate as a P0 custody incident; do not attempt automated repair."}
}
