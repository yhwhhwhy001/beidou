{
  "$schema": "../../schemas/acceptance.schema.json",
  "schema_version": "1.0.0",
  "task_id": "BD-AF-P2-T04",
  "dependencies": ["BD-AF-P2-T03"],
  "preflight": {"worktree": "CLEAN_APPROVED_BASELINE", "dependency_status": "ACCEPTED", "approval_ids": ["H1_TASK_START_PER_TASK"], "network_policy": "DENIED"},
  "criteria": [
    {
      "id": "AC-BD-AF-P2-T04-01", "requirement_ids": ["AF-REQ-006", "AF-REQ-017"], "mandatory": true,
      "statement": "Checkpoint canonical form binds all lineage, RNG, search, queue, completed-candidate, multiple-testing, cursor, sequence, hash, version, and fencing state.",
      "oracle": {"phase": "POST_IMPLEMENTATION", "argv": ["python", "-m", "pytest", "tests/research/test_checkpoint_protocol.py", "-q", "--junitxml={evidence_dir}/BD-AF-P2-T04-checkpoint.xml"], "cwd": "REPO_ROOT", "timeout_seconds": 240, "network_policy": "DENIED", "expected_exit_codes": [0], "test_expectation": {"junit_path": "{evidence_dir}/BD-AF-P2-T04-checkpoint.xml", "minimum_collected": 12, "maximum_skipped": 0, "maximum_xfailed": 0}},
      "evidence": {"kind": "MUTATION", "artifacts": ["BD-AF-P2-T04-checkpoint.xml", "checkpoint-field-coverage.json", "canonical-hash-report.json"]},
      "failure_action": "Reject the checkpoint version and block resume; no missing field may receive a generated default."
    },
    {
      "id": "AC-BD-AF-P2-T04-02", "requirement_ids": ["AF-REQ-006"], "mandatory": true,
      "statement": "Partial writes, corrupt hashes, incompatible versions, stale writers, and policy or denominator drift are rejected without fresh-run fallback.",
      "oracle": {"phase": "POST_IMPLEMENTATION", "argv": ["python", "-m", "pytest", "tests/research/test_checkpoint_protocol.py", "-q", "--junitxml={evidence_dir}/BD-AF-P2-T04-recovery.xml"], "cwd": "REPO_ROOT", "timeout_seconds": 240, "network_policy": "DENIED", "expected_exit_codes": [0], "test_expectation": {"junit_path": "{evidence_dir}/BD-AF-P2-T04-recovery.xml", "minimum_collected": 12, "maximum_skipped": 0, "maximum_xfailed": 0}},
      "evidence": {"kind": "REHEARSAL", "artifacts": ["BD-AF-P2-T04-recovery.xml", "atomicity-fencing-report.json", "checkpoint-recovery-transcript.json"]},
      "failure_action": "Return FAIL and retain the last known-good generation plus corrupt artifact; never delete or overwrite either."
    }
  ],
  "completion": {"required_status": "PASS", "independent_review": true, "zero_open_p0_p1": true, "evidence_manifest_required": true},
  "rollback_rehearsal": {"required": true, "command": ["python", "-m", "pytest", "tests/research/test_checkpoint_protocol.py", "-q", "-k", "rollback", "--junitxml={evidence_dir}/RB-BD-AF-P2-T04.xml"], "timeout_seconds": 240, "network_policy": "DENIED", "expected_exit_codes": [0], "test_expectation": {"junit_path": "{evidence_dir}/RB-BD-AF-P2-T04.xml", "minimum_collected": 1, "maximum_skipped": 0, "maximum_xfailed": 0}, "evidence": {"kind": "REHEARSAL", "artifacts": ["RB-BD-AF-P2-T04.xml"]}, "success_oracle": "Checkpoint writes can be disabled and the last compatible generation remains hash-chained to the unchanged ledger.", "failure_action": "Block resume work if rollback deletes state, starts fresh, or cannot distinguish stale and current writers."}
}
