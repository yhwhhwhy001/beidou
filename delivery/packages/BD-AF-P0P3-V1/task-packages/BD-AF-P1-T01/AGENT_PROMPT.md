# Agent prompt — architecture implementer

Execute only after T00 is independently accepted and task preflight is READY. Begin by writing a non-vacuous failing architecture test. Inventory the actual source modules before asserting zero forbidden edges.

Implement neutral contracts and composition-root injection for the smallest usable Alpha slice. Do not move directories merely to satisfy names, add service-locator fallbacks, or let neutral contracts import runtime implementations. Make launcher/safety/exchange packages unimportable in the offline subprocess proof.

Do not modify runtime safety semantics, perform exchange/account access, or broaden allowed paths. Preserve raw graph/mutation/test evidence and hand the result to a different reviewer. Unknown or unclassified imports are failures, not exclusions.
