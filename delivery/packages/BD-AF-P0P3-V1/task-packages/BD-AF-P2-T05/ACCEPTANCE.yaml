{
  "$schema": "../../schemas/acceptance.schema.json",
  "schema_version": "1.0.0",
  "task_id": "BD-AF-P2-T05",
  "dependencies": ["BD-AF-P2-T04"],
  "preflight": {"worktree": "CLEAN_APPROVED_BASELINE", "dependency_status": "ACCEPTED", "approval_ids": ["H1_TASK_START_PER_TASK"], "network_policy": "DENIED"},
  "criteria": [
    {
      "id": "AC-BD-AF-P2-T05-01", "requirement_ids": ["AF-REQ-006", "AF-REQ-018"], "mandatory": true,
      "statement": "The real resume CLI resolves exactly one compatible run and refuses missing, corrupt, ambiguous, mismatched, or already-finalized state.",
      "oracle": {"phase": "POST_IMPLEMENTATION", "argv": ["python", "-m", "pytest", "tests/research/test_factor_miner_resume.py", "-q", "--junitxml={evidence_dir}/BD-AF-P2-T05-resume-cli.xml"], "cwd": "REPO_ROOT", "timeout_seconds": 300, "network_policy": "DENIED", "expected_exit_codes": [0], "test_expectation": {"junit_path": "{evidence_dir}/BD-AF-P2-T05-resume-cli.xml", "minimum_collected": 8, "maximum_skipped": 0, "maximum_xfailed": 0}},
      "evidence": {"kind": "JUNIT", "artifacts": ["BD-AF-P2-T05-resume-cli.xml", "resume-resolution-manifest.json"]},
      "failure_action": "Disable resume routing and return FAIL; a rejected resume request must never fall through to a fresh run."
    },
    {
      "id": "AC-BD-AF-P2-T05-02", "requirement_ids": ["AF-REQ-006"], "mandatory": true,
      "statement": "Every durable-boundary crash resumes to the same normalized candidates, evidence, rejections, family denominator, and final result as uninterrupted execution.",
      "oracle": {"phase": "POST_IMPLEMENTATION", "argv": ["python", "-m", "pytest", "tests/research/test_resume_crash_matrix.py", "-q", "--junitxml={evidence_dir}/BD-AF-P2-T05-crash-matrix.xml"], "cwd": "REPO_ROOT", "timeout_seconds": 600, "network_policy": "DENIED", "expected_exit_codes": [0], "test_expectation": {"junit_path": "{evidence_dir}/BD-AF-P2-T05-crash-matrix.xml", "minimum_collected": 12, "maximum_skipped": 0, "maximum_xfailed": 0}},
      "evidence": {"kind": "DIFFERENTIAL", "artifacts": ["BD-AF-P2-T05-crash-matrix.xml", "uninterrupted-result.json", "resumed-result.json", "candidate-family-hashes.json"]},
      "failure_action": "Mark resume semantics FAIL and preserve the first divergent boundary; do not regenerate candidates or shrink the family."
    }
  ],
  "completion": {"required_status": "PASS", "independent_review": true, "zero_open_p0_p1": true, "evidence_manifest_required": true},
  "rollback_rehearsal": {"required": true, "command": ["python", "-m", "pytest", "tests/research/test_factor_miner_resume.py", "tests/research/test_resume_crash_matrix.py", "-q", "-k", "rollback", "--junitxml={evidence_dir}/RB-BD-AF-P2-T05.xml"], "timeout_seconds": 600, "network_policy": "DENIED", "expected_exit_codes": [0], "test_expectation": {"junit_path": "{evidence_dir}/RB-BD-AF-P2-T05.xml", "minimum_collected": 1, "maximum_skipped": 0, "maximum_xfailed": 0}, "evidence": {"kind": "REHEARSAL", "artifacts": ["RB-BD-AF-P2-T05.xml"]}, "success_oracle": "Disabling resume makes every resume request fail explicitly while frozen fresh-run behavior remains characterized.", "failure_action": "Block acceptance if resume falls through, history changes, or statistical family membership differs."}
}
