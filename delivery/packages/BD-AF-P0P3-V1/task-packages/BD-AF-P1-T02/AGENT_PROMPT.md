# Agent prompt — CLI boundary implementer

Start only after T01 acceptance and READY preflight. Characterize the installed console script, not just a Python function. Add an independent side-effect inventory before changing behavior.

Implement a minimal command facade: safe help/status behavior at the root, explicit offline Alpha commands, and an explicit execution composition route. Never execute the execution route during this task. Do not read credentials or query account/exchange state.

Use isolated subprocess tests with denied imports/network and a temporary filesystem. Missing instrumentation is NOT_VERIFIABLE, not zero side effects. Preserve CLI stdout/exit contracts and write-registry evidence for independent review.
