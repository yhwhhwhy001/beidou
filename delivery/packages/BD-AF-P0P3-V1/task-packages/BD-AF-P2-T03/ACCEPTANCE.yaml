{
  "$schema": "../../schemas/acceptance.schema.json",
  "schema_version": "1.0.0",
  "task_id": "BD-AF-P2-T03",
  "dependencies": ["BD-AF-P1-T02"],
  "preflight": {"worktree": "CLEAN_APPROVED_BASELINE", "dependency_status": "ACCEPTED", "approval_ids": ["H1_TASK_START_PER_TASK"], "network_policy": "DENIED"},
  "criteria": [
    {
      "id": "AC-BD-AF-P2-T03-01", "requirement_ids": ["AF-REQ-005", "AF-REQ-017"], "mandatory": true,
      "statement": "ExperimentRun identity binds every declared code, policy, data, feature, label, cost, seed, universe, timeframe, and schema input.",
      "oracle": {"phase": "POST_IMPLEMENTATION", "argv": ["python", "-m", "pytest", "tests/research/test_experiment_run_ledger.py", "-q", "--junitxml={evidence_dir}/BD-AF-P2-T03-ledger.xml"], "cwd": "REPO_ROOT", "timeout_seconds": 240, "network_policy": "DENIED", "expected_exit_codes": [0], "test_expectation": {"junit_path": "{evidence_dir}/BD-AF-P2-T03-ledger.xml", "minimum_collected": 10, "maximum_skipped": 0, "maximum_xfailed": 0}},
      "evidence": {"kind": "JUNIT", "artifacts": ["BD-AF-P2-T03-ledger.xml", "experiment-run-schema.json", "identity-field-mutation.json"]},
      "failure_action": "Reject the ledger schema and prohibit downstream checkpoint work until every identity mutation changes or invalidates the run."
    },
    {
      "id": "AC-BD-AF-P2-T03-02", "requirement_ids": ["AF-REQ-005"], "mandatory": true,
      "statement": "Append, idempotency conflict, stale writer, corrupt chain, unknown version, concurrency, and replay semantics fail closed and reproduce one state.",
      "oracle": {"phase": "POST_IMPLEMENTATION", "argv": ["python", "-m", "pytest", "tests/research/test_experiment_run_ledger.py", "-q", "--junitxml={evidence_dir}/BD-AF-P2-T03-state-machine.xml"], "cwd": "REPO_ROOT", "timeout_seconds": 240, "network_policy": "DENIED", "expected_exit_codes": [0], "test_expectation": {"junit_path": "{evidence_dir}/BD-AF-P2-T03-state-machine.xml", "minimum_collected": 10, "maximum_skipped": 0, "maximum_xfailed": 0}},
      "evidence": {"kind": "MUTATION", "artifacts": ["BD-AF-P2-T03-state-machine.xml", "ledger-replay-hashes.json", "concurrency-fencing-report.json"]},
      "failure_action": "Disable new ledger writes, preserve the corrupt fixture, and return FAIL; never repair or skip an event silently."
    }
  ],
  "completion": {"required_status": "PASS", "independent_review": true, "zero_open_p0_p1": true, "evidence_manifest_required": true},
  "rollback_rehearsal": {"required": true, "command": ["python", "-m", "pytest", "tests/research/test_experiment_run_ledger.py", "-q", "-k", "rollback", "--junitxml={evidence_dir}/RB-BD-AF-P2-T03.xml"], "timeout_seconds": 240, "network_policy": "DENIED", "expected_exit_codes": [0], "test_expectation": {"junit_path": "{evidence_dir}/RB-BD-AF-P2-T03.xml", "minimum_collected": 1, "maximum_skipped": 0, "maximum_xfailed": 0}, "evidence": {"kind": "REHEARSAL", "artifacts": ["RB-BD-AF-P2-T03.xml"]}, "success_oracle": "New writes can be disabled while immutable events export and replay from a copied store with identical hashes.", "failure_action": "Block acceptance if migration is destructive, events change, or the prior read path cannot be selected explicitly."}
}
