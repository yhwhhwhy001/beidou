# Agent prompt — portfolio decision implementer

Do not start without a named Portfolio Owner and frozen decision policy. Characterize the current `NOT_IMPLEMENTED` compare path and create standalone-Sharpe counterexamples first.

Implement an offline, immutable Candidate-vs-Champion decision that recomputes incremental portfolio contribution with uncertainty, correlation, turnover/cost, capacity, tail, and regime inputs. Bind all inputs to accepted T06/T07 evidence.

Do not emit Paper/Testnet promotion, allocate capital, query accounts, invent missing inputs, or treat standalone metrics as sufficient. Missing facts are `NOT_VERIFIABLE`. Preserve decision bundles and independent recomputation for a different reviewer.
